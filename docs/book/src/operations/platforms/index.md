# Platform Operations

The [platform-support architecture](../../architecture/platform-support.md)
defines the support tiers and portability boundaries. The pages in this
section cover installation, service integration, filesystem prerequisites,
and platform-specific operational checks.

A platform is not promoted to a supported tier until its native build,
package installation, authenticated API workflow, and mount lifecycle gates
all pass without soft skips.
