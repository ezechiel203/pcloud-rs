# Complete internal module and helper catalog

This is the exhaustive implementation-unit layer of the feature encyclopedia.
It includes runtime modules, private helpers, binaries, build scripts, examples,
benchmarks, fuzz targets, and tests. The adjacent generated crate pages index
every named Rust declaration inside these units.

**Coverage: 499 of 499 Rust source/test/helper files owned by Cargo packages.**

> A file being present means an implementation or verification surface exists; it
> does not automatically make that surface public, enabled, wired, or release-qualified.

## `pcloud-auth` (10 units)

Package context: Interactive and unattended authentication flows, token refresh, idle expiry, and safe recovery from authentication challenges.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-auth/src/commands.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-auth/src/commands.rs) | runtime/library unit | Commands that drive the \[`crate::manager::SessionManager`\] state | Keep this responsibility explicit: Commands that drive the \[`crate::manager::SessionManager`\] state. A dedicated commands unit localizes dependencies, review, and regression testing inside `pcloud-auth`. | The focused `pcloud-auth` responsibility described as: Commands that drive the \[`crate::manager::SessionManager`\] state. Typed commands, states, and secret-free events make invalid transitions visible and independently testable. This unit contains 4 indexed Rust declarations. |
| [`crates/pcloud-auth/src/events.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-auth/src/events.rs) | runtime/library unit | Typed events emitted by the auth state machine. Events never carry | Keep this responsibility explicit: Typed events emitted by the auth state machine. Events never carry. A dedicated events unit localizes dependencies, review, and regression testing inside `pcloud-auth`. | The focused `pcloud-auth` responsibility described as: Typed events emitted by the auth state machine. Events never carry. Typed commands, states, and secret-free events make invalid transitions visible and independently testable. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-auth/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-auth/src/lib.rs) | runtime/library unit | pcloud-auth | Declare and compose the `pcloud-auth` crate boundary. Keep login, TFA, refresh, logout, and session transitions in one explicit state machine instead of scattering credential rules through callers. | The focused `pcloud-auth` responsibility described as: pcloud-auth. Typed commands, states, and secret-free events make invalid transitions visible and independently testable. This unit contains 9 indexed Rust declarations. |
| [`crates/pcloud-auth/src/lifecycle.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-auth/src/lifecycle.rs) | runtime/library unit | Session lifecycle tracking: proactive refresh, idle logout, and | Keep this responsibility explicit: Session lifecycle tracking: proactive refresh, idle logout, and. A dedicated lifecycle unit localizes dependencies, review, and regression testing inside `pcloud-auth`. | The focused `pcloud-auth` responsibility described as: Session lifecycle tracking: proactive refresh, idle logout, and. Typed commands, states, and secret-free events make invalid transitions visible and independently testable. This unit contains 40 indexed Rust declarations. |
| [`crates/pcloud-auth/src/manager.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-auth/src/manager.rs) | runtime/library unit | \[`SessionManager`\] — owns the auth state machine, the live auth | Keep this responsibility explicit: \[`SessionManager`\] — owns the auth state machine, the live auth. A dedicated manager unit localizes dependencies, review, and regression testing inside `pcloud-auth`. | The focused `pcloud-auth` responsibility described as: \[`SessionManager`\] — owns the auth state machine, the live auth. Typed commands, states, and secret-free events make invalid transitions visible and independently testable. This unit contains 18 indexed Rust declarations. |
| [`crates/pcloud-auth/src/orchestrator.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-auth/src/orchestrator.rs) | runtime/library unit | \[`ProtocolAuthFlow`\] — glue between the I/O-free | Keep this responsibility explicit: \[`ProtocolAuthFlow`\] — glue between the I/O-free. A dedicated orchestrator unit localizes dependencies, review, and regression testing inside `pcloud-auth`. | The focused `pcloud-auth` responsibility described as: \[`ProtocolAuthFlow`\] — glue between the I/O-free. Typed commands, states, and secret-free events make invalid transitions visible and independently testable. This unit contains 32 indexed Rust declarations. |
| [`crates/pcloud-auth/src/refresh.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-auth/src/refresh.rs) | runtime/library unit | Refresh coordinator: glue between `SessionManager`, `RefreshPolicy`, | Keep this responsibility explicit: Refresh coordinator: glue between `SessionManager`, `RefreshPolicy`,. A dedicated refresh unit localizes dependencies, review, and regression testing inside `pcloud-auth`. | The focused `pcloud-auth` responsibility described as: Refresh coordinator: glue between `SessionManager`, `RefreshPolicy`,. Typed commands, states, and secret-free events make invalid transitions visible and independently testable. This unit contains 19 indexed Rust declarations. |
| [`crates/pcloud-auth/src/state.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-auth/src/state.rs) | runtime/library unit | State types tracked by \[`crate::manager::SessionManager`\]. | Keep this responsibility explicit: State types tracked by \[`crate::manager::SessionManager`\]. A dedicated state unit localizes dependencies, review, and regression testing inside `pcloud-auth`. | The focused `pcloud-auth` responsibility described as: State types tracked by \[`crate::manager::SessionManager`\]. Typed commands, states, and secret-free events make invalid transitions visible and independently testable. This unit contains 5 indexed Rust declarations. |
| [`crates/pcloud-auth/tests/auth_session.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-auth/tests/auth_session.rs) | integration test | Integration tests for the `pcloud-auth` session state machine. | Prove the auth session behavior at a crate or process boundary instead of relying only on unit tests. | Protecting auth session boundary behavior from regressions. Typed commands, states, and secret-free events make invalid transitions visible and independently testable. This unit contains 17 indexed Rust declarations. |
| [`crates/pcloud-auth/tests/orchestrator_coverage.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-auth/tests/orchestrator_coverage.rs) | integration test | Public-API coverage for protocol-backed authentication orchestration. | Prove the orchestrator coverage behavior at a crate or process boundary instead of relying only on unit tests. | Protecting orchestrator coverage boundary behavior from regressions. Typed commands, states, and secret-free events make invalid transitions visible and independently testable. This unit contains 12 indexed Rust declarations. |

All declarations: [`pcloud-auth` generated crate page](../crates/pcloud-auth.md).

## `pcloud-backends` (27 units)

Package context: Daemon business logic and the canonical RemoteFs service used by CLI, SDK, sync, mount, and web adapters.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-backends/src/account_backend.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/src/account_backend.rs) | runtime/library unit | Account-level operations backend: email verification, lost/change | Keep this responsibility explicit: Account-level operations backend: email verification, lost/change. A dedicated account backend unit localizes dependencies, review, and regression testing inside `pcloud-backends`. | The focused `pcloud-backends` responsibility described as: Account-level operations backend: email verification, lost/change. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 41 indexed Rust declarations. |
| [`crates/pcloud-backends/src/account_backend_tests.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/src/account_backend_tests.rs) | runtime/library unit | Unit tests for `account_backend`: exercises the typed happy-path and | Keep this responsibility explicit: Unit tests for `account_backend`: exercises the typed happy-path and. A dedicated account backend tests unit localizes dependencies, review, and regression testing inside `pcloud-backends`. | The focused `pcloud-backends` responsibility described as: Unit tests for `account_backend`: exercises the typed happy-path and. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 5 indexed Rust declarations. |
| [`crates/pcloud-backends/src/auth_backend.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/src/auth_backend.rs) | runtime/library unit | Authentication backend: password/token login, TFA code and recovery | Keep this responsibility explicit: Authentication backend: password/token login, TFA code and recovery. A dedicated auth backend unit localizes dependencies, review, and regression testing inside `pcloud-backends`. | The focused `pcloud-backends` responsibility described as: Authentication backend: password/token login, TFA code and recovery. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 44 indexed Rust declarations. |
| [`crates/pcloud-backends/src/backup_backend.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/src/backup_backend.rs) | runtime/library unit | Backup / device runtime backend. | Keep this responsibility explicit: Backup / device runtime backend. A dedicated backup backend unit localizes dependencies, review, and regression testing inside `pcloud-backends`. | The focused `pcloud-backends` responsibility described as: Backup / device runtime backend. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 68 indexed Rust declarations. |
| [`crates/pcloud-backends/src/crypto_backend.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/src/crypto_backend.rs) | runtime/library unit | Daemon-side transport runtime for the crypto password-change family. | Keep this responsibility explicit: Daemon-side transport runtime for the crypto password-change family. A dedicated crypto backend unit localizes dependencies, review, and regression testing inside `pcloud-backends`. | The focused `pcloud-backends` responsibility described as: Daemon-side transport runtime for the crypto password-change family. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 32 indexed Rust declarations. |
| [`crates/pcloud-backends/src/folder_backend.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/src/folder_backend.rs) | runtime/library unit | Folder runtime backend. | Keep this responsibility explicit: Folder runtime backend. A dedicated folder backend unit localizes dependencies, review, and regression testing inside `pcloud-backends`. | The focused `pcloud-backends` responsibility described as: Folder runtime backend. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 57 indexed Rust declarations. |
| [`crates/pcloud-backends/src/ignore_patterns.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/src/ignore_patterns.rs) | runtime/library unit | Filename glob matcher for the `ignorepatterns` setting. | Keep this responsibility explicit: Filename glob matcher for the `ignorepatterns` setting. A dedicated ignore patterns unit localizes dependencies, review, and regression testing inside `pcloud-backends`. | The focused `pcloud-backends` responsibility described as: Filename glob matcher for the `ignorepatterns` setting. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 18 indexed Rust declarations. |
| [`crates/pcloud-backends/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/src/lib.rs) | runtime/library unit | pcloud-backends | Declare and compose the `pcloud-backends` crate boundary. Turn low-level pCloud protocol methods into coherent filesystem, account, sharing, backup, transfer, residency, and snapshot behaviors. | The focused `pcloud-backends` responsibility described as: pcloud-backends. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 22 indexed Rust declarations. |
| [`crates/pcloud-backends/src/mock.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/src/mock.rs) | runtime/library unit | Shared mock primitives for backend integration testing. | Keep this responsibility explicit: Shared mock primitives for backend integration testing. A dedicated mock unit localizes dependencies, review, and regression testing inside `pcloud-backends`. | The focused `pcloud-backends` responsibility described as: Shared mock primitives for backend integration testing. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 44 indexed Rust declarations. |
| [`crates/pcloud-backends/src/mount_discovery.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/src/mount_discovery.rs) | runtime/library unit | Mount-point and ignore-path auto-discovery for folder syncability checks. | Keep this responsibility explicit: Mount-point and ignore-path auto-discovery for folder syncability checks. A dedicated mount discovery unit localizes dependencies, review, and regression testing inside `pcloud-backends`. | The focused `pcloud-backends` responsibility described as: Mount-point and ignore-path auto-discovery for folder syncability checks. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 40 indexed Rust declarations. |
| [`crates/pcloud-backends/src/notifications_backend.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/src/notifications_backend.rs) | runtime/library unit | Notifications runtime backend. | Keep this responsibility explicit: Notifications runtime backend. A dedicated notifications backend unit localizes dependencies, review, and regression testing inside `pcloud-backends`. | The focused `pcloud-backends` responsibility described as: Notifications runtime backend. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 38 indexed Rust declarations. |
| [`crates/pcloud-backends/src/path_resolver.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/src/path_resolver.rs) | runtime/library unit | Production-grade \[`PublicLinkPathResolver`\] backed by the pCloud drive | Keep this responsibility explicit: Production-grade \[`PublicLinkPathResolver`\] backed by the pCloud drive. A dedicated path resolver unit localizes dependencies, review, and regression testing inside `pcloud-backends`. | The focused `pcloud-backends` responsibility described as: Production-grade \[`PublicLinkPathResolver`\] backed by the pCloud drive. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 53 indexed Rust declarations. |
| [`crates/pcloud-backends/src/public_link_backend.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/src/public_link_backend.rs) | runtime/library unit | Public-link backend: file/folder public link create/list/show/delete, | Keep this responsibility explicit: Public-link backend: file/folder public link create/list/show/delete,. A dedicated public link backend unit localizes dependencies, review, and regression testing inside `pcloud-backends`. | The focused `pcloud-backends` responsibility described as: Public-link backend: file/folder public link create/list/show/delete,. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 72 indexed Rust declarations. |
| [`crates/pcloud-backends/src/remote_fs.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/src/remote_fs.rs) | runtime/library unit | Canonical, cache-independent remote filesystem service. | Keep this responsibility explicit: Canonical, cache-independent remote filesystem service. A dedicated remote fs unit localizes dependencies, review, and regression testing inside `pcloud-backends`. | The focused `pcloud-backends` responsibility described as: Canonical, cache-independent remote filesystem service. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 96 indexed Rust declarations. |
| [`crates/pcloud-backends/src/residency.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/src/residency.rs) | runtime/library unit | Data-residency region resolver, cache, enforcement, and audit helpers. | Keep this responsibility explicit: Data-residency region resolver, cache, enforcement, and audit helpers. A dedicated residency unit localizes dependencies, review, and regression testing inside `pcloud-backends`. | The focused `pcloud-backends` responsibility described as: Data-residency region resolver, cache, enforcement, and audit helpers. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 34 indexed Rust declarations. |
| [`crates/pcloud-backends/src/shares_backend.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/src/shares_backend.rs) | runtime/library unit | Shares / business / teams backend: share request listing, share | Keep this responsibility explicit: Shares / business / teams backend: share request listing, share. A dedicated shares backend unit localizes dependencies, review, and regression testing inside `pcloud-backends`. | The focused `pcloud-backends` responsibility described as: Shares / business / teams backend: share request listing, share. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 71 indexed Rust declarations. |
| [`crates/pcloud-backends/src/snapshot.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/src/snapshot.rs) | runtime/library unit | Backup snapshot builder (H12b/c): tar + manifest + SQLite VACUUM core, | Keep this responsibility explicit: Backup snapshot builder (H12b/c): tar + manifest + SQLite VACUUM core,. A dedicated snapshot unit localizes dependencies, review, and regression testing inside `pcloud-backends`. | The focused `pcloud-backends` responsibility described as: Backup snapshot builder (H12b/c): tar + manifest + SQLite VACUUM core,. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 80 indexed Rust declarations. |
| [`crates/pcloud-backends/src/sync_backend.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/src/sync_backend.rs) | runtime/library unit | Sync-root lifecycle backend: authenticated add with remote-folder | Keep this responsibility explicit: Sync-root lifecycle backend: authenticated add with remote-folder. A dedicated sync backend unit localizes dependencies, review, and regression testing inside `pcloud-backends`. | The focused `pcloud-backends` responsibility described as: Sync-root lifecycle backend: authenticated add with remote-folder. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 80 indexed Rust declarations. |
| [`crates/pcloud-backends/src/sync_suggest.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/src/sync_suggest.rs) | runtime/library unit | Extension-weighted sync-folder suggestion scorer. | Keep this responsibility explicit: Extension-weighted sync-folder suggestion scorer. A dedicated sync suggest unit localizes dependencies, review, and regression testing inside `pcloud-backends`. | The focused `pcloud-backends` responsibility described as: Extension-weighted sync-folder suggestion scorer. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 44 indexed Rust declarations. |
| [`crates/pcloud-backends/src/transfer_backend.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/src/transfer_backend.rs) | runtime/library unit | Transfer backend: `getfilelink` resolution, signed HTTP download | Keep this responsibility explicit: Transfer backend: `getfilelink` resolution, signed HTTP download. A dedicated transfer backend unit localizes dependencies, review, and regression testing inside `pcloud-backends`. | The focused `pcloud-backends` responsibility described as: Transfer backend: `getfilelink` resolution, signed HTTP download. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 143 indexed Rust declarations. |
| [`crates/pcloud-backends/src/upload_journal.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/src/upload_journal.rs) | runtime/library unit | Crash-safe upload resume journal. | Keep this responsibility explicit: Crash-safe upload resume journal. A dedicated upload journal unit localizes dependencies, review, and regression testing inside `pcloud-backends`. | The focused `pcloud-backends` responsibility described as: Crash-safe upload resume journal. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 25 indexed Rust declarations. |
| [`crates/pcloud-backends/src/upload_sessions.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/src/upload_sessions.rs) | runtime/library unit | High-level upload-session registry (pause / resume / cancel / list). | Keep this responsibility explicit: High-level upload-session registry (pause / resume / cancel / list). A dedicated upload sessions unit localizes dependencies, review, and regression testing inside `pcloud-backends`. | The focused `pcloud-backends` responsibility described as: High-level upload-session registry (pause / resume / cancel / list). ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 39 indexed Rust declarations. |
| [`crates/pcloud-backends/src/upload_state.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/src/upload_state.rs) | runtime/library unit | Upload resume state machine. | Keep this responsibility explicit: Upload resume state machine. A dedicated upload state unit localizes dependencies, review, and regression testing inside `pcloud-backends`. | The focused `pcloud-backends` responsibility described as: Upload resume state machine. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 52 indexed Rust declarations. |
| [`crates/pcloud-backends/tests/crypto_share_rsa_e2e.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/tests/crypto_share_rsa_e2e.rs) | integration test | Mock-backed two-account end-to-end proof for the RSA-4096-OAEP | Prove the crypto share rsa e2e behavior at a crate or process boundary instead of relying only on unit tests. | Protecting crypto share rsa e2e boundary behavior from regressions. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 14 indexed Rust declarations. |
| [`crates/pcloud-backends/tests/folder_coverage.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/tests/folder_coverage.rs) | integration test | Public folder-runtime coverage over the deterministic development transport. | Prove the folder coverage behavior at a crate or process boundary instead of relying only on unit tests. | Protecting folder coverage boundary behavior from regressions. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 5 indexed Rust declarations. |
| [`crates/pcloud-backends/tests/mock_integration.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/tests/mock_integration.rs) | integration test | Shared integration smoke tests for the per-backend `mock` submodules | Prove the mock integration behavior at a crate or process boundary instead of relying only on unit tests. | Protecting mock integration boundary behavior from regressions. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 11 indexed Rust declarations. |
| [`crates/pcloud-backends/tests/transfer_coverage.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-backends/tests/transfer_coverage.rs) | integration test | \[test\] | Prove the transfer coverage behavior at a crate or process boundary instead of relying only on unit tests. | Protecting transfer coverage boundary behavior from regressions. ID-first operations, narrow subsystem backends, durable transfer state, and a single remote namespace prevent competing interpretations of cloud state. This unit contains 5 indexed Rust declarations. |

All declarations: [`pcloud-backends` generated crate page](../crates/pcloud-backends.md).

## `pcloud-cache` (8 units)

Package context: Metadata/page reuse, checksum reuse, bounded staging, eviction, and optional encrypted local cache blobs.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-cache/examples/warm_cache.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cache/examples/warm_cache.rs) | example | Constructs a `PageCacheGeneric&lt;String&gt;`, warms it with 16 pages, then | Show a runnable warm cache workflow and keep the intended call sequence discoverable. | Teaching and smoke-testing the warm cache call path. Bounded LRU-style structures and explicit staging/cipher boundaries make memory use and invalidation behavior controllable. This unit contains 3 indexed Rust declarations. |
| [`crates/pcloud-cache/src/checksum_cache.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cache/src/checksum_cache.rs) | runtime/library unit | Entry-count bounded cache of local file checksums. | Keep this responsibility explicit: Entry-count bounded cache of local file checksums. A dedicated checksum cache unit localizes dependencies, review, and regression testing inside `pcloud-cache`. | The focused `pcloud-cache` responsibility described as: Entry-count bounded cache of local file checksums. Bounded LRU-style structures and explicit staging/cipher boundaries make memory use and invalidation behavior controllable. This unit contains 2 indexed Rust declarations. |
| [`crates/pcloud-cache/src/cipher.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cache/src/cipher.rs) | runtime/library unit | T2.3.a — Encryption-at-rest for the local cache. | Keep this responsibility explicit: T2.3.a — Encryption-at-rest for the local cache. A dedicated cipher unit localizes dependencies, review, and regression testing inside `pcloud-cache`. | The focused `pcloud-cache` responsibility described as: T2.3.a — Encryption-at-rest for the local cache. Bounded LRU-style structures and explicit staging/cipher boundaries make memory use and invalidation behavior controllable. This unit contains 30 indexed Rust declarations. |
| [`crates/pcloud-cache/src/eviction.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cache/src/eviction.rs) | runtime/library unit | Declarative eviction-policy tag attached to a \[`crate::CacheShell`\]. | Keep this responsibility explicit: Declarative eviction-policy tag attached to a \[`crate::CacheShell`\]. A dedicated eviction unit localizes dependencies, review, and regression testing inside `pcloud-cache`. | The focused `pcloud-cache` responsibility described as: Declarative eviction-policy tag attached to a \[`crate::CacheShell`\]. Bounded LRU-style structures and explicit staging/cipher boundaries make memory use and invalidation behavior controllable. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-cache/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cache/src/lib.rs) | runtime/library unit | pcloud-cache | Declare and compose the `pcloud-cache` crate boundary. Reduce latency and repeated I/O without allowing cached data to become remote truth. | The focused `pcloud-cache` responsibility described as: pcloud-cache. Bounded LRU-style structures and explicit staging/cipher boundaries make memory use and invalidation behavior controllable. This unit contains 14 indexed Rust declarations. |
| [`crates/pcloud-cache/src/page_cache_generic.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cache/src/page_cache_generic.rs) | runtime/library unit | Key-typed generic LRU page cache. Canonical string-keyed page cache | Keep this responsibility explicit: Key-typed generic LRU page cache. Canonical string-keyed page cache. A dedicated page cache generic unit localizes dependencies, review, and regression testing inside `pcloud-cache`. | The focused `pcloud-cache` responsibility described as: Key-typed generic LRU page cache. Canonical string-keyed page cache. Bounded LRU-style structures and explicit staging/cipher boundaries make memory use and invalidation behavior controllable. This unit contains 54 indexed Rust declarations. |
| [`crates/pcloud-cache/src/sealed_blob.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cache/src/sealed_blob.rs) | runtime/library unit | T2.3.b — disk-shaped wrapper around \[`CacheCipher`\]. | Keep this responsibility explicit: T2.3.b — disk-shaped wrapper around \[`CacheCipher`\]. A dedicated sealed blob unit localizes dependencies, review, and regression testing inside `pcloud-cache`. | The focused `pcloud-cache` responsibility described as: T2.3.b — disk-shaped wrapper around \[`CacheCipher`\]. Bounded LRU-style structures and explicit staging/cipher boundaries make memory use and invalidation behavior controllable. This unit contains 12 indexed Rust declarations. |
| [`crates/pcloud-cache/src/staging.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cache/src/staging.rs) | runtime/library unit | In-memory staging buffer for in-flight local writes. | Keep this responsibility explicit: In-memory staging buffer for in-flight local writes. A dedicated staging unit localizes dependencies, review, and regression testing inside `pcloud-cache`. | The focused `pcloud-cache` responsibility described as: In-memory staging buffer for in-flight local writes. Bounded LRU-style structures and explicit staging/cipher boundaries make memory use and invalidation behavior controllable. This unit contains 17 indexed Rust declarations. |

All declarations: [`pcloud-cache` generated crate page](../crates/pcloud-cache.md).

## `pcloud-chaos` (6 units)

Package context: Disk-full, process-kill, slow-peer, clock-jump, and network-blackhole recovery validation.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-chaos/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-chaos/src/lib.rs) | runtime/library unit | pcloud-chaos | Declare and compose the `pcloud-chaos` crate boundary. Exercise failure modes that ordinary success-path tests cannot prove. | The focused `pcloud-chaos` responsibility described as: pcloud-chaos. Scripted fault scenarios have predicted outcomes, allowing crash safety and resilience claims to be falsified repeatably. This unit contains 2 indexed Rust declarations. |
| [`crates/pcloud-chaos/tests/blackhole_trips_breaker.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-chaos/tests/blackhole_trips_breaker.rs) | integration test | Scenario 3: blackhole at connect → circuit breaker trips, retries back off. | Prove the blackhole trips breaker behavior at a crate or process boundary instead of relying only on unit tests. | Protecting blackhole trips breaker boundary behavior from regressions. Scripted fault scenarios have predicted outcomes, allowing crash safety and resilience claims to be falsified repeatably. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-chaos/tests/clock_jump_ttl.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-chaos/tests/clock_jump_ttl.rs) | integration test | Scenario 4: 30 s clock jump forward → TTL-based caches re-fetch, don't crash. | Prove the clock jump ttl behavior at a crate or process boundary instead of relying only on unit tests. | Protecting clock jump ttl boundary behavior from regressions. Scripted fault scenarios have predicted outcomes, allowing crash safety and resilience claims to be falsified repeatably. This unit contains 4 indexed Rust declarations. |
| [`crates/pcloud-chaos/tests/disk_full_journal.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-chaos/tests/disk_full_journal.rs) | integration test | Scenario 2: disk-full on journal write → typed error, no panic. | Prove the disk full journal behavior at a crate or process boundary instead of relying only on unit tests. | Protecting disk full journal boundary behavior from regressions. Scripted fault scenarios have predicted outcomes, allowing crash safety and resilience claims to be falsified repeatably. This unit contains 9 indexed Rust declarations. |
| [`crates/pcloud-chaos/tests/sigkill_mid_flush.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-chaos/tests/sigkill_mid_flush.rs) | integration test | Scenario 1: SIGKILL mid-flush → journal replay completes without panic. | Prove the sigkill mid flush behavior at a crate or process boundary instead of relying only on unit tests. | Protecting sigkill mid flush boundary behavior from regressions. Scripted fault scenarios have predicted outcomes, allowing crash safety and resilience claims to be falsified repeatably. This unit contains 5 indexed Rust declarations. |
| [`crates/pcloud-chaos/tests/slowloris_timeout.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-chaos/tests/slowloris_timeout.rs) | integration test | Scenario 5: slowloris partial response → per-request timeout fires. | Prove the slowloris timeout behavior at a crate or process boundary instead of relying only on unit tests. | Protecting slowloris timeout boundary behavior from regressions. Scripted fault scenarios have predicted outcomes, allowing crash safety and resilience claims to be falsified repeatably. This unit contains 2 indexed Rust declarations. |

All declarations: [`pcloud-chaos` generated crate page](../crates/pcloud-chaos.md).

## `pcloud-cli` (22 units)

Package context: Login, status, file operations, sync, backup, Crypto, account administration, diagnostics, migration, JSON output, and shell completion.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-cli/build.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/build.rs) | build helper | Build script: embed the short git commit hash into the binary as | Perform the build package's deterministic build-time preparation outside runtime code. | Reproducible preparation for the package build. Typed IPC commands, stable exit codes, field selection, redacted prompts, and machine-readable output serve both operators and scripts. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-cli/src/app.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/app.rs) | runtime/library unit | **PLATFORM:** all supported daemon platforms. | Keep this responsibility explicit: **PLATFORM:** all supported daemon platforms. A dedicated app unit localizes dependencies, review, and regression testing inside `pcloud-cli`. | The focused `pcloud-cli` responsibility described as: **PLATFORM:** all supported daemon platforms. Typed IPC commands, stable exit codes, field selection, redacted prompts, and machine-readable output serve both operators and scripts. This unit contains 157 indexed Rust declarations. |
| [`crates/pcloud-cli/src/commands.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs) | runtime/library unit | Parsed-command enumeration and its mapping onto typed IPC | Keep this responsibility explicit: Parsed-command enumeration and its mapping onto typed IPC. A dedicated commands unit localizes dependencies, review, and regression testing inside `pcloud-cli`. | The focused `pcloud-cli` responsibility described as: Parsed-command enumeration and its mapping onto typed IPC. Typed IPC commands, stable exit codes, field selection, redacted prompts, and machine-readable output serve both operators and scripts. This unit contains 13 indexed Rust declarations. |
| [`crates/pcloud-cli/src/completion.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/completion.rs) | runtime/library unit | Shell completion generation. | Keep this responsibility explicit: Shell completion generation. A dedicated completion unit localizes dependencies, review, and regression testing inside `pcloud-cli`. | The focused `pcloud-cli` responsibility described as: Shell completion generation. Typed IPC commands, stable exit codes, field selection, redacted prompts, and machine-readable output serve both operators and scripts. This unit contains 13 indexed Rust declarations. |
| [`crates/pcloud-cli/src/config.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/config.rs) | runtime/library unit | User-facing CLI config file at `~/.pcloud/config.toml` (or a path | Keep this responsibility explicit: User-facing CLI config file at `~/.pcloud/config.toml` (or a path. A dedicated config unit localizes dependencies, review, and regression testing inside `pcloud-cli`. | The focused `pcloud-cli` responsibility described as: User-facing CLI config file at `~/.pcloud/config.toml` (or a path. Typed IPC commands, stable exit codes, field selection, redacted prompts, and machine-readable output serve both operators and scripts. This unit contains 17 indexed Rust declarations. |
| [`crates/pcloud-cli/src/crypto_setup_picker.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/crypto_setup_picker.rs) | runtime/library unit | Interactive crypto-backend picker for `pcloudc crypto setup`. | Keep this responsibility explicit: Interactive crypto-backend picker for `pcloudc crypto setup`. A dedicated crypto setup picker unit localizes dependencies, review, and regression testing inside `pcloud-cli`. | The focused `pcloud-cli` responsibility described as: Interactive crypto-backend picker for `pcloudc crypto setup`. Typed IPC commands, stable exit codes, field selection, redacted prompts, and machine-readable output serve both operators and scripts. This unit contains 14 indexed Rust declarations. |
| [`crates/pcloud-cli/src/doctor.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/doctor.rs) | runtime/library unit | `pcloudc doctor` — self-diagnostic command. | Keep this responsibility explicit: `pcloudc doctor` — self-diagnostic command. A dedicated doctor unit localizes dependencies, review, and regression testing inside `pcloud-cli`. | The focused `pcloud-cli` responsibility described as: `pcloudc doctor` — self-diagnostic command. Typed IPC commands, stable exit codes, field selection, redacted prompts, and machine-readable output serve both operators and scripts. This unit contains 72 indexed Rust declarations. |
| [`crates/pcloud-cli/src/exit_code.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/exit_code.rs) | runtime/library unit | Enterprise exit-code discipline for `pcloud-cli`. | Keep this responsibility explicit: Enterprise exit-code discipline for `pcloud-cli`. A dedicated exit code unit localizes dependencies, review, and regression testing inside `pcloud-cli`. | The focused `pcloud-cli` responsibility described as: Enterprise exit-code discipline for `pcloud-cli`. Typed IPC commands, stable exit codes, field selection, redacted prompts, and machine-readable output serve both operators and scripts. This unit contains 9 indexed Rust declarations. |
| [`crates/pcloud-cli/src/field_selector.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/field_selector.rs) | runtime/library unit | Built-in dotted-path field selector for `pcloudc`. | Keep this responsibility explicit: Built-in dotted-path field selector for `pcloudc`. A dedicated field selector unit localizes dependencies, review, and regression testing inside `pcloud-cli`. | The focused `pcloud-cli` responsibility described as: Built-in dotted-path field selector for `pcloudc`. Typed IPC commands, stable exit codes, field selection, redacted prompts, and machine-readable output serve both operators and scripts. This unit contains 32 indexed Rust declarations. |
| [`crates/pcloud-cli/src/globals.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/globals.rs) | runtime/library unit | Global CLI flags that apply to every subcommand. | Keep this responsibility explicit: Global CLI flags that apply to every subcommand. A dedicated globals unit localizes dependencies, review, and regression testing inside `pcloud-cli`. | The focused `pcloud-cli` responsibility described as: Global CLI flags that apply to every subcommand. Typed IPC commands, stable exit codes, field selection, redacted prompts, and machine-readable output serve both operators and scripts. This unit contains 45 indexed Rust declarations. |
| [`crates/pcloud-cli/src/i18n.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/i18n.rs) | runtime/library unit | T1.5 — minimal in-process i18n runtime for `pcloudc`. | Keep this responsibility explicit: T1.5 — minimal in-process i18n runtime for `pcloudc`. A dedicated i18n unit localizes dependencies, review, and regression testing inside `pcloud-cli`. | The focused `pcloud-cli` responsibility described as: T1.5 — minimal in-process i18n runtime for `pcloudc`. Typed IPC commands, stable exit codes, field selection, redacted prompts, and machine-readable output serve both operators and scripts. This unit contains 26 indexed Rust declarations. |
| [`crates/pcloud-cli/src/json_output.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/json_output.rs) | runtime/library unit | Machine-readable JSON output for `pcloud-cli`. | Keep this responsibility explicit: Machine-readable JSON output for `pcloud-cli`. A dedicated json output unit localizes dependencies, review, and regression testing inside `pcloud-cli`. | The focused `pcloud-cli` responsibility described as: Machine-readable JSON output for `pcloud-cli`. Typed IPC commands, stable exit codes, field selection, redacted prompts, and machine-readable output serve both operators and scripts. This unit contains 17 indexed Rust declarations. |
| [`crates/pcloud-cli/src/main.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/main.rs) | binary entrypoint/helper | `pcloudc` — the pcloud-rs command-line client. | Own the main executable lifecycle without mixing process concerns into reusable library code. | Process startup, argument/lifecycle ownership, and operator integration. Typed IPC commands, stable exit codes, field selection, redacted prompts, and machine-readable output serve both operators and scripts. This unit contains 84 indexed Rust declarations. |
| [`crates/pcloud-cli/src/migrate.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/migrate.rs) | runtime/library unit | `pcloudc migrate-from-c` — one-shot migration helper that imports the | Keep this responsibility explicit: `pcloudc migrate-from-c` — one-shot migration helper that imports the. A dedicated migrate unit localizes dependencies, review, and regression testing inside `pcloud-cli`. | The focused `pcloud-cli` responsibility described as: `pcloudc migrate-from-c` — one-shot migration helper that imports the. Typed IPC commands, stable exit codes, field selection, redacted prompts, and machine-readable output serve both operators and scripts. This unit contains 27 indexed Rust declarations. |
| [`crates/pcloud-cli/src/output.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/output.rs) | runtime/library unit | **PLATFORM:** all | Keep this responsibility explicit: **PLATFORM:** all. A dedicated output unit localizes dependencies, review, and regression testing inside `pcloud-cli`. | The focused `pcloud-cli` responsibility described as: **PLATFORM:** all. Typed IPC commands, stable exit codes, field selection, redacted prompts, and machine-readable output serve both operators and scripts. This unit contains 2 indexed Rust declarations. |
| [`crates/pcloud-cli/src/progress.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/progress.rs) | runtime/library unit | Progress indicator wrapper for long-running CLI subcommands. | Keep this responsibility explicit: Progress indicator wrapper for long-running CLI subcommands. A dedicated progress unit localizes dependencies, review, and regression testing inside `pcloud-cli`. | The focused `pcloud-cli` responsibility described as: Progress indicator wrapper for long-running CLI subcommands. Typed IPC commands, stable exit codes, field selection, redacted prompts, and machine-readable output serve both operators and scripts. This unit contains 39 indexed Rust declarations. |
| [`crates/pcloud-cli/src/prompt.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/prompt.rs) | runtime/library unit | Interactive terminal prompts for `pcloudc` login / unlock flows. | Keep this responsibility explicit: Interactive terminal prompts for `pcloudc` login / unlock flows. A dedicated prompt unit localizes dependencies, review, and regression testing inside `pcloud-cli`. | The focused `pcloud-cli` responsibility described as: Interactive terminal prompts for `pcloudc` login / unlock flows. Typed IPC commands, stable exit codes, field selection, redacted prompts, and machine-readable output serve both operators and scripts. This unit contains 13 indexed Rust declarations. |
| [`crates/pcloud-cli/src/verify.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/verify.rs) | runtime/library unit | `pcloudc verify` — walk a synced tree and cross-check local SHA256 | Keep this responsibility explicit: `pcloudc verify` — walk a synced tree and cross-check local SHA256. A dedicated verify unit localizes dependencies, review, and regression testing inside `pcloud-cli`. | The focused `pcloud-cli` responsibility described as: `pcloudc verify` — walk a synced tree and cross-check local SHA256. Typed IPC commands, stable exit codes, field selection, redacted prompts, and machine-readable output serve both operators and scripts. This unit contains 33 indexed Rust declarations. |
| [`crates/pcloud-cli/tests/coverage_binary.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/tests/coverage_binary.rs) | integration test | Executable verification for the behavior named by this file. | Prove the coverage binary behavior at a crate or process boundary instead of relying only on unit tests. | Protecting coverage binary boundary behavior from regressions. Typed IPC commands, stable exit codes, field selection, redacted prompts, and machine-readable output serve both operators and scripts. This unit contains 26 indexed Rust declarations. |
| [`crates/pcloud-cli/tests/crypto_setup_cli.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/tests/crypto_setup_cli.rs) | integration test | Stage 4b.4 — CLI UX for the dual crypto backend. | Prove the crypto setup cli behavior at a crate or process boundary instead of relying only on unit tests. | Protecting crypto setup cli boundary behavior from regressions. Typed IPC commands, stable exit codes, field selection, redacted prompts, and machine-readable output serve both operators and scripts. This unit contains 9 indexed Rust declarations. |
| [`crates/pcloud-cli/tests/field_selector_cli.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/tests/field_selector_cli.rs) | integration test | Integration coverage for the built-in dotted-path field selector. | Prove the field selector cli behavior at a crate or process boundary instead of relying only on unit tests. | Protecting field selector cli boundary behavior from regressions. Typed IPC commands, stable exit codes, field selection, redacted prompts, and machine-readable output serve both operators and scripts. This unit contains 9 indexed Rust declarations. |
| [`crates/pcloud-cli/tests/migrate_fixture.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/tests/migrate_fixture.rs) | integration test | Integration coverage for the `pcloudc migrate-from-c` helper. | Prove the migrate fixture behavior at a crate or process boundary instead of relying only on unit tests. | Protecting migrate fixture boundary behavior from regressions. Typed IPC commands, stable exit codes, field selection, redacted prompts, and machine-readable output serve both operators and scripts. This unit contains 5 indexed Rust declarations. |

All declarations: [`pcloud-cli` generated crate page](../crates/pcloud-cli.md).

## `pcloud-compat` (6 units)

Package context: Decoding legacy rpc_message_t frames and, when explicitly enabled, producing the old SysV shared-memory folder-list layout.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-compat/src/bin/shm_peek.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-compat/src/bin/shm_peek.rs) | binary entrypoint/helper | Tiny helper used by the cross-process integration test. | Own the shm peek executable lifecycle without mixing process concerns into reusable library code. | Process startup, argument/lifecycle ownership, and operator integration. Byte-exact codecs live outside the canonical daemon so compatibility cannot silently constrain modern internal design. This unit contains 2 indexed Rust declarations. |
| [`crates/pcloud-compat/src/folder_list.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-compat/src/folder_list.rs) | runtime/library unit | ABI-exact mirror of the legacy `psync_folder_list_t` shared-memory payload. | Keep this responsibility explicit: ABI-exact mirror of the legacy `psync_folder_list_t` shared-memory payload. A dedicated folder list unit localizes dependencies, review, and regression testing inside `pcloud-compat`. | The focused `pcloud-compat` responsibility described as: ABI-exact mirror of the legacy `psync_folder_list_t` shared-memory payload. Byte-exact codecs live outside the canonical daemon so compatibility cannot silently constrain modern internal design. This unit contains 27 indexed Rust declarations. |
| [`crates/pcloud-compat/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-compat/src/lib.rs) | runtime/library unit | `pcloud-compat` — C-to-Rust IPC compatibility primitives. | Declare and compose the `pcloud-compat` crate boundary. Isolate the small legacy C-client ABI surfaces that are still useful during migration. | The focused `pcloud-compat` responsibility described as: `pcloud-compat` — C-to-Rust IPC compatibility primitives. Byte-exact codecs live outside the canonical daemon so compatibility cannot silently constrain modern internal design. This unit contains 3 indexed Rust declarations. |
| [`crates/pcloud-compat/src/rpc_codec.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-compat/src/rpc_codec.rs) | runtime/library unit | Binary codec for the C `rpc_message_t` frame. | Keep this responsibility explicit: Binary codec for the C `rpc_message_t` frame. A dedicated rpc codec unit localizes dependencies, review, and regression testing inside `pcloud-compat`. | The focused `pcloud-compat` responsibility described as: Binary codec for the C `rpc_message_t` frame. Byte-exact codecs live outside the canonical daemon so compatibility cannot silently constrain modern internal design. This unit contains 27 indexed Rust declarations. |
| [`crates/pcloud-compat/src/shm_producer.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-compat/src/shm_producer.rs) | runtime/library unit | SysV shared-memory producer matching the legacy `pclsync/pshm.c` layout. | Keep this responsibility explicit: SysV shared-memory producer matching the legacy `pclsync/pshm.c` layout. A dedicated shm producer unit localizes dependencies, review, and regression testing inside `pcloud-compat`. | The focused `pcloud-compat` responsibility described as: SysV shared-memory producer matching the legacy `pclsync/pshm.c` layout. Byte-exact codecs live outside the canonical daemon so compatibility cannot silently constrain modern internal design. This unit contains 22 indexed Rust declarations. |
| [`crates/pcloud-compat/tests/cross_process_shm.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-compat/tests/cross_process_shm.rs) | integration test | Cross-process shm integration test. | Prove the cross process shm behavior at a crate or process boundary instead of relying only on unit tests. | Protecting cross process shm boundary behavior from regressions. Byte-exact codecs live outside the canonical daemon so compatibility cannot silently constrain modern internal design. This unit contains 1 indexed Rust declaration. |

All declarations: [`pcloud-compat` generated crate page](../crates/pcloud-compat.md).

## `pcloud-config` (28 units)

Package context: Profiles, paths, API/TLS settings, vault selection, limits, sync/mount policy, observability, HA, residency, KMS, bandwidth, and upgrades.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-config/examples/parse_config.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/examples/parse_config.rs) | example | Parses an inline JSON config envelope (the real on-disk format) and prints | Show a runnable parse config workflow and keep the intended call sequence discoverable. | Teaching and smoke-testing the parse config call path. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-config/src/api.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/api.rs) | runtime/library unit | API endpoint binding (transport mode, host/port, TLS SNI, timeouts). | Keep this responsibility explicit: API endpoint binding (transport mode, host/port, TLS SNI, timeouts). A dedicated api unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: API endpoint binding (transport mode, host/port, TLS SNI, timeouts). Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 30 indexed Rust declarations. |
| [`crates/pcloud-config/src/audit_verifier.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/audit_verifier.rs) | runtime/library unit | Scheduled audit-chain verifier configuration. | Keep this responsibility explicit: Scheduled audit-chain verifier configuration. A dedicated audit verifier unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: Scheduled audit-chain verifier configuration. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 8 indexed Rust declarations. |
| [`crates/pcloud-config/src/auth.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/auth.rs) | runtime/library unit | Authentication-subsystem configuration attached to a | Keep this responsibility explicit: Authentication-subsystem configuration attached to a. A dedicated auth unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: Authentication-subsystem configuration attached to a. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 15 indexed Rust declarations. |
| [`crates/pcloud-config/src/bandwidth_schedule.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/bandwidth_schedule.rs) | runtime/library unit | T1.4 — Time-of-day + metered-network bandwidth scheduling. | Keep this responsibility explicit: T1.4 — Time-of-day + metered-network bandwidth scheduling. A dedicated bandwidth schedule unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: T1.4 — Time-of-day + metered-network bandwidth scheduling. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 25 indexed Rust declarations. |
| [`crates/pcloud-config/src/crypto_kms.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/crypto_kms.rs) | runtime/library unit | Enterprise KMS configuration attached to a \[`crate::ConfigProfile`\]. | Keep this responsibility explicit: Enterprise KMS configuration attached to a \[`crate::ConfigProfile`\]. A dedicated crypto kms unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: Enterprise KMS configuration attached to a \[`crate::ConfigProfile`\]. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 19 indexed Rust declarations. |
| [`crates/pcloud-config/src/data_residency.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/data_residency.rs) | runtime/library unit | Data-residency policy attached to a \[`crate::ConfigProfile`\]. | Keep this responsibility explicit: Data-residency policy attached to a \[`crate::ConfigProfile`\]. A dedicated data residency unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: Data-residency policy attached to a \[`crate::ConfigProfile`\]. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 9 indexed Rust declarations. |
| [`crates/pcloud-config/src/env.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/env.rs) | runtime/library unit | `PCLOUD_*` environment-variable overrides for a \[`ConfigProfile`\]. | Keep this responsibility explicit: `PCLOUD_*` environment-variable overrides for a \[`ConfigProfile`\]. A dedicated env unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: `PCLOUD_*` environment-variable overrides for a \[`ConfigProfile`\]. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 8 indexed Rust declarations. |
| [`crates/pcloud-config/src/extensions.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/extensions.rs) | runtime/library unit | Plugin / extension loader policy attached to a | Keep this responsibility explicit: Plugin / extension loader policy attached to a. A dedicated extensions unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: Plugin / extension loader policy attached to a. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 5 indexed Rust declarations. |
| [`crates/pcloud-config/src/features.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/features.rs) | runtime/library unit | Product feature flags attached to a \[`crate::ConfigProfile`\]. | Keep this responsibility explicit: Product feature flags attached to a \[`crate::ConfigProfile`\]. A dedicated features unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: Product feature flags attached to a \[`crate::ConfigProfile`\]. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-config/src/ha.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/ha.rs) | runtime/library unit | Tier-2 HA configuration block. | Keep this responsibility explicit: Tier-2 HA configuration block. A dedicated ha unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: Tier-2 HA configuration block. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 10 indexed Rust declarations. |
| [`crates/pcloud-config/src/integrity_sweeper.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/integrity_sweeper.rs) | runtime/library unit | Background-integrity-sweeper configuration scaffolding (H14a). | Keep this responsibility explicit: Background-integrity-sweeper configuration scaffolding (H14a). A dedicated integrity sweeper unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: Background-integrity-sweeper configuration scaffolding (H14a). Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 28 indexed Rust declarations. |
| [`crates/pcloud-config/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/lib.rs) | runtime/library unit | pcloud-config | Declare and compose the `pcloud-config` crate boundary. Give every process one validated, versioned, secure configuration model across platforms and deployment sizes. | The focused `pcloud-config` responsibility described as: pcloud-config. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 32 indexed Rust declarations. |
| [`crates/pcloud-config/src/limits.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/limits.rs) | runtime/library unit | Resource-consumption bounds attached to a \[`crate::ConfigProfile`\]. | Keep this responsibility explicit: Resource-consumption bounds attached to a \[`crate::ConfigProfile`\]. A dedicated limits unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: Resource-consumption bounds attached to a \[`crate::ConfigProfile`\]. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 5 indexed Rust declarations. |
| [`crates/pcloud-config/src/loader.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/loader.rs) | runtime/library unit | Config-file loader with permission check, schema validation, and | Keep this responsibility explicit: Config-file loader with permission check, schema validation, and. A dedicated loader unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: Config-file loader with permission check, schema validation, and. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 22 indexed Rust declarations. |
| [`crates/pcloud-config/src/migrate.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/migrate.rs) | runtime/library unit | Versioned migrations for the on-disk config envelope. | Keep this responsibility explicit: Versioned migrations for the on-disk config envelope. A dedicated migrate unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: Versioned migrations for the on-disk config envelope. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 15 indexed Rust declarations. |
| [`crates/pcloud-config/src/mount.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/mount.rs) | runtime/library unit | FUSE mount policy attached to a \[`crate::ConfigProfile`\]. | Keep this responsibility explicit: FUSE mount policy attached to a \[`crate::ConfigProfile`\]. A dedicated mount unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: FUSE mount policy attached to a \[`crate::ConfigProfile`\]. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 7 indexed Rust declarations. |
| [`crates/pcloud-config/src/observability.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/observability.rs) | runtime/library unit | Observability feature flags attached to a \[`crate::ConfigProfile`\]. | Keep this responsibility explicit: Observability feature flags attached to a \[`crate::ConfigProfile`\]. A dedicated observability unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: Observability feature flags attached to a \[`crate::ConfigProfile`\]. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 3 indexed Rust declarations. |
| [`crates/pcloud-config/src/paths.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/paths.rs) | runtime/library unit | Canonical on-disk path layout for the pcloud-rs Rust workspace. | Keep this responsibility explicit: Canonical on-disk path layout for the pcloud-rs Rust workspace. A dedicated paths unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: Canonical on-disk path layout for the pcloud-rs Rust workspace. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 17 indexed Rust declarations. |
| [`crates/pcloud-config/src/rate_limit.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/rate_limit.rs) | runtime/library unit | Per-category rate-limit policy attached to a \[`crate::ConfigProfile`\]. | Keep this responsibility explicit: Per-category rate-limit policy attached to a \[`crate::ConfigProfile`\]. A dedicated rate limit unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: Per-category rate-limit policy attached to a \[`crate::ConfigProfile`\]. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 22 indexed Rust declarations. |
| [`crates/pcloud-config/src/resilience.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/resilience.rs) | runtime/library unit | Resilience policy attached to a \[`crate::ConfigProfile`\]. | Keep this responsibility explicit: Resilience policy attached to a \[`crate::ConfigProfile`\]. A dedicated resilience unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: Resilience policy attached to a \[`crate::ConfigProfile`\]. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 11 indexed Rust declarations. |
| [`crates/pcloud-config/src/runtime.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/runtime.rs) | runtime/library unit | Runtime directory permission policy attached to a | Keep this responsibility explicit: Runtime directory permission policy attached to a. A dedicated runtime unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: Runtime directory permission policy attached to a. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 2 indexed Rust declarations. |
| [`crates/pcloud-config/src/schema.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/schema.rs) | runtime/library unit | JSON schema definition + a minimal, focused validator for the on-disk | Keep this responsibility explicit: JSON schema definition + a minimal, focused validator for the on-disk. A dedicated schema unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: JSON schema definition + a minimal, focused validator for the on-disk. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 47 indexed Rust declarations. |
| [`crates/pcloud-config/src/sync_loop.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/sync_loop.rs) | runtime/library unit | Background sync-loop configuration. | Keep this responsibility explicit: Background sync-loop configuration. A dedicated sync loop unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: Background sync-loop configuration. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 35 indexed Rust declarations. |
| [`crates/pcloud-config/src/traceparent.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/traceparent.rs) | runtime/library unit | T2.7 — W3C `traceparent` parser + child-span generator. | Keep this responsibility explicit: T2.7 — W3C `traceparent` parser + child-span generator. A dedicated traceparent unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: T2.7 — W3C `traceparent` parser + child-span generator. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 22 indexed Rust declarations. |
| [`crates/pcloud-config/src/transport_protocol.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/transport_protocol.rs) | runtime/library unit | T2.6 — transport-protocol selector + fallback decision matrix. | Keep this responsibility explicit: T2.6 — transport-protocol selector + fallback decision matrix. A dedicated transport protocol unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: T2.6 — transport-protocol selector + fallback decision matrix. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 13 indexed Rust declarations. |
| [`crates/pcloud-config/src/upgrade.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/src/upgrade.rs) | runtime/library unit | Upgrade / daemon-handoff configuration attached to a | Keep this responsibility explicit: Upgrade / daemon-handoff configuration attached to a. A dedicated upgrade unit localizes dependencies, review, and regression testing inside `pcloud-config`. | The focused `pcloud-config` responsibility described as: Upgrade / daemon-handoff configuration attached to a. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 14 indexed Rust declarations. |
| [`crates/pcloud-config/tests/config_validation.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-config/tests/config_validation.rs) | integration test | Integration tests for `pcloud-config` profile construction, env-var | Prove the config validation behavior at a crate or process boundary instead of relying only on unit tests. | Protecting config validation boundary behavior from regressions. Schema validation, migrations, permission checks, typed environment overrides, and cross-field invariants reject unsafe combinations early. This unit contains 45 indexed Rust declarations. |

All declarations: [`pcloud-config` generated crate page](../crates/pcloud-config.md).

## `pcloud-crypto` (32 units)

Package context: Content and filename encryption, key derivation/wrapping, unlock lifecycle, password rotation, crypto sharing, policy, and compatibility KATs.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-crypto/benches/aead_sector.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/benches/aead_sector.rs) | benchmark | AES-256-GCM sector seal/open throughput. | Measure the aead sector path so performance and capacity decisions have executable evidence. | Measuring aead sector changes under a stable harness. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 3 indexed Rust declarations. |
| [`crates/pcloud-crypto/build.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/build.rs) | build helper | Build script: emits the `\[\[u8; 8\]; N\]` Rust const `PASSWORD_DICT` used by | Perform the build package's deterministic build-time preparation outside runtime code. | Reproducible preparation for the package build. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 2 indexed Rust declarations. |
| [`crates/pcloud-crypto/examples/seal_unseal.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/examples/seal_unseal.rs) | example | Derives a per-file key from a master key, seals a sector with AES-256-GCM, | Show a runnable seal unseal workflow and keep the intended call sequence discoverable. | Teaching and smoke-testing the seal unseal call path. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-crypto/fuzz/fuzz_targets/fuzz_open_sector.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/fuzz/fuzz_targets/fuzz_open_sector.rs) | fuzz target/helper | Split data into a fake 32-byte key and the remainder as ciphertext frame. | Drive malformed or adversarial inputs through the fuzz open sector boundary to find parser and invariant failures. | Finding malformed-input and invariant failures around fuzz open sector. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 0 indexed Rust declarations. |
| [`crates/pcloud-crypto/fuzz/fuzz_targets/fuzz_pclsync_filename_decode.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/fuzz/fuzz_targets/fuzz_pclsync_filename_decode.rs) | fuzz target/helper | Audit-06 wave-2 (bd-pcloud-rs-ncx.70) — fuzz the pclsync reversible | Drive malformed or adversarial inputs through the fuzz pclsync filename decode boundary to find parser and invariant failures. | Finding malformed-input and invariant failures around fuzz pclsync filename decode. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 0 indexed Rust declarations. |
| [`crates/pcloud-crypto/src/content.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/src/content.rs) | runtime/library unit | Content encryption (sector-oriented AES-256-GCM). | Keep this responsibility explicit: Content encryption (sector-oriented AES-256-GCM). A dedicated content unit localizes dependencies, review, and regression testing inside `pcloud-crypto`. | The focused `pcloud-crypto` responsibility described as: Content encryption (sector-oriented AES-256-GCM). Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 19 indexed Rust declarations. |
| [`crates/pcloud-crypto/src/crypto_util.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/src/crypto_util.rs) | runtime/library unit | Shared base64 encode/decode helpers for `pcloud-crypto`. | Keep this responsibility explicit: Shared base64 encode/decode helpers for `pcloud-crypto`. A dedicated crypto util unit localizes dependencies, review, and regression testing inside `pcloud-crypto`. | The focused `pcloud-crypto` responsibility described as: Shared base64 encode/decode helpers for `pcloud-crypto`. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 2 indexed Rust declarations. |
| [`crates/pcloud-crypto/src/folder_policy.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/src/folder_policy.rs) | runtime/library unit | T2.4 — per-folder crypto policy + unlock state machine. | Keep this responsibility explicit: T2.4 — per-folder crypto policy + unlock state machine. A dedicated folder policy unit localizes dependencies, review, and regression testing inside `pcloud-crypto`. | The focused `pcloud-crypto` responsibility described as: T2.4 — per-folder crypto policy + unlock state machine. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 36 indexed Rust declarations. |
| [`crates/pcloud-crypto/src/keys.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/src/keys.rs) | runtime/library unit | Key derivation and wrapping for the Rust crypto path. | Keep this responsibility explicit: Key derivation and wrapping for the Rust crypto path. A dedicated keys unit localizes dependencies, review, and regression testing inside `pcloud-crypto`. | The focused `pcloud-crypto` responsibility described as: Key derivation and wrapping for the Rust crypto path. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 25 indexed Rust declarations. |
| [`crates/pcloud-crypto/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/src/lib.rs) | runtime/library unit | pcloud-crypto | Declare and compose the `pcloud-crypto` crate boundary. Provide client-side Crypto-folder confidentiality while supporting both official-client interoperability and a Rust-native hardened format. | The focused `pcloud-crypto` responsibility described as: pcloud-crypto. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 173 indexed Rust declarations. |
| [`crates/pcloud-crypto/src/metadata.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/src/metadata.rs) | runtime/library unit | Metadata (filename) encryption. | Keep this responsibility explicit: Metadata (filename) encryption. A dedicated metadata unit localizes dependencies, review, and regression testing inside `pcloud-crypto`. | The focused `pcloud-crypto` responsibility described as: Metadata (filename) encryption. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 12 indexed Rust declarations. |
| [`crates/pcloud-crypto/src/password_scorer.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/src/password_scorer.rs) | runtime/library unit | Faithful Rust port of the legacy C password-quality scorer and | Keep this responsibility explicit: Faithful Rust port of the legacy C password-quality scorer and. A dedicated password scorer unit localizes dependencies, review, and regression testing inside `pcloud-crypto`. | The focused `pcloud-crypto` responsibility described as: Faithful Rust port of the legacy C password-quality scorer and. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 44 indexed Rust declarations. |
| [`crates/pcloud-crypto/src/pclsync_auth_tree.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/src/pclsync_auth_tree.rs) | runtime/library unit | Branching factor of the auth tree: `PSYNC_CRYPTO_SECTOR_SIZE / PSYNC_CRYPTO_AUTH_SIZE` | Keep this responsibility explicit: Branching factor of the auth tree: `PSYNC_CRYPTO_SECTOR_SIZE / PSYNC_CRYPTO_AUTH_SIZE`. A dedicated pclsync auth tree unit localizes dependencies, review, and regression testing inside `pcloud-crypto`. | The focused `pcloud-crypto` responsibility described as: Branching factor of the auth tree: `PSYNC_CRYPTO_SECTOR_SIZE / PSYNC_CRYPTO_AUTH_SIZE`. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 36 indexed Rust declarations. |
| [`crates/pcloud-crypto/src/pclsync_compat_profile.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/src/pclsync_compat_profile.rs) | runtime/library unit | PclsyncCompat profile persistence and runtime state (Wave 2 Stage 2+3). | Keep this responsibility explicit: PclsyncCompat profile persistence and runtime state (Wave 2 Stage 2+3). A dedicated pclsync compat profile unit localizes dependencies, review, and regression testing inside `pcloud-crypto`. | The focused `pcloud-crypto` responsibility described as: PclsyncCompat profile persistence and runtime state (Wave 2 Stage 2+3). Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 33 indexed Rust declarations. |
| [`crates/pcloud-crypto/src/pclsync_filename.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/src/pclsync_filename.rs) | runtime/library unit | Wave 1 / Primitive F — pclsync-compatible reversible filename encoding. | Keep this responsibility explicit: Wave 1 / Primitive F — pclsync-compatible reversible filename encoding. A dedicated pclsync filename unit localizes dependencies, review, and regression testing inside `pcloud-crypto`. | The focused `pcloud-crypto` responsibility described as: Wave 1 / Primitive F — pclsync-compatible reversible filename encoding. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 34 indexed Rust declarations. |
| [`crates/pcloud-crypto/src/pclsync_kdf.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/src/pclsync_kdf.rs) | runtime/library unit | pclsync-compatible password-to-KEK derivation (Wave 1, Primitive A) | Keep this responsibility explicit: pclsync-compatible password-to-KEK derivation (Wave 1, Primitive A). A dedicated pclsync kdf unit localizes dependencies, review, and regression testing inside `pcloud-crypto`. | The focused `pcloud-crypto` responsibility described as: pclsync-compatible password-to-KEK derivation (Wave 1, Primitive A). Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 16 indexed Rust declarations. |
| [`crates/pcloud-crypto/src/pclsync_modes.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/src/pclsync_modes.rs) | runtime/library unit | Wave 1 / Primitive C — AES-256-CTR (for wrapping the private key) and | Keep this responsibility explicit: Wave 1 / Primitive C — AES-256-CTR (for wrapping the private key) and. A dedicated pclsync modes unit localizes dependencies, review, and regression testing inside `pcloud-crypto`. | The focused `pcloud-crypto` responsibility described as: Wave 1 / Primitive C — AES-256-CTR (for wrapping the private key) and. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 23 indexed Rust declarations. |
| [`crates/pcloud-crypto/src/pclsync_rsa.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/src/pclsync_rsa.rs) | runtime/library unit | pclsync-compatible RSA-4096 keypair generation and RSAES-OAEP wrap/unwrap | Keep this responsibility explicit: pclsync-compatible RSA-4096 keypair generation and RSAES-OAEP wrap/unwrap. A dedicated pclsync rsa unit localizes dependencies, review, and regression testing inside `pcloud-crypto`. | The focused `pcloud-crypto` responsibility described as: pclsync-compatible RSA-4096 keypair generation and RSAES-OAEP wrap/unwrap. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 40 indexed Rust declarations. |
| [`crates/pcloud-crypto/src/pclsync_sector.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/src/pclsync_sector.rs) | runtime/library unit | Wave 1 / Primitive D — pclsync-compatible per-sector AEAD. | Keep this responsibility explicit: Wave 1 / Primitive D — pclsync-compatible per-sector AEAD. A dedicated pclsync sector unit localizes dependencies, review, and regression testing inside `pcloud-crypto`. | The focused `pcloud-crypto` responsibility described as: Wave 1 / Primitive D — pclsync-compatible per-sector AEAD. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 33 indexed Rust declarations. |
| [`crates/pcloud-crypto/src/policy.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/src/policy.rs) | runtime/library unit | Runtime policy for the crypto subsystem. | Keep this responsibility explicit: Runtime policy for the crypto subsystem. A dedicated policy unit localizes dependencies, review, and regression testing inside `pcloud-crypto`. | The focused `pcloud-crypto` responsibility described as: Runtime policy for the crypto subsystem. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 6 indexed Rust declarations. |
| [`crates/pcloud-crypto/src/share_rsa.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/src/share_rsa.rs) | runtime/library unit | RSA-4096-OAEP wrap of a folder/file `SymKeyVer1` for crypto share | Keep this responsibility explicit: RSA-4096-OAEP wrap of a folder/file `SymKeyVer1` for crypto share. A dedicated share rsa unit localizes dependencies, review, and regression testing inside `pcloud-crypto`. | The focused `pcloud-crypto` responsibility described as: RSA-4096-OAEP wrap of a folder/file `SymKeyVer1` for crypto share. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 20 indexed Rust declarations. |
| [`crates/pcloud-crypto/src/share_temppass.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/src/share_temppass.rs) | runtime/library unit | Temporary-password (“temppass”) key-rewrap for crypto-folder sharing. | Keep this responsibility explicit: Temporary-password (“temppass”) key-rewrap for crypto-folder sharing. A dedicated share temppass unit localizes dependencies, review, and regression testing inside `pcloud-crypto`. | The focused `pcloud-crypto` responsibility described as: Temporary-password (“temppass”) key-rewrap for crypto-folder sharing. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 32 indexed Rust declarations. |
| [`crates/pcloud-crypto/src/state.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/src/state.rs) | runtime/library unit | Lifecycle state machine for the crypto subsystem. | Keep this responsibility explicit: Lifecycle state machine for the crypto subsystem. A dedicated state unit localizes dependencies, review, and regression testing inside `pcloud-crypto`. | The focused `pcloud-crypto` responsibility described as: Lifecycle state machine for the crypto subsystem. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 3 indexed Rust declarations. |
| [`crates/pcloud-crypto/tests/integration.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/tests/integration.rs) | integration test | Integration tests for the active crypto path. | Prove the integration behavior at a crate or process boundary instead of relying only on unit tests. | Protecting integration boundary behavior from regressions. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 5 indexed Rust declarations. |
| [`crates/pcloud-crypto/tests/kms_routing.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/tests/kms_routing.rs) | integration test | Integration tests proving that `CryptoShell` DEK operations route | Prove the kms routing behavior at a crate or process boundary instead of relying only on unit tests. | Protecting kms routing boundary behavior from regressions. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 24 indexed Rust declarations. |
| [`crates/pcloud-crypto/tests/pclsync_compat_kat_live.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/tests/pclsync_compat_kat_live.rs) | integration test | Live known-answer test: prove the pclsync-v2 primitives in this crate | Prove the pclsync compat kat live behavior at a crate or process boundary instead of relying only on unit tests. | Protecting pclsync compat kat live boundary behavior from regressions. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 7 indexed Rust declarations. |
| [`crates/pcloud-crypto/tests/pclsync_compat_kat_offline.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/tests/pclsync_compat_kat_offline.rs) | integration test | Offline fixture-shape KAT: prove the pclsync-compat parsing surfaces | Prove the pclsync compat kat offline behavior at a crate or process boundary instead of relying only on unit tests. | Protecting pclsync compat kat offline boundary behavior from regressions. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 14 indexed Rust declarations. |
| [`crates/pcloud-crypto/tests/pclsync_compat_roundtrip.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/tests/pclsync_compat_roundtrip.rs) | integration test | Build a synthetic SymKeyVer1 filled with recognisable byte patterns. | Prove the pclsync compat roundtrip behavior at a crate or process boundary instead of relying only on unit tests. | Protecting pclsync compat roundtrip boundary behavior from regressions. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 14 indexed Rust declarations. |
| [`crates/pcloud-crypto/tests/pclsync_ctr_kat.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/tests/pclsync_ctr_kat.rs) | integration test | Regression KAT for `pclsync_modes::aes256_ctr_pclsync_xor_inplace`. | Prove the pclsync ctr kat behavior at a crate or process boundary instead of relying only on unit tests. | Protecting pclsync ctr kat boundary behavior from regressions. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 6 indexed Rust declarations. |
| [`crates/pcloud-crypto/tests/proptest_seal.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/tests/proptest_seal.rs) | integration test | Property tests for pcloud-crypto sector seal/unseal. | Prove the proptest seal behavior at a crate or process boundary instead of relying only on unit tests. | Protecting proptest seal boundary behavior from regressions. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 3 indexed Rust declarations. |
| [`crates/pcloud-crypto/tests/round_trip.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/tests/round_trip.rs) | integration test | Self round-trip tests for pcloud-crypto. These are NOT known-answer tests | Prove the round trip behavior at a crate or process boundary instead of relying only on unit tests. | Protecting round trip boundary behavior from regressions. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 10 indexed Rust declarations. |
| [`crates/pcloud-crypto/vendored/password_dict.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-crypto/vendored/password_dict.rs) | runtime/library unit | AUTO-GENERATED from pclsync/ppassworddict.h. Do not edit. | Keep this responsibility explicit: AUTO-GENERATED from pclsync/ppassworddict.h. Do not edit. A dedicated password dict unit localizes dependencies, review, and regression testing inside `pcloud-crypto`. | The focused `pcloud-crypto` responsibility described as: AUTO-GENERATED from pclsync/ppassworddict.h. Do not edit. Domain-separated keys, zeroized secret material, authenticated sectors, nonce budgets, lockout, and explicit backend identity avoid silent format or key misuse. This unit contains 1 indexed Rust declaration. |

All declarations: [`pcloud-crypto` generated crate page](../crates/pcloud-crypto.md).

## `pcloud-daemon` (55 units)

Package context: Running pcloudd as the authoritative local service behind CLI, SDK, web, sync, mount, backup, health, and metrics.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-daemon/benches/cold_start.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/benches/cold_start.rs) | benchmark | Cold-start latency profiling (CLAUDEREV T3.7). | Measure the cold start path so performance and capacity decisions have executable evidence. | Measuring cold start changes under a stable harness. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 7 indexed Rust declarations. |
| [`crates/pcloud-daemon/benches/dispatch_end_to_end.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/benches/dispatch_end_to_end.rs) | benchmark | End-to-end dispatch benchmark. | Measure the dispatch end to end path so performance and capacity decisions have executable evidence. | Measuring dispatch end to end changes under a stable harness. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 4 indexed Rust declarations. |
| [`crates/pcloud-daemon/benches/sync_root_canonicalize.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/benches/sync_root_canonicalize.rs) | benchmark | Sync-root canonicalization throughput. | Measure the sync root canonicalize path so performance and capacity decisions have executable evidence. | Measuring sync root canonicalize changes under a stable harness. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 2 indexed Rust declarations. |
| [`crates/pcloud-daemon/benches/vault_open_close.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/benches/vault_open_close.rs) | benchmark | Auth-vault open/close latency benchmark. | Measure the vault open close path so performance and capacity decisions have executable evidence. | Measuring vault open close changes under a stable harness. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-daemon/fuzz/fuzz_targets/fuzz_auth_vault_decode.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/fuzz/fuzz_targets/fuzz_auth_vault_decode.rs) | fuzz target/helper | Audit-06 wave-2 (bd-pcloud-rs-ncx.70) — fuzz the auth-vault token | Drive malformed or adversarial inputs through the fuzz auth vault decode boundary to find parser and invariant failures. | Finding malformed-input and invariant failures around fuzz auth vault decode. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 0 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/account_scope.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/account_scope.rs) | runtime/library unit | T2.8.b — account-scoped bootstrap inputs. | Keep this responsibility explicit: T2.8.b — account-scoped bootstrap inputs. A dedicated account scope unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: T2.8.b — account-scoped bootstrap inputs. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 13 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/audit_verifier_service.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/audit_verifier_service.rs) | runtime/library unit | Scheduled audit-chain verifier (I04 follow-up). | Keep this responsibility explicit: Scheduled audit-chain verifier (I04 follow-up). A dedicated audit verifier service unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: Scheduled audit-chain verifier (I04 follow-up). A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 43 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/auth_vault.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/auth_vault.rs) | runtime/library unit | Backwards-compatibility shim for the legacy file-based auth vault API. | Keep this responsibility explicit: Backwards-compatibility shim for the legacy file-based auth vault API. A dedicated auth vault unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: Backwards-compatibility shim for the legacy file-based auth vault API. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-daemon/src/bandwidth_schedule_applier.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/bandwidth_schedule_applier.rs) | runtime/library unit | T1.4.b — Drive `BandwidthPacer` from a `BandwidthScheduleConfig`. | Keep this responsibility explicit: T1.4.b — Drive `BandwidthPacer` from a `BandwidthScheduleConfig`. A dedicated bandwidth schedule applier unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: T1.4.b — Drive `BandwidthPacer` from a `BandwidthScheduleConfig`. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 17 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/bootstrap.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/bootstrap.rs) | runtime/library unit | Daemon bootstrap: composes configuration, store, auth vault, protocol | Keep this responsibility explicit: Daemon bootstrap: composes configuration, store, auth vault, protocol. A dedicated bootstrap unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: Daemon bootstrap: composes configuration, store, auth vault, protocol. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 35 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/config_reload.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/config_reload.rs) | runtime/library unit | SIGHUP-driven config hot-reload. | Keep this responsibility explicit: SIGHUP-driven config hot-reload. A dedicated config reload unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: SIGHUP-driven config hot-reload. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 20 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/dispatch.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/dispatch.rs) | runtime/library unit | IPC method dispatcher: decodes typed `Request` frames from | Keep this responsibility explicit: IPC method dispatcher: decodes typed `Request` frames from. A dedicated dispatch unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: IPC method dispatcher: decodes typed `Request` frames from. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 31 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/ha_lease.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/ha_lease.rs) | runtime/library unit | Tier-2 active-passive HA lease for `pcloudd`. | Keep this responsibility explicit: Tier-2 active-passive HA lease for `pcloudd`. A dedicated ha lease unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: Tier-2 active-passive HA lease for `pcloudd`. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 51 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/health_server.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/health_server.rs) | runtime/library unit | Minimal HTTP health-check surface for the daemon. | Keep this responsibility explicit: Minimal HTTP health-check surface for the daemon. A dedicated health server unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: Minimal HTTP health-check surface for the daemon. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 12 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/integrity_sweeper_service.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/integrity_sweeper_service.rs) | runtime/library unit | Background-integrity-sweeper daemon service (H14d). | Keep this responsibility explicit: Background-integrity-sweeper daemon service (H14d). A dedicated integrity sweeper service unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: Background-integrity-sweeper daemon service (H14d). A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 113 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/lib.rs) | runtime/library unit | pcloud-daemon | Declare and compose the `pcloud-daemon` crate boundary. Own long-lived credentials, validated request dispatch, state, background work, and native resources in one per-user composition root. | The focused `pcloud-daemon` responsibility described as: pcloud-daemon. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 87 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/main.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/main.rs) | binary entrypoint/helper | `pcloudd` — pcloud-rs daemon binary entry point. | Own the main executable lifecycle without mixing process concerns into reusable library code. | Process startup, argument/lifecycle ownership, and operator integration. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 13 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/metered_network.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/metered_network.rs) | runtime/library unit | T1.4.c — Metered-network detector. | Keep this responsibility explicit: T1.4.c — Metered-network detector. A dedicated metered network unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: T1.4.c — Metered-network detector. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 20 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/metrics_server.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/metrics_server.rs) | runtime/library unit | Feature-gated glue between the runtime shell and the | Keep this responsibility explicit: Feature-gated glue between the runtime shell and the. A dedicated metrics server unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: Feature-gated glue between the runtime shell and the. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 21 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/mount_runtime.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/mount_runtime.rs) | runtime/library unit | Daemon-side native mount orchestration. | Keep this responsibility explicit: Daemon-side native mount orchestration. A dedicated mount runtime unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: Daemon-side native mount orchestration. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 96 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/power.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/power.rs) | runtime/library unit | Daemon-grade \[`PowerSource`\] reader. | Keep this responsibility explicit: Daemon-grade \[`PowerSource`\] reader. A dedicated power unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: Daemon-grade \[`PowerSource`\] reader. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 11 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/rate_limit.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/rate_limit.rs) | runtime/library unit | Per-session, per-category IPC rate limiter. | Keep this responsibility explicit: Per-session, per-category IPC rate limiter. A dedicated rate limit unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: Per-session, per-category IPC rate limiter. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 34 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/runtime.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs) | runtime/library unit | `RuntimeShell`: the long-lived in-process state that backends mutate. | Keep this responsibility explicit: `RuntimeShell`: the long-lived in-process state that backends mutate. A dedicated runtime unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: `RuntimeShell`: the long-lived in-process state that backends mutate. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 238 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/serve.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/serve.rs) | runtime/library unit | IPC accept loop: binds the Unix domain socket via `pcloud-ipc`, | Keep this responsibility explicit: IPC accept loop: binds the Unix domain socket via `pcloud-ipc`,. A dedicated serve unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: IPC accept loop: binds the Unix domain socket via `pcloud-ipc`,. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 33 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/session_refresh.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/session_refresh.rs) | runtime/library unit | Session refresh integration for the daemon serve loop. | Keep this responsibility explicit: Session refresh integration for the daemon serve loop. A dedicated session refresh unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: Session refresh integration for the daemon serve loop. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 11 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/signals.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/signals.rs) | runtime/library unit | Process-level UNIX signal handling for the pcloud-rs Rust daemon. | Keep this responsibility explicit: Process-level UNIX signal handling for the pcloud-rs Rust daemon. A dedicated signals unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: Process-level UNIX signal handling for the pcloud-rs Rust daemon. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 36 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/sync_loop.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/sync_loop.rs) | runtime/library unit | Autonomous background sync loop. | Keep this responsibility explicit: Autonomous background sync loop. A dedicated sync loop unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: Autonomous background sync loop. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 74 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/sync_loop_runtime.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/sync_loop_runtime.rs) | runtime/library unit | Real \[`SyncLoopRuntime`\] implementation that bridges to the daemon's | Keep this responsibility explicit: Real \[`SyncLoopRuntime`\] implementation that bridges to the daemon's. A dedicated sync loop runtime unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: Real \[`SyncLoopRuntime`\] implementation that bridges to the daemon's. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 106 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/transport_factory.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/transport_factory.rs) | runtime/library unit | Transport factory policy used by the daemon bootstrap. | Keep this responsibility explicit: Transport factory policy used by the daemon bootstrap. A dedicated transport factory unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: Transport factory policy used by the daemon bootstrap. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 19 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/vault/dpapi.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/vault/dpapi.rs) | runtime/library unit | **PLATFORM: Windows 10/11.** DPAPI-backed auth token vault. | Keep this responsibility explicit: **PLATFORM: Windows 10/11.** DPAPI-backed auth token vault. A dedicated dpapi unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: **PLATFORM: Windows 10/11.** DPAPI-backed auth token vault. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 14 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/vault/file.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/vault/file.rs) | runtime/library unit | **PLATFORM: all** (Linux/BSD/macOS/Windows-file-fallback). | Keep this responsibility explicit: **PLATFORM: all** (Linux/BSD/macOS/Windows-file-fallback). A dedicated file unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: **PLATFORM: all** (Linux/BSD/macOS/Windows-file-fallback). A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 23 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/vault/keychain.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/vault/keychain.rs) | runtime/library unit | **PLATFORM: macOS.** Keychain-backed auth token vault. | Keep this responsibility explicit: **PLATFORM: macOS.** Keychain-backed auth token vault. A dedicated keychain unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: **PLATFORM: macOS.** Keychain-backed auth token vault. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 13 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/vault/mod.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/vault/mod.rs) | runtime/library unit | **PLATFORM: all.** Cross-platform vault abstraction for the auth token. | Keep this responsibility explicit: **PLATFORM: all.** Cross-platform vault abstraction for the auth token. A dedicated mod unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: **PLATFORM: all.** Cross-platform vault abstraction for the auth token. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 28 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/vault/secret_service.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/vault/secret_service.rs) | runtime/library unit | **PLATFORM: Linux (opt-in).** Freedesktop Secret Service–backed vault. | Keep this responsibility explicit: **PLATFORM: Linux (opt-in).** Freedesktop Secret Service–backed vault. A dedicated secret service unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: **PLATFORM: Linux (opt-in).** Freedesktop Secret Service–backed vault. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 18 indexed Rust declarations. |
| [`crates/pcloud-daemon/src/vault/windows_secure_file.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/vault/windows_secure_file.rs) | runtime/library unit | Durable Windows file replacement with a protected owner-scoped DACL. | Keep this responsibility explicit: Durable Windows file replacement with a protected owner-scoped DACL. A dedicated windows secure file unit localizes dependencies, review, and regression testing inside `pcloud-daemon`. | The focused `pcloud-daemon` responsibility described as: Durable Windows file replacement with a protected owner-scoped DACL. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 6 indexed Rust declarations. |
| [`crates/pcloud-daemon/tests/audit_verifier_tamper.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/tests/audit_verifier_tamper.rs) | integration test | Integration test for the scheduled audit-chain verifier. | Prove the audit verifier tamper behavior at a crate or process boundary instead of relying only on unit tests. | Protecting audit verifier tamper boundary behavior from regressions. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 13 indexed Rust declarations. |
| [`crates/pcloud-daemon/tests/binary_coverage.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/tests/binary_coverage.rs) | integration test | \[test\] | Prove the binary coverage behavior at a crate or process boundary instead of relying only on unit tests. | Protecting binary coverage boundary behavior from regressions. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 4 indexed Rust declarations. |
| [`crates/pcloud-daemon/tests/crypto_change_password.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/tests/crypto_change_password.rs) | integration test | Integration test for the crypto password-change family. | Prove the crypto change password behavior at a crate or process boundary instead of relying only on unit tests. | Protecting crypto change password boundary behavior from regressions. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 6 indexed Rust declarations. |
| [`crates/pcloud-daemon/tests/graceful_drain.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/tests/graceful_drain.rs) | integration test | Integration coverage for the graceful-drain protocol. | Prove the graceful drain behavior at a crate or process boundary instead of relying only on unit tests. | Protecting graceful drain boundary behavior from regressions. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 6 indexed Rust declarations. |
| [`crates/pcloud-daemon/tests/ha_two_daemon_contention.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/tests/ha_two_daemon_contention.rs) | integration test | Integration test: Tier-2 HA file-lock contention between two | Prove the ha two daemon contention behavior at a crate or process boundary instead of relying only on unit tests. | Protecting ha two daemon contention boundary behavior from regressions. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 8 indexed Rust declarations. |
| [`crates/pcloud-daemon/tests/health_server_coverage.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/tests/health_server_coverage.rs) | integration test | \[test\] | Prove the health server coverage behavior at a crate or process boundary instead of relying only on unit tests. | Protecting health server coverage boundary behavior from regressions. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 4 indexed Rust declarations. |
| [`crates/pcloud-daemon/tests/integrity_walker.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/tests/integrity_walker.rs) | integration test | End-to-end integration test for the `bd-1du.4.6.1` integrity-sweeper | Prove the integrity walker behavior at a crate or process boundary instead of relying only on unit tests. | Protecting integrity walker boundary behavior from regressions. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 9 indexed Rust declarations. |
| [`crates/pcloud-daemon/tests/live_auth.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/tests/live_auth.rs) | integration test | Audit finding L1: this test harness intentionally references live-auth env | Prove the live auth behavior at a crate or process boundary instead of relying only on unit tests. | Protecting live auth boundary behavior from regressions. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 10 indexed Rust declarations. |
| [`crates/pcloud-daemon/tests/macos_pcloud_live.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/tests/macos_pcloud_live.rs) | integration test | **PLATFORM: macOS only.** | Prove the macos pcloud live behavior at a crate or process boundary instead of relying only on unit tests. | Protecting macos pcloud live boundary behavior from regressions. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 18 indexed Rust declarations. |
| [`crates/pcloud-daemon/tests/observability_metrics.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/tests/observability_metrics.rs) | integration test | Feature-gated integration tests for the observability metric wiring | Prove the observability metrics behavior at a crate or process boundary instead of relying only on unit tests. | Protecting observability metrics boundary behavior from regressions. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 11 indexed Rust declarations. |
| [`crates/pcloud-daemon/tests/platform_vault_crossplat.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/tests/platform_vault_crossplat.rs) | integration test | **PLATFORM: all** (Linux \| FreeBSD \| OpenBSD \| NetBSD \| macOS \| Windows). | Prove the platform vault crossplat behavior at a crate or process boundary instead of relying only on unit tests. | Protecting platform vault crossplat boundary behavior from regressions. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 16 indexed Rust declarations. |
| [`crates/pcloud-daemon/tests/proptest_sync_and_resolver.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/tests/proptest_sync_and_resolver.rs) | integration test | Property tests for the sync-root canonicalization classifier and the | Prove the proptest sync and resolver behavior at a crate or process boundary instead of relying only on unit tests. | Protecting proptest sync and resolver boundary behavior from regressions. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 12 indexed Rust declarations. |
| [`crates/pcloud-daemon/tests/residency.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/tests/residency.rs) | integration test | Integration tests for data-residency enforcement at the three daemon | Prove the residency behavior at a crate or process boundary instead of relying only on unit tests. | Protecting residency boundary behavior from regressions. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 12 indexed Rust declarations. |
| [`crates/pcloud-daemon/tests/runtime_surface_coverage.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/tests/runtime_surface_coverage.rs) | integration test | Broad public IPC-contract coverage for the daemon composition root. | Prove the runtime surface coverage behavior at a crate or process boundary instead of relying only on unit tests. | Protecting runtime surface coverage boundary behavior from regressions. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 16 indexed Rust declarations. |
| [`crates/pcloud-daemon/tests/slo_dispatch.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/tests/slo_dispatch.rs) | integration test | Integration test for the canonical SLO IPC surface. | Prove the slo dispatch behavior at a crate or process boundary instead of relying only on unit tests. | Protecting slo dispatch boundary behavior from regressions. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 6 indexed Rust declarations. |
| [`crates/pcloud-daemon/tests/snapshot_pipeline.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/tests/snapshot_pipeline.rs) | integration test | Integration test: `Request::BackupSnapshot { action: Create }` through | Prove the snapshot pipeline behavior at a crate or process boundary instead of relying only on unit tests. | Protecting snapshot pipeline boundary behavior from regressions. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 4 indexed Rust declarations. |
| [`crates/pcloud-daemon/tests/sync_loop_e2e.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/tests/sync_loop_e2e.rs) | integration test | End-to-end integration test for the background sync loop. | Prove the sync loop e2e behavior at a crate or process boundary instead of relying only on unit tests. | Protecting sync loop e2e boundary behavior from regressions. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 6 indexed Rust declarations. |
| [`crates/pcloud-daemon/tests/transport_factory_bootstrap.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/tests/transport_factory_bootstrap.rs) | integration test | Integration test: bootstrap decides transport wrapping by environment. | Prove the transport factory bootstrap behavior at a crate or process boundary instead of relying only on unit tests. | Protecting transport factory bootstrap boundary behavior from regressions. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 6 indexed Rust declarations. |
| [`crates/pcloud-daemon/tests/upload_journal_crash_replay.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/tests/upload_journal_crash_replay.rs) | integration test | Crash-replay proof for the upload journal (PLAN_A_PLUS P1.2). | Prove the upload journal crash replay behavior at a crate or process boundary instead of relying only on unit tests. | Protecting upload journal crash replay boundary behavior from regressions. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 5 indexed Rust declarations. |
| [`crates/pcloud-daemon/tests/upload_sessions.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/tests/upload_sessions.rs) | integration test | Integration test: upload session lifecycle through the daemon IPC | Prove the upload sessions behavior at a crate or process boundary instead of relying only on unit tests. | Protecting upload sessions boundary behavior from regressions. A single RuntimeShell, peer-authenticated IPC, durable stores, controlled shutdown, and narrowly composed backends centralize authority and cleanup. This unit contains 17 indexed Rust declarations. |

All declarations: [`pcloud-daemon` generated crate page](../crates/pcloud-daemon.md).

## `pcloud-daemon-win` (1 units)

Package context: Experimental Windows service installation and SCM lifecycle integration.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-daemon-win/src/main.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon-win/src/main.rs) | binary entrypoint/helper | pcloud-daemon-win | Own the main executable lifecycle without mixing process concerns into reusable library code. | Process startup, argument/lifecycle ownership, and operator integration. The wrapper is isolated and explicitly unshipped, so Windows-specific service semantics cannot be mistaken for the supported daemon contract. This unit contains 12 indexed Rust declarations. |

All declarations: [`pcloud-daemon-win` generated crate page](../crates/pcloud-daemon-win.md).

## `pcloud-embedded-sdk` (11 units)

Package context: Embedding daemon-like behavior, broad pCloud API coverage, upload sessions, Crypto workflows, and compatibility examples.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-sdk/benches/upload_session.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-sdk/benches/upload_session.rs) | benchmark | Criterion micro-benchmark for the \[`UploadSession`\] state machine | Measure the upload session path so performance and capacity decisions have executable evidence. | Measuring upload session changes under a stable harness. It composes existing runtime/backends rather than reimplementing protocol semantics, but remains unpublished to avoid freezing internals. This unit contains 12 indexed Rust declarations. |
| [`crates/pcloud-sdk/examples/create_tree_public_link_from_paths.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-sdk/examples/create_tree_public_link_from_paths.rs) | example | Example: create a tree public link by resolving pCloud-drive paths | Show a runnable create tree public link from paths workflow and keep the intended call sequence discoverable. | Teaching and smoke-testing the create tree public link from paths call path. It composes existing runtime/backends rather than reimplementing protocol semantics, but remains unpublished to avoid freezing internals. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-sdk/examples/crypto_lifecycle.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-sdk/examples/crypto_lifecycle.rs) | example | Example: crypto folder lifecycle — setup, unlock, mkdir, lock. | Show a runnable crypto lifecycle workflow and keep the intended call sequence discoverable. | Teaching and smoke-testing the crypto lifecycle call path. It composes existing runtime/backends rather than reimplementing protocol semantics, but remains unpublished to avoid freezing internals. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-sdk/examples/login_and_list.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-sdk/examples/login_and_list.rs) | example | Boots an in-process `EmbeddedDaemon`, dispatches a small set of typed IPC | Show a runnable login and list workflow and keep the intended call sequence discoverable. | Teaching and smoke-testing the login and list call path. It composes existing runtime/backends rather than reimplementing protocol semantics, but remains unpublished to avoid freezing internals. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-sdk/examples/public_link.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-sdk/examples/public_link.rs) | example | Example: create a public link, list it, then delete it. | Show a runnable public link workflow and keep the intended call sequence discoverable. | Teaching and smoke-testing the public link call path. It composes existing runtime/backends rather than reimplementing protocol semantics, but remains unpublished to avoid freezing internals. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-sdk/examples/upload_and_download.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-sdk/examples/upload_and_download.rs) | example | Example: upload a file then download it back. | Show a runnable upload and download workflow and keep the intended call sequence discoverable. | Teaching and smoke-testing the upload and download call path. It composes existing runtime/backends rather than reimplementing protocol semantics, but remains unpublished to avoid freezing internals. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-sdk/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-sdk/src/lib.rs) | runtime/library unit | pcloud-embedded-sdk | Declare and compose the `pcloud-embedded-sdk` crate boundary. Retain a broad first-party in-process compatibility API for tests, migration, and tightly controlled embeddings. | The focused `pcloud-embedded-sdk` responsibility described as: pcloud-embedded-sdk. It composes existing runtime/backends rather than reimplementing protocol semantics, but remains unpublished to avoid freezing internals. This unit contains 205 indexed Rust declarations. |
| [`crates/pcloud-sdk/src/remote.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-sdk/src/remote.rs) | runtime/library unit | Stable, filesystem-focused SDK facade. | Keep this responsibility explicit: Stable, filesystem-focused SDK facade. A dedicated remote unit localizes dependencies, review, and regression testing inside `pcloud-embedded-sdk`. | The focused `pcloud-embedded-sdk` responsibility described as: Stable, filesystem-focused SDK facade. It composes existing runtime/backends rather than reimplementing protocol semantics, but remains unpublished to avoid freezing internals. This unit contains 31 indexed Rust declarations. |
| [`crates/pcloud-sdk/src/upload_session.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-sdk/src/upload_session.rs) | runtime/library unit | High-level `UploadSession` handle for the SDK. | Keep this responsibility explicit: High-level `UploadSession` handle for the SDK. A dedicated upload session unit localizes dependencies, review, and regression testing inside `pcloud-embedded-sdk`. | The focused `pcloud-embedded-sdk` responsibility described as: High-level `UploadSession` handle for the SDK. It composes existing runtime/backends rather than reimplementing protocol semantics, but remains unpublished to avoid freezing internals. This unit contains 63 indexed Rust declarations. |
| [`crates/pcloud-sdk/tests/coverage_surface.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-sdk/tests/coverage_surface.rs) | integration test | \[test\] | Prove the coverage surface behavior at a crate or process boundary instead of relying only on unit tests. | Protecting coverage surface boundary behavior from regressions. It composes existing runtime/backends rather than reimplementing protocol semantics, but remains unpublished to avoid freezing internals. This unit contains 20 indexed Rust declarations. |
| [`crates/pcloud-sdk/tests/upload_session_chunked.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-sdk/tests/upload_session_chunked.rs) | integration test | Integration tests for the chunked `UploadSession` state machine. | Prove the upload session chunked behavior at a crate or process boundary instead of relying only on unit tests. | Protecting upload session chunked boundary behavior from regressions. It composes existing runtime/backends rather than reimplementing protocol semantics, but remains unpublished to avoid freezing internals. This unit contains 20 indexed Rust declarations. |

All declarations: [`pcloud-embedded-sdk` generated crate page](../crates/pcloud-embedded-sdk.md).

## `pcloud-engine` (22 units)

Package context: Scanning, diff polling, selective sync, conflict policy, planning, scheduling, recovery, stall detection, and upload/download coordination.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-engine/benches/engine.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-engine/benches/engine.rs) | benchmark | Criterion micro-benchmarks for `pcloud-engine` hot paths. | Measure the engine path so performance and capacity decisions have executable evidence. | Measuring engine changes under a stable harness. Pure planners and typed events make reconciliation deterministic, testable, priority-aware, and recoverable. This unit contains 3 indexed Rust declarations. |
| [`crates/pcloud-engine/src/conflict_resolver.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-engine/src/conflict_resolver.rs) | runtime/library unit | Policy that determines how local/remote conflicts are resolved when | Keep this responsibility explicit: Policy that determines how local/remote conflicts are resolved when. A dedicated conflict resolver unit localizes dependencies, review, and regression testing inside `pcloud-engine`. | The focused `pcloud-engine` responsibility described as: Policy that determines how local/remote conflicts are resolved when. Pure planners and typed events make reconciliation deterministic, testable, priority-aware, and recoverable. This unit contains 23 indexed Rust declarations. |
| [`crates/pcloud-engine/src/diff_events.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-engine/src/diff_events.rs) | runtime/library unit | Typed dispatch for diff event subtypes (sync row 75 — diff polling). | Keep this responsibility explicit: Typed dispatch for diff event subtypes (sync row 75 — diff polling). A dedicated diff events unit localizes dependencies, review, and regression testing inside `pcloud-engine`. | The focused `pcloud-engine` responsibility described as: Typed dispatch for diff event subtypes (sync row 75 — diff polling). Pure planners and typed events make reconciliation deterministic, testable, priority-aware, and recoverable. This unit contains 24 indexed Rust declarations. |
| [`crates/pcloud-engine/src/diff_poller.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-engine/src/diff_poller.rs) | runtime/library unit | Configuration state for the remote diff poller. Holds the maximum | Keep this responsibility explicit: Configuration state for the remote diff poller. Holds the maximum. A dedicated diff poller unit localizes dependencies, review, and regression testing inside `pcloud-engine`. | The focused `pcloud-engine` responsibility described as: Configuration state for the remote diff poller. Holds the maximum. Pure planners and typed events make reconciliation deterministic, testable, priority-aware, and recoverable. This unit contains 12 indexed Rust declarations. |
| [`crates/pcloud-engine/src/divergence_sweeper.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-engine/src/divergence_sweeper.rs) | runtime/library unit | Sync-state divergence sweeper (audit-06 M-4.2). | Keep this responsibility explicit: Sync-state divergence sweeper (audit-06 M-4.2). A dedicated divergence sweeper unit localizes dependencies, review, and regression testing inside `pcloud-engine`. | The focused `pcloud-engine` responsibility described as: Sync-state divergence sweeper (audit-06 M-4.2). Pure planners and typed events make reconciliation deterministic, testable, priority-aware, and recoverable. This unit contains 35 indexed Rust declarations. |
| [`crates/pcloud-engine/src/fs_events.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-engine/src/fs_events.rs) | runtime/library unit | Ingests raw local filesystem events (from notify/inotify) and | Keep this responsibility explicit: Ingests raw local filesystem events (from notify/inotify) and. A dedicated fs events unit localizes dependencies, review, and regression testing inside `pcloud-engine`. | The focused `pcloud-engine` responsibility described as: Ingests raw local filesystem events (from notify/inotify) and. Pure planners and typed events make reconciliation deterministic, testable, priority-aware, and recoverable. This unit contains 11 indexed Rust declarations. |
| [`crates/pcloud-engine/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-engine/src/lib.rs) | runtime/library unit | pcloud-engine | Declare and compose the `pcloud-engine` crate boundary. Separate synchronization decisions from transport and process lifecycle. | The focused `pcloud-engine` responsibility described as: pcloud-engine. Pure planners and typed events make reconciliation deterministic, testable, priority-aware, and recoverable. This unit contains 86 indexed Rust declarations. |
| [`crates/pcloud-engine/src/local_scan.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-engine/src/local_scan.rs) | runtime/library unit | Local filesystem scanner state. Tracks how frequently a full | Keep this responsibility explicit: Local filesystem scanner state. Tracks how frequently a full. A dedicated local scan unit localizes dependencies, review, and regression testing inside `pcloud-engine`. | The focused `pcloud-engine` responsibility described as: Local filesystem scanner state. Tracks how frequently a full. Pure planners and typed events make reconciliation deterministic, testable, priority-aware, and recoverable. This unit contains 35 indexed Rust declarations. |
| [`crates/pcloud-engine/src/planner.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-engine/src/planner.rs) | runtime/library unit | Candidate-ingestion planner. | Keep this responsibility explicit: Candidate-ingestion planner. A dedicated planner unit localizes dependencies, review, and regression testing inside `pcloud-engine`. | The focused `pcloud-engine` responsibility described as: Candidate-ingestion planner. Pure planners and typed events make reconciliation deterministic, testable, priority-aware, and recoverable. This unit contains 26 indexed Rust declarations. |
| [`crates/pcloud-engine/src/power.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-engine/src/power.rs) | runtime/library unit | Power-source awareness for the sync engine (audit-06 M-4.1). | Keep this responsibility explicit: Power-source awareness for the sync engine (audit-06 M-4.1). A dedicated power unit localizes dependencies, review, and regression testing inside `pcloud-engine`. | The focused `pcloud-engine` responsibility described as: Power-source awareness for the sync engine (audit-06 M-4.1). Pure planners and typed events make reconciliation deterministic, testable, priority-aware, and recoverable. This unit contains 18 indexed Rust declarations. |
| [`crates/pcloud-engine/src/reconcile_worker.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-engine/src/reconcile_worker.rs) | runtime/library unit | Continuous reconcile worker (sync row 65 — `psync_start_sync`). | Keep this responsibility explicit: Continuous reconcile worker (sync row 65 — `psync_start_sync`). A dedicated reconcile worker unit localizes dependencies, review, and regression testing inside `pcloud-engine`. | The focused `pcloud-engine` responsibility described as: Continuous reconcile worker (sync row 65 — `psync_start_sync`). Pure planners and typed events make reconciliation deterministic, testable, priority-aware, and recoverable. This unit contains 19 indexed Rust declarations. |
| [`crates/pcloud-engine/src/recovery.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-engine/src/recovery.rs) | runtime/library unit | Recovery classifier that maps observed transfer failures to a | Keep this responsibility explicit: Recovery classifier that maps observed transfer failures to a. A dedicated recovery unit localizes dependencies, review, and regression testing inside `pcloud-engine`. | The focused `pcloud-engine` responsibility described as: Recovery classifier that maps observed transfer failures to a. Pure planners and typed events make reconciliation deterministic, testable, priority-aware, and recoverable. This unit contains 8 indexed Rust declarations. |
| [`crates/pcloud-engine/src/scheduler.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-engine/src/scheduler.rs) | runtime/library unit | Priority-aware scheduler queue for planned operations. | Keep this responsibility explicit: Priority-aware scheduler queue for planned operations. A dedicated scheduler unit localizes dependencies, review, and regression testing inside `pcloud-engine`. | The focused `pcloud-engine` responsibility described as: Priority-aware scheduler queue for planned operations. Pure planners and typed events make reconciliation deterministic, testable, priority-aware, and recoverable. This unit contains 22 indexed Rust declarations. |
| [`crates/pcloud-engine/src/selective.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-engine/src/selective.rs) | runtime/library unit | Selective sync include/exclude pattern policy (parity item P4.7). | Keep this responsibility explicit: Selective sync include/exclude pattern policy (parity item P4.7). A dedicated selective unit localizes dependencies, review, and regression testing inside `pcloud-engine`. | The focused `pcloud-engine` responsibility described as: Selective sync include/exclude pattern policy (parity item P4.7). Pure planners and typed events make reconciliation deterministic, testable, priority-aware, and recoverable. This unit contains 27 indexed Rust declarations. |
| [`crates/pcloud-engine/src/session_manager.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-engine/src/session_manager.rs) | runtime/library unit | Per-sync-root session state. Tracks session lifetime and cached | Keep this responsibility explicit: Per-sync-root session state. Tracks session lifetime and cached. A dedicated session manager unit localizes dependencies, review, and regression testing inside `pcloud-engine`. | The focused `pcloud-engine` responsibility described as: Per-sync-root session state. Tracks session lifetime and cached. Pure planners and typed events make reconciliation deterministic, testable, priority-aware, and recoverable. This unit contains 2 indexed Rust declarations. |
| [`crates/pcloud-engine/src/stall_detector.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-engine/src/stall_detector.rs) | runtime/library unit | Stall detection for the sync engine loop. | Keep this responsibility explicit: Stall detection for the sync engine loop. A dedicated stall detector unit localizes dependencies, review, and regression testing inside `pcloud-engine`. | The focused `pcloud-engine` responsibility described as: Stall detection for the sync engine loop. Pure planners and typed events make reconciliation deterministic, testable, priority-aware, and recoverable. This unit contains 19 indexed Rust declarations. |
| [`crates/pcloud-engine/src/transfers/bandwidth.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-engine/src/transfers/bandwidth.rs) | runtime/library unit | Token-bucket bandwidth limiter. | Keep this responsibility explicit: Token-bucket bandwidth limiter. A dedicated bandwidth unit localizes dependencies, review, and regression testing inside `pcloud-engine`. | The focused `pcloud-engine` responsibility described as: Token-bucket bandwidth limiter. Pure planners and typed events make reconciliation deterministic, testable, priority-aware, and recoverable. This unit contains 18 indexed Rust declarations. |
| [`crates/pcloud-engine/src/transfers/differential.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-engine/src/transfers/differential.rs) | runtime/library unit | T2.1.c — plan-side differential-upload strategy. | Keep this responsibility explicit: T2.1.c — plan-side differential-upload strategy. A dedicated differential unit localizes dependencies, review, and regression testing inside `pcloud-engine`. | The focused `pcloud-engine` responsibility described as: T2.1.c — plan-side differential-upload strategy. Pure planners and typed events make reconciliation deterministic, testable, priority-aware, and recoverable. This unit contains 7 indexed Rust declarations. |
| [`crates/pcloud-engine/src/transfers/downloads.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-engine/src/transfers/downloads.rs) | runtime/library unit | Tracks the download side of the transfer cycle: in-flight file | Keep this responsibility explicit: Tracks the download side of the transfer cycle: in-flight file. A dedicated downloads unit localizes dependencies, review, and regression testing inside `pcloud-engine`. | The focused `pcloud-engine` responsibility described as: Tracks the download side of the transfer cycle: in-flight file. Pure planners and typed events make reconciliation deterministic, testable, priority-aware, and recoverable. This unit contains 17 indexed Rust declarations. |
| [`crates/pcloud-engine/src/transfers/mod.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-engine/src/transfers/mod.rs) | runtime/library unit | Token-bucket bandwidth limiter (placeholder, disabled by default). | Keep this responsibility explicit: Token-bucket bandwidth limiter (placeholder, disabled by default). A dedicated mod unit localizes dependencies, review, and regression testing inside `pcloud-engine`. | The focused `pcloud-engine` responsibility described as: Token-bucket bandwidth limiter (placeholder, disabled by default). Pure planners and typed events make reconciliation deterministic, testable, priority-aware, and recoverable. This unit contains 4 indexed Rust declarations. |
| [`crates/pcloud-engine/src/transfers/uploads.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-engine/src/transfers/uploads.rs) | runtime/library unit | Tracks the upload side of the transfer cycle: in-flight file | Keep this responsibility explicit: Tracks the upload side of the transfer cycle: in-flight file. A dedicated uploads unit localizes dependencies, review, and regression testing inside `pcloud-engine`. | The focused `pcloud-engine` responsibility described as: Tracks the upload side of the transfer cycle: in-flight file. Pure planners and typed events make reconciliation deterministic, testable, priority-aware, and recoverable. This unit contains 25 indexed Rust declarations. |
| [`crates/pcloud-engine/tests/engine_basics.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-engine/tests/engine_basics.rs) | integration test | Integration tests for the pcloud-engine crate. | Prove the engine basics behavior at a crate or process boundary instead of relying only on unit tests. | Protecting engine basics boundary behavior from regressions. Pure planners and typed events make reconciliation deterministic, testable, priority-aware, and recoverable. This unit contains 15 indexed Rust declarations. |

All declarations: [`pcloud-engine` generated crate page](../crates/pcloud-engine.md).

## `pcloud-error` (2 units)

Package context: Mapping failures consistently across protocol, IPC, CLI, SDK, retry, policy, and operator diagnostics.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-error/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-error/src/lib.rs) | runtime/library unit | pcloud-error | Declare and compose the `pcloud-error` crate boundary. Give the workspace one stable failure language instead of ad-hoc strings and platform errno leakage. | The focused `pcloud-error` responsibility described as: pcloud-error. Stable codes and structured variants preserve actionable context while keeping secret material out of messages. This unit contains 37 indexed Rust declarations. |
| [`crates/pcloud-error/tests/code_stability.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-error/tests/code_stability.rs) | integration test | Snapshot test for stable numeric error codes. | Prove the code stability behavior at a crate or process boundary instead of relying only on unit tests. | Protecting code stability boundary behavior from regressions. Stable codes and structured variants preserve actionable context while keeping secret material out of messages. This unit contains 3 indexed Rust declarations. |

All declarations: [`pcloud-error` generated crate page](../crates/pcloud-error.md).

## `pcloud-fleet` (4 units)

Package context: Experimental standalone enrollment, device identity, CA-authenticated HTTPS heartbeat, Ed25519 device/command signatures, SLO reporting, and fleet command envelopes.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-fleet/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fleet/src/lib.rs) | runtime/library unit | Standalone fleet contract using controller-authenticated HTTPS plus Ed25519 device/command signatures; not wired into pcloudd. | Declare and compose the `pcloud-fleet` crate boundary. Let centrally managed deployments report health and receive bounded commands without changing single-user defaults. | The focused `pcloud-fleet` responsibility described as: Standalone fleet contract using controller-authenticated HTTPS plus Ed25519 device/command signatures; not wired into pcloudd. Null-by-default behavior, owner-only identity files, pinned controller CA trust, and constrained signed commands limit the management trust boundary; the crate is not wired into pcloudd. This unit contains 55 indexed Rust declarations. |
| [`crates/pcloud-fleet/tests/coverage_surface.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fleet/tests/coverage_surface.rs) | integration test | Public fleet-agent error and value-surface coverage. | Prove the coverage surface behavior at a crate or process boundary instead of relying only on unit tests. | Protecting coverage surface boundary behavior from regressions. Null-by-default behavior, owner-only identity files, pinned controller CA trust, and constrained signed commands limit the management trust boundary; the crate is not wired into pcloudd. This unit contains 3 indexed Rust declarations. |
| [`crates/pcloud-fleet/tests/live_mtls.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fleet/tests/live_mtls.rs) | integration test | End-to-end integration test: fleet agent -&gt; in-process reference server. | Prove the live mtls behavior at a crate or process boundary instead of relying only on unit tests. | Protecting live mtls boundary behavior from regressions. Null-by-default behavior, owner-only identity files, pinned controller CA trust, and constrained signed commands limit the management trust boundary; the crate is not wired into pcloudd. This unit contains 6 indexed Rust declarations. |
| [`crates/pcloud-fleet/tests/reference_server.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fleet/tests/reference_server.rs) | integration test | In-process reference fleet server used by `live_mtls.rs`. | Prove the reference server behavior at a crate or process boundary instead of relying only on unit tests. | Protecting reference server boundary behavior from regressions. Null-by-default behavior, owner-only identity files, pinned controller CA trust, and constrained signed commands limit the management trust boundary; the crate is not wired into pcloudd. This unit contains 12 indexed Rust declarations. |

All declarations: [`pcloud-fleet` generated crate page](../crates/pcloud-fleet.md).

## `pcloud-fs` (53 units)

Package context: Linux/BSD FUSE, macOS fuse-t/macFUSE, Windows WinFSP, read caching, staged writeback, journals, orphan cleanup, and filesystem watching.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-fs/benches/chunked_flush.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/benches/chunked_flush.rs) | benchmark | Criterion micro-benchmarks for the chunked-upload flush path landed in | Measure the chunked flush path so performance and capacity decisions have executable evidence. | Measuring chunked flush changes under a stable harness. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 12 indexed Rust declarations. |
| [`crates/pcloud-fs/benches/page_cache.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/benches/page_cache.rs) | benchmark | Criterion micro-benchmarks for \[`pcloud_fs::page_cache::PageCache`\]. | Measure the page cache path so performance and capacity decisions have executable evidence. | Measuring page cache changes under a stable harness. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 11 indexed Rust declarations. |
| [`crates/pcloud-fs/benches/writeback_flush.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/benches/writeback_flush.rs) | benchmark | FUSE writeback-flush latency benchmark. | Measure the writeback flush path so performance and capacity decisions have executable evidence. | Measuring writeback flush changes under a stable harness. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-fs/build.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/build.rs) | build helper | On macOS, the fuse-t/macFUSE dylib is loaded at runtime via dlopen | Perform the build package's deterministic build-time preparation outside runtime code. | Reproducible preparation for the package build. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-fs/src/backend.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/backend.rs) | runtime/library unit | Backend abstraction for the FUSE adapter. | Keep this responsibility explicit: Backend abstraction for the FUSE adapter. A dedicated backend unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: Backend abstraction for the FUSE adapter. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 81 indexed Rust declarations. |
| [`crates/pcloud-fs/src/errors.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/errors.rs) | runtime/library unit | Error taxonomy for the FUSE adapter layer. | Keep this responsibility explicit: Error taxonomy for the FUSE adapter layer. A dedicated errors unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: Error taxonomy for the FUSE adapter layer. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 17 indexed Rust declarations. |
| [`crates/pcloud-fs/src/fs_watcher.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/fs_watcher.rs) | runtime/library unit | Real filesystem watcher that detects local file changes and feeds them | Keep this responsibility explicit: Real filesystem watcher that detects local file changes and feeds them. A dedicated fs watcher unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: Real filesystem watcher that detects local file changes and feeds them. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 36 indexed Rust declarations. |
| [`crates/pcloud-fs/src/fuse_adapter.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/fuse_adapter.rs) | runtime/library unit | FUSE adapter traits and the 4.b `ProtoFuseAdapter` implementation. | Keep this responsibility explicit: FUSE adapter traits and the 4.b `ProtoFuseAdapter` implementation. A dedicated fuse adapter unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: FUSE adapter traits and the 4.b `ProtoFuseAdapter` implementation. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 150 indexed Rust declarations. |
| [`crates/pcloud-fs/src/fuser_shim.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/fuser_shim.rs) | runtime/library unit | `fuser::Filesystem` shim (bd-1du.4.e, sub-task 1). | Keep this responsibility explicit: `fuser::Filesystem` shim (bd-1du.4.e, sub-task 1). A dedicated fuser shim unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: `fuser::Filesystem` shim (bd-1du.4.e, sub-task 1). A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 48 indexed Rust declarations. |
| [`crates/pcloud-fs/src/inode.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/inode.rs) | runtime/library unit | Inode table and bidirectional path↔inode mapping for the FUSE adapter. | Keep this responsibility explicit: Inode table and bidirectional path↔inode mapping for the FUSE adapter. A dedicated inode unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: Inode table and bidirectional path↔inode mapping for the FUSE adapter. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 31 indexed Rust declarations. |
| [`crates/pcloud-fs/src/integrity_sweeper.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/integrity_sweeper.rs) | runtime/library unit | Integrity sweeper (H14b/c — PR3 of the H14 audit pipeline). | Keep this responsibility explicit: Integrity sweeper (H14b/c — PR3 of the H14 audit pipeline). A dedicated integrity sweeper unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: Integrity sweeper (H14b/c — PR3 of the H14 audit pipeline). A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 47 indexed Rust declarations. |
| [`crates/pcloud-fs/src/journal.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/journal.rs) | runtime/library unit | Writeback journal: ordered, crash-recoverable record of pending | Keep this responsibility explicit: Writeback journal: ordered, crash-recoverable record of pending. A dedicated journal unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: Writeback journal: ordered, crash-recoverable record of pending. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 17 indexed Rust declarations. |
| [`crates/pcloud-fs/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/lib.rs) | runtime/library unit | pcloud-fs | Declare and compose the `pcloud-fs` crate boundary. Translate remote-drive semantics into native filesystem behavior while surviving caching, partial writes, crashes, and platform ABI differences. | The focused `pcloud-fs` responsibility described as: pcloud-fs. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 39 indexed Rust declarations. |
| [`crates/pcloud-fs/src/metadata_cache.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/metadata_cache.rs) | runtime/library unit | LRU metadata cache with TTL for FUSE lookups/getattr/readdir. | Keep this responsibility explicit: LRU metadata cache with TTL for FUSE lookups/getattr/readdir. A dedicated metadata cache unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: LRU metadata cache with TTL for FUSE lookups/getattr/readdir. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 35 indexed Rust declarations. |
| [`crates/pcloud-fs/src/mount.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/mount.rs) | runtime/library unit | Mount policy types: options (`allow_other`, `read_only`) and their | Keep this responsibility explicit: Mount policy types: options (`allow_other`, `read_only`) and their. A dedicated mount unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: Mount policy types: options (`allow_other`, `read_only`) and their. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 10 indexed Rust declarations. |
| [`crates/pcloud-fs/src/mount_orphan.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/mount_orphan.rs) | runtime/library unit | **PLATFORM: all** (Linux \| FreeBSD \| macOS \| Windows). | Keep this responsibility explicit: **PLATFORM: all** (Linux \| FreeBSD \| macOS \| Windows). A dedicated mount orphan unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: **PLATFORM: all** (Linux \| FreeBSD \| macOS \| Windows). A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 26 indexed Rust declarations. |
| [`crates/pcloud-fs/src/mount_service.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/mount_service.rs) | runtime/library unit | **PLATFORM: all** (Linux \| FreeBSD \| macOS \| Windows) at the type | Keep this responsibility explicit: **PLATFORM: all** (Linux \| FreeBSD \| macOS \| Windows) at the type. A dedicated mount service unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: **PLATFORM: all** (Linux \| FreeBSD \| macOS \| Windows) at the type. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 37 indexed Rust declarations. |
| [`crates/pcloud-fs/src/page_cache.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/page_cache.rs) | runtime/library unit | Typed-key page cache facade for the FUSE read path. | Keep this responsibility explicit: Typed-key page cache facade for the FUSE read path. A dedicated page cache unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: Typed-key page cache facade for the FUSE read path. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 14 indexed Rust declarations. |
| [`crates/pcloud-fs/src/path_norm.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/path_norm.rs) | runtime/library unit | Path canonicalisation for FUSE adapter inputs. | Keep this responsibility explicit: Path canonicalisation for FUSE adapter inputs. A dedicated path norm unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: Path canonicalisation for FUSE adapter inputs. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 18 indexed Rust declarations. |
| [`crates/pcloud-fs/src/platform/bsd.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/platform/bsd.rs) | runtime/library unit | **PLATFORM: FreeBSD, NetBSD, OpenBSD, DragonFlyBSD.** | Keep this responsibility explicit: **PLATFORM: FreeBSD, NetBSD, OpenBSD, DragonFlyBSD.**. A dedicated bsd unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: **PLATFORM: FreeBSD, NetBSD, OpenBSD, DragonFlyBSD.**. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 35 indexed Rust declarations. |
| [`crates/pcloud-fs/src/platform/fuser_shim.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/platform/fuser_shim.rs) | runtime/library unit | **PLATFORM: FreeBSD, NetBSD, OpenBSD, and DragonFlyBSD.** | Keep this responsibility explicit: **PLATFORM: FreeBSD, NetBSD, OpenBSD, and DragonFlyBSD.**. A dedicated fuser shim unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: **PLATFORM: FreeBSD, NetBSD, OpenBSD, and DragonFlyBSD.**. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 63 indexed Rust declarations. |
| [`crates/pcloud-fs/src/platform/linux.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/platform/linux.rs) | runtime/library unit | **PLATFORM: Linux only.** | Keep this responsibility explicit: **PLATFORM: Linux only.**. A dedicated linux unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: **PLATFORM: Linux only.**. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 79 indexed Rust declarations. |
| [`crates/pcloud-fs/src/platform/macos.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/platform/macos.rs) | runtime/library unit | **PLATFORM: macOS only.** | Keep this responsibility explicit: **PLATFORM: macOS only.**. A dedicated macos unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: **PLATFORM: macOS only.**. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 138 indexed Rust declarations. |
| [`crates/pcloud-fs/src/platform/macos_ffi.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/platform/macos_ffi.rs) | runtime/library unit | **PLATFORM: macOS only.** | Keep this responsibility explicit: **PLATFORM: macOS only.**. A dedicated macos ffi unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: **PLATFORM: macOS only.**. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 35 indexed Rust declarations. |
| [`crates/pcloud-fs/src/platform/mod.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/platform/mod.rs) | runtime/library unit | **PLATFORM: all** (Linux \| BSD \| macOS \| Windows). | Keep this responsibility explicit: **PLATFORM: all** (Linux \| BSD \| macOS \| Windows). A dedicated mod unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: **PLATFORM: all** (Linux \| BSD \| macOS \| Windows). A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 14 indexed Rust declarations. |
| [`crates/pcloud-fs/src/platform/windows.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/platform/windows.rs) | runtime/library unit | **PLATFORM: Windows only.** | Keep this responsibility explicit: **PLATFORM: Windows only.**. A dedicated windows unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: **PLATFORM: Windows only.**. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 97 indexed Rust declarations. |
| [`crates/pcloud-fs/src/platform/windows_mountinfo.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/platform/windows_mountinfo.rs) | runtime/library unit | Win32 volume enumeration for pcloud-rs orphan-mount discovery. | Keep this responsibility explicit: Win32 volume enumeration for pcloud-rs orphan-mount discovery. A dedicated windows mountinfo unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: Win32 volume enumeration for pcloud-rs orphan-mount discovery. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 11 indexed Rust declarations. |
| [`crates/pcloud-fs/src/platform/winfsp_ffi.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/platform/winfsp_ffi.rs) | runtime/library unit | **PLATFORM: Windows only.** Hand-rolled thin FFI bindings against the | Keep this responsibility explicit: **PLATFORM: Windows only.** Hand-rolled thin FFI bindings against the. A dedicated winfsp ffi unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: **PLATFORM: Windows only.** Hand-rolled thin FFI bindings against the. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 50 indexed Rust declarations. |
| [`crates/pcloud-fs/src/read_path.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/read_path.rs) | runtime/library unit | Read path: in-memory read service with prefetch window and cache, | Keep this responsibility explicit: Read path: in-memory read service with prefetch window and cache,. A dedicated read path unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: Read path: in-memory read service with prefetch window and cache,. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 13 indexed Rust declarations. |
| [`crates/pcloud-fs/src/slo_hook.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/slo_hook.rs) | runtime/library unit | Process-wide SLO observation hook for the FUSE read path and the | Keep this responsibility explicit: Process-wide SLO observation hook for the FUSE read path and the. A dedicated slo hook unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: Process-wide SLO observation hook for the FUSE read path and the. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 14 indexed Rust declarations. |
| [`crates/pcloud-fs/src/staging.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/staging.rs) | runtime/library unit | Disk-backed staging directory for pending uploads (bd-1du.4.d). | Keep this responsibility explicit: Disk-backed staging directory for pending uploads (bd-1du.4.d). A dedicated staging unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: Disk-backed staging directory for pending uploads (bd-1du.4.d). A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 33 indexed Rust declarations. |
| [`crates/pcloud-fs/src/write_journal.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/write_journal.rs) | runtime/library unit | Write-ahead journal for the FUSE write path (bd-1du.4.d). | Keep this responsibility explicit: Write-ahead journal for the FUSE write path (bd-1du.4.d). A dedicated write journal unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: Write-ahead journal for the FUSE write path (bd-1du.4.d). A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 34 indexed Rust declarations. |
| [`crates/pcloud-fs/src/write_path.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/write_path.rs) | runtime/library unit | FUSE write path (bd-1du.4.d). | Keep this responsibility explicit: FUSE write path (bd-1du.4.d). A dedicated write path unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: FUSE write path (bd-1du.4.d). A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 126 indexed Rust declarations. |
| [`crates/pcloud-fs/src/writeback.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/src/writeback.rs) | runtime/library unit | Writeback service: stages writes into the journal, tracks completed | Keep this responsibility explicit: Writeback service: stages writes into the journal, tracks completed. A dedicated writeback unit localizes dependencies, review, and regression testing inside `pcloud-fs`. | The focused `pcloud-fs` responsibility described as: Writeback service: stages writes into the journal, tracks completed. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 8 indexed Rust declarations. |
| [`crates/pcloud-fs/tests/backend_coverage.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/tests/backend_coverage.rs) | integration test | Public-contract coverage for the protocol-backed filesystem adapters. | Prove the backend coverage behavior at a crate or process boundary instead of relying only on unit tests. | Protecting backend coverage boundary behavior from regressions. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 43 indexed Rust declarations. |
| [`crates/pcloud-fs/tests/chunked_upload_write_multi_gib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/tests/chunked_upload_write_multi_gib.rs) | integration test | Multi-GiB chunked upload pipelining integration test. | Prove the chunked upload write multi gib behavior at a crate or process boundary instead of relying only on unit tests. | Protecting chunked upload write multi gib boundary behavior from regressions. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 20 indexed Rust declarations. |
| [`crates/pcloud-fs/tests/fuse_adapter_unit.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/tests/fuse_adapter_unit.rs) | integration test | Integration tests for the `FuseAdapter` trait implementation | Prove the fuse adapter unit behavior at a crate or process boundary instead of relying only on unit tests. | Protecting fuse adapter unit boundary behavior from regressions. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 43 indexed Rust declarations. |
| [`crates/pcloud-fs/tests/fuse_dyn_shim_write.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/tests/fuse_dyn_shim_write.rs) | integration test | bd-1du.4.6 — dyn-shim write-path integration test. | Prove the fuse dyn shim write behavior at a crate or process boundary instead of relying only on unit tests. | Protecting fuse dyn shim write boundary behavior from regressions. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 7 indexed Rust declarations. |
| [`crates/pcloud-fs/tests/fuse_kernel_e2e.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/tests/fuse_kernel_e2e.rs) | integration test | PLAN_A_PLUS P0.5 — FUSE kernel end-to-end test. | Prove the fuse kernel e2e behavior at a crate or process boundary instead of relying only on unit tests. | Protecting fuse kernel e2e boundary behavior from regressions. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 20 indexed Rust declarations. |
| [`crates/pcloud-fs/tests/fuse_lifecycle_hardening.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/tests/fuse_lifecycle_hardening.rs) | integration test | bd-1du.4 — FUSE mount lifecycle hardening integration tests. | Prove the fuse lifecycle hardening behavior at a crate or process boundary instead of relying only on unit tests. | Protecting fuse lifecycle hardening boundary behavior from regressions. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 5 indexed Rust declarations. |
| [`crates/pcloud-fs/tests/fuse_macos_integration.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/tests/fuse_macos_integration.rs) | integration test | macOS FUSE integration tests using fuse-t. | Prove the fuse macos integration behavior at a crate or process boundary instead of relying only on unit tests. | Protecting fuse macos integration boundary behavior from regressions. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 6 indexed Rust declarations. |
| [`crates/pcloud-fs/tests/fuse_mount_integration.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/tests/fuse_mount_integration.rs) | integration test | bd-1du.4.b / 4.c / 4.e integration tests. | Prove the fuse mount integration behavior at a crate or process boundary instead of relying only on unit tests. | Protecting fuse mount integration boundary behavior from regressions. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 12 indexed Rust declarations. |
| [`crates/pcloud-fs/tests/fuse_read_path_live.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/tests/fuse_read_path_live.rs) | integration test | bd-1du.4 — live read-path FUSE integration test. | Prove the fuse read path live behavior at a crate or process boundary instead of relying only on unit tests. | Protecting fuse read path live boundary behavior from regressions. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 11 indexed Rust declarations. |
| [`crates/pcloud-fs/tests/fuse_small_write_wiring.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/tests/fuse_small_write_wiring.rs) | integration test | bd-1du.4.6 footnote `\[fuse-wiring\]` integration test. | Prove the fuse small write wiring behavior at a crate or process boundary instead of relying only on unit tests. | Protecting fuse small write wiring boundary behavior from regressions. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 18 indexed Rust declarations. |
| [`crates/pcloud-fs/tests/fuse_write_path_live.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/tests/fuse_write_path_live.rs) | integration test | bd-1du.4.6 — live write-path + remount readback integration test. | Prove the fuse write path live behavior at a crate or process boundary instead of relying only on unit tests. | Protecting fuse write path live boundary behavior from regressions. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 22 indexed Rust declarations. |
| [`crates/pcloud-fs/tests/inode_unit.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/tests/inode_unit.rs) | integration test | Integration tests for `InodeTable` focused on path↔ino round-trip, | Prove the inode unit behavior at a crate or process boundary instead of relying only on unit tests. | Protecting inode unit boundary behavior from regressions. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 10 indexed Rust declarations. |
| [`crates/pcloud-fs/tests/macos_mount_live.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/tests/macos_mount_live.rs) | integration test | **PLATFORM: macOS only.** | Prove the macos mount live behavior at a crate or process boundary instead of relying only on unit tests. | Protecting macos mount live boundary behavior from regressions. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 29 indexed Rust declarations. |
| [`crates/pcloud-fs/tests/macos_platform_integration.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/tests/macos_platform_integration.rs) | integration test | **PLATFORM: macOS only.** | Prove the macos platform integration behavior at a crate or process boundary instead of relying only on unit tests. | Protecting macos platform integration boundary behavior from regressions. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 32 indexed Rust declarations. |
| [`crates/pcloud-fs/tests/mount_transport_wiring.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/tests/mount_transport_wiring.rs) | integration test | Integration test: verify mount transport wiring with mock backends. | Prove the mount transport wiring behavior at a crate or process boundary instead of relying only on unit tests. | Protecting mount transport wiring boundary behavior from regressions. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 6 indexed Rust declarations. |
| [`crates/pcloud-fs/tests/platform_mountinfo_crossplat.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/tests/platform_mountinfo_crossplat.rs) | integration test | **PLATFORM: all** (Linux \| BSD \| macOS \| Windows). | Prove the platform mountinfo crossplat behavior at a crate or process boundary instead of relying only on unit tests. | Protecting platform mountinfo crossplat boundary behavior from regressions. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 5 indexed Rust declarations. |
| [`crates/pcloud-fs/tests/winfsp_mount_live.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/tests/winfsp_mount_live.rs) | integration test | bd-xplat-windows — live WinFSP mount smoke test. | Prove the winfsp mount live behavior at a crate or process boundary instead of relying only on unit tests. | Protecting winfsp mount live boundary behavior from regressions. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 50 indexed Rust declarations. |
| [`crates/pcloud-fs/tests/write_path_replay.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/tests/write_path_replay.rs) | integration test | bd-1du.4.d integration test: write-path durability across remount. | Prove the write path replay behavior at a crate or process boundary instead of relying only on unit tests. | Protecting write path replay boundary behavior from regressions. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 5 indexed Rust declarations. |
| [`crates/pcloud-fs/tests/write_path_unit.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-fs/tests/write_path_unit.rs) | integration test | Integration tests for the `WritePathService` — the staging-blob + | Prove the write path unit behavior at a crate or process boundary instead of relying only on unit tests. | Protecting write path unit boundary behavior from regressions. A portable adapter core plus narrow native shims, stable inode mapping, bounded caches, and write-ahead staging protect correctness across kernels. This unit contains 37 indexed Rust declarations. |

All declarations: [`pcloud-fs` generated crate page](../crates/pcloud-fs.md).

## `pcloud-idp` (5 units)

Package context: OIDC Authorization Code with PKCE, discovery/JWKS validation, and future SAML/LDAP or trusted-issuer exchange adapters.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-idp/src/exchange.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-idp/src/exchange.rs) | runtime/library unit | pCloud trusted-issuer token exchange. | Keep this responsibility explicit: pCloud trusted-issuer token exchange. A dedicated exchange unit localizes dependencies, review, and regression testing inside `pcloud-idp`. | The focused `pcloud-idp` responsibility described as: pCloud trusted-issuer token exchange. S256-only PKCE, RS256 verification, issuer/audience checks, cached JWKS, and an explicit unimplemented exchange keep incomplete federation honest. This unit contains 26 indexed Rust declarations. |
| [`crates/pcloud-idp/src/jwks.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-idp/src/jwks.rs) | runtime/library unit | OIDC discovery and JWKS fetch/cache with RS256-only ID token verification. | Keep this responsibility explicit: OIDC discovery and JWKS fetch/cache with RS256-only ID token verification. A dedicated jwks unit localizes dependencies, review, and regression testing inside `pcloud-idp`. | The focused `pcloud-idp` responsibility described as: OIDC discovery and JWKS fetch/cache with RS256-only ID token verification. S256-only PKCE, RS256 verification, issuer/audience checks, cached JWKS, and an explicit unimplemented exchange keep incomplete federation honest. This unit contains 20 indexed Rust declarations. |
| [`crates/pcloud-idp/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-idp/src/lib.rs) | runtime/library unit | Enterprise Identity Provider (IdP) broker trait scaffold. | Declare and compose the `pcloud-idp` crate boundary. Model enterprise federated-login flows separately from native pCloud authentication. | The focused `pcloud-idp` responsibility described as: Enterprise Identity Provider (IdP) broker trait scaffold. S256-only PKCE, RS256 verification, issuer/audience checks, cached JWKS, and an explicit unimplemented exchange keep incomplete federation honest. This unit contains 22 indexed Rust declarations. |
| [`crates/pcloud-idp/src/oidc.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-idp/src/oidc.rs) | runtime/library unit | OIDC Authorization Code + PKCE broker. | Keep this responsibility explicit: OIDC Authorization Code + PKCE broker. A dedicated oidc unit localizes dependencies, review, and regression testing inside `pcloud-idp`. | The focused `pcloud-idp` responsibility described as: OIDC Authorization Code + PKCE broker. S256-only PKCE, RS256 verification, issuer/audience checks, cached JWKS, and an explicit unimplemented exchange keep incomplete federation honest. This unit contains 32 indexed Rust declarations. |
| [`crates/pcloud-idp/src/pkce.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-idp/src/pkce.rs) | runtime/library unit | RFC 7636 PKCE helpers (S256 only). | Keep this responsibility explicit: RFC 7636 PKCE helpers (S256 only). A dedicated pkce unit localizes dependencies, review, and regression testing inside `pcloud-idp`. | The focused `pcloud-idp` responsibility described as: RFC 7636 PKCE helpers (S256 only). S256-only PKCE, RS256 verification, issuer/audience checks, cached JWKS, and an explicit unimplemented exchange keep incomplete federation honest. This unit contains 9 indexed Rust declarations. |

All declarations: [`pcloud-idp` generated crate page](../crates/pcloud-idp.md).

## `pcloud-ipc` (24 units)

Package context: CLI/SDK/web requests over Unix sockets, Solaris credentials, or Windows named pipes with typed framing and trace propagation.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-ipc/benches/ipc_codec.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/benches/ipc_codec.rs) | benchmark | IPC codec benchmarks. | Measure the ipc codec path so performance and capacity decisions have executable evidence. | Measuring ipc codec changes under a stable harness. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 4 indexed Rust declarations. |
| [`crates/pcloud-ipc/examples/client_ping.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/examples/client_ping.rs) | example | Demonstrates an end-to-end IPC round-trip against an in-process daemon. | Show a runnable client ping workflow and keep the intended call sequence discoverable. | Teaching and smoke-testing the client ping call path. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-ipc/fuzz/fuzz_targets/fuzz_ipc_frame.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/fuzz/fuzz_targets/fuzz_ipc_frame.rs) | fuzz target/helper | Fuzz target: feed arbitrary bytes into the IPC frame decoders. | Drive malformed or adversarial inputs through the fuzz ipc frame boundary to find parser and invariant failures. | Finding malformed-input and invariant failures around fuzz ipc frame. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 0 indexed Rust declarations. |
| [`crates/pcloud-ipc/src/auth.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/auth.rs) | runtime/library unit | IPC peer identity | Keep this responsibility explicit: IPC peer identity. A dedicated auth unit localizes dependencies, review, and regression testing inside `pcloud-ipc`. | The focused `pcloud-ipc` responsibility described as: IPC peer identity. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 3 indexed Rust declarations. |
| [`crates/pcloud-ipc/src/client.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/client.rs) | runtime/library unit | IPC client: connects to the daemon's Unix domain socket (or Windows | Keep this responsibility explicit: IPC client: connects to the daemon's Unix domain socket (or Windows. A dedicated client unit localizes dependencies, review, and regression testing inside `pcloud-ipc`. | The focused `pcloud-ipc` responsibility described as: IPC client: connects to the daemon's Unix domain socket (or Windows. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 6 indexed Rust declarations. |
| [`crates/pcloud-ipc/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/lib.rs) | runtime/library unit | pcloud-ipc | Declare and compose the `pcloud-ipc` crate boundary. Provide one owner-authenticated local protocol between untrusted client processes and the credential-holding daemon. | The focused `pcloud-ipc` responsibility described as: pcloud-ipc. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 10 indexed Rust declarations. |
| [`crates/pcloud-ipc/src/methods.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs) | runtime/library unit | Typed `Method` enum: exhaustive catalog of IPC operations the daemon | Keep this responsibility explicit: Typed `Method` enum: exhaustive catalog of IPC operations the daemon. A dedicated methods unit localizes dependencies, review, and regression testing inside `pcloud-ipc`. | The focused `pcloud-ipc` responsibility described as: Typed `Method` enum: exhaustive catalog of IPC operations the daemon. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 46 indexed Rust declarations. |
| [`crates/pcloud-ipc/src/path_validation.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/path_validation.rs) | runtime/library unit | Local sync-root path validation helpers. | Keep this responsibility explicit: Local sync-root path validation helpers. A dedicated path validation unit localizes dependencies, review, and regression testing inside `pcloud-ipc`. | The focused `pcloud-ipc` responsibility described as: Local sync-root path validation helpers. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 12 indexed Rust declarations. |
| [`crates/pcloud-ipc/src/platform/linux.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/platform/linux.rs) | runtime/library unit | **PLATFORM: Linux only.** Peer authentication via `getsockopt(SO_PEERCRED)`. | Keep this responsibility explicit: **PLATFORM: Linux only.** Peer authentication via `getsockopt(SO_PEERCRED)`. A dedicated linux unit localizes dependencies, review, and regression testing inside `pcloud-ipc`. | The focused `pcloud-ipc` responsibility described as: **PLATFORM: Linux only.** Peer authentication via `getsockopt(SO_PEERCRED)`. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 8 indexed Rust declarations. |
| [`crates/pcloud-ipc/src/platform/mod.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/platform/mod.rs) | runtime/library unit | **PLATFORM: all.** Trait layer over peer-authenticated local IPC. | Keep this responsibility explicit: **PLATFORM: all.** Trait layer over peer-authenticated local IPC. A dedicated mod unit localizes dependencies, review, and regression testing inside `pcloud-ipc`. | The focused `pcloud-ipc` responsibility described as: **PLATFORM: all.** Trait layer over peer-authenticated local IPC. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 15 indexed Rust declarations. |
| [`crates/pcloud-ipc/src/platform/solarish.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/platform/solarish.rs) | runtime/library unit | **PLATFORM: illumos and Solaris.** | Keep this responsibility explicit: **PLATFORM: illumos and Solaris.**. A dedicated solarish unit localizes dependencies, review, and regression testing inside `pcloud-ipc`. | The focused `pcloud-ipc` responsibility described as: **PLATFORM: illumos and Solaris.**. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 8 indexed Rust declarations. |
| [`crates/pcloud-ipc/src/platform/unix.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/platform/unix.rs) | runtime/library unit | **PLATFORM: FreeBSD, OpenBSD, NetBSD, DragonFly BSD, macOS.** | Keep this responsibility explicit: **PLATFORM: FreeBSD, OpenBSD, NetBSD, DragonFly BSD, macOS.**. A dedicated unix unit localizes dependencies, review, and regression testing inside `pcloud-ipc`. | The focused `pcloud-ipc` responsibility described as: **PLATFORM: FreeBSD, OpenBSD, NetBSD, DragonFly BSD, macOS.**. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 9 indexed Rust declarations. |
| [`crates/pcloud-ipc/src/platform/windows.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/platform/windows.rs) | runtime/library unit | **PLATFORM: Windows 10/11 (tier 1).** | Keep this responsibility explicit: **PLATFORM: Windows 10/11 (tier 1).**. A dedicated windows unit localizes dependencies, review, and regression testing inside `pcloud-ipc`. | The focused `pcloud-ipc` responsibility described as: **PLATFORM: Windows 10/11 (tier 1).**. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 55 indexed Rust declarations. |
| [`crates/pcloud-ipc/src/protocol.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/protocol.rs) | runtime/library unit | Wire protocol: \[`Request`\] / \[`Response`\] envelopes, framing, | Keep this responsibility explicit: Wire protocol: \[`Request`\] / \[`Response`\] envelopes, framing,. A dedicated protocol unit localizes dependencies, review, and regression testing inside `pcloud-ipc`. | The focused `pcloud-ipc` responsibility described as: Wire protocol: \[`Request`\] / \[`Response`\] envelopes, framing,. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 22 indexed Rust declarations. |
| [`crates/pcloud-ipc/src/redacted.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/redacted.rs) | runtime/library unit | Secret-bearing wire-type wrapper. | Keep this responsibility explicit: Secret-bearing wire-type wrapper. A dedicated redacted unit localizes dependencies, review, and regression testing inside `pcloud-ipc`. | The focused `pcloud-ipc` responsibility described as: Secret-bearing wire-type wrapper. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 18 indexed Rust declarations. |
| [`crates/pcloud-ipc/src/server.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/server.rs) | runtime/library unit | IPC server helpers | Keep this responsibility explicit: IPC server helpers. A dedicated server unit localizes dependencies, review, and regression testing inside `pcloud-ipc`. | The focused `pcloud-ipc` responsibility described as: IPC server helpers. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 9 indexed Rust declarations. |
| [`crates/pcloud-ipc/src/transport.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/transport.rs) | runtime/library unit | Local IPC transport | Keep this responsibility explicit: Local IPC transport. A dedicated transport unit localizes dependencies, review, and regression testing inside `pcloud-ipc`. | The focused `pcloud-ipc` responsibility described as: Local IPC transport. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 77 indexed Rust declarations. |
| [`crates/pcloud-ipc/tests/envelope.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/tests/envelope.rs) | integration test | Wire-shape and back-compat tests for \[`RequestEnvelope`\]. | Prove the envelope behavior at a crate or process boundary instead of relying only on unit tests. | Protecting envelope boundary behavior from regressions. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 5 indexed Rust declarations. |
| [`crates/pcloud-ipc/tests/peer_and_protocol.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/tests/peer_and_protocol.rs) | integration test | Integration tests for pcloud-ipc peer authorization and protocol framing. | Prove the peer and protocol behavior at a crate or process boundary instead of relying only on unit tests. | Protecting peer and protocol boundary behavior from regressions. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 17 indexed Rust declarations. |
| [`crates/pcloud-ipc/tests/platform_ipc_crossplat.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/tests/platform_ipc_crossplat.rs) | integration test | **PLATFORM: all** (Linux \| FreeBSD \| OpenBSD \| NetBSD \| macOS \| Windows). | Prove the platform ipc crossplat behavior at a crate or process boundary instead of relying only on unit tests. | Protecting platform ipc crossplat boundary behavior from regressions. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 5 indexed Rust declarations. |
| [`crates/pcloud-ipc/tests/proptest_methods_roundtrip.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/tests/proptest_methods_roundtrip.rs) | integration test | Property-based round-trip tests for every IPC `Method`, `Request`, and | Prove the proptest methods roundtrip behavior at a crate or process boundary instead of relying only on unit tests. | Protecting proptest methods roundtrip boundary behavior from regressions. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 14 indexed Rust declarations. |
| [`crates/pcloud-ipc/tests/request_size_cap.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/tests/request_size_cap.rs) | integration test | Tests for the per-request IPC frame size cap (P0.8). | Prove the request size cap behavior at a crate or process boundary instead of relying only on unit tests. | Protecting request size cap boundary behavior from regressions. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 4 indexed Rust declarations. |
| [`crates/pcloud-ipc/tests/security_invariants.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/tests/security_invariants.rs) | integration test | Security invariants integration harness (IPC slice) | Prove the security invariants behavior at a crate or process boundary instead of relying only on unit tests. | Protecting security invariants boundary behavior from regressions. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 9 indexed Rust declarations. |
| [`crates/pcloud-ipc/tests/stress_concurrent_clients.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/tests/stress_concurrent_clients.rs) | integration test | Concurrent IPC stress test: 50 clients × 500 sequential requests each | Prove the stress concurrent clients behavior at a crate or process boundary instead of relying only on unit tests. | Protecting stress concurrent clients boundary behavior from regressions. Peer UID/SID checks, owner-only endpoints, request-size caps, redacted secret fields, and exhaustive typed methods harden the local boundary. This unit contains 4 indexed Rust declarations. |

All declarations: [`pcloud-ipc` generated crate page](../crates/pcloud-ipc.md).

## `pcloud-kms` (2 units)

Package context: Null/default operation plus optional AWS KMS, HashiCorp Vault Transit, and PKCS#11 provider paths.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-kms/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-kms/src/lib.rs) | runtime/library unit | pcloud-kms — Enterprise KMS / HSM key-wrapping integration | Declare and compose the `pcloud-kms` crate boundary. Allow enterprise operators to wrap data-encryption keys under external KMS or HSM control. | The focused `pcloud-kms` responsibility described as: pcloud-kms — Enterprise KMS / HSM key-wrapping integration. A narrow KmsProvider contract, provider-specific feature gates, no silent downgrade, and zeroized plaintext DEKs isolate external key custody. This unit contains 97 indexed Rust declarations. |
| [`crates/pcloud-kms/tests/coverage_surface.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-kms/tests/coverage_surface.rs) | integration test | \[test\] | Prove the coverage surface behavior at a crate or process boundary instead of relying only on unit tests. | Protecting coverage surface boundary behavior from regressions. A narrow KmsProvider contract, provider-specific feature gates, no silent downgrade, and zeroized plaintext DEKs isolate external key custody. This unit contains 7 indexed Rust declarations. |

All declarations: [`pcloud-kms` generated crate page](../crates/pcloud-kms.md).

## `pcloud-live-e2e` (28 units)

Package context: Gated auth, account, transfer, sharing, backup, Crypto, sync, mount, snapshot, rate-limit, and fleet integration checks.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-live-e2e/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/src/lib.rs) | runtime/library unit | `pcloud-live-e2e` — integration-test-only crate that exercises the Rust | Declare and compose the `pcloud-live-e2e` crate boundary. Prove selected behavior against the real pCloud service and native environments instead of relying only on mocks. | The focused `pcloud-live-e2e` responsibility described as: `pcloud-live-e2e` — integration-test-only crate that exercises the Rust. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 0 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/account_utility.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/account_utility.rs) | integration test | Live coverage for account-utility verbs (CLAUDEREV iter-1 TEST-H-2, | Prove the account utility behavior at a crate or process boundary instead of relying only on unit tests. | Protecting account utility boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 6 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/account_utility_destructive.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/account_utility_destructive.rs) | integration test | Live coverage for **destructive** account-utility verbs (CLAUDEREV | Prove the account utility destructive behavior at a crate or process boundary instead of relying only on unit tests. | Protecting account utility destructive boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 5 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/auth_lifecycle.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/auth_lifecycle.rs) | integration test | Live auth-lifecycle coverage: login (password/token), logout, | Prove the auth lifecycle behavior at a crate or process boundary instead of relying only on unit tests. | Protecting auth lifecycle boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 6 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/backup_lifecycle.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/backup_lifecycle.rs) | integration test | Live coverage: backup create / stop-device / delete-backup-device | Prove the backup lifecycle behavior at a crate or process boundary instead of relying only on unit tests. | Protecting backup lifecycle boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 10 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/change_crypto_pass.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/change_crypto_pass.rs) | integration test | Live verb-reached coverage for the `change_crypto_pass` family | Prove the change crypto pass behavior at a crate or process boundary instead of relying only on unit tests. | Protecting change crypto pass boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 3 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/common/mod.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/common/mod.rs) | integration test | Shared gating / bootstrap helpers for the live-E2E harness. | Prove the mod behavior at a crate or process boundary instead of relying only on unit tests. | Protecting mod boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 40 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/crypto.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/crypto.rs) | integration test | Live crypto-lifecycle coverage: setup (or unlock if already set), get | Prove the crypto behavior at a crate or process boundary instead of relying only on unit tests. | Protecting crypto boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 3 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/drain.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/drain.rs) | integration test | Live drain-state coverage: bootstrap a daemon, verify it reports | Prove the drain behavior at a crate or process boundary instead of relying only on unit tests. | Protecting drain boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 4 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/field_selectors.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/field_selectors.rs) | integration test | Live field-selector probes: verify userinfo / session-status / | Prove the field selectors behavior at a crate or process boundary instead of relying only on unit tests. | Protecting field selectors boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 5 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/fleet_mtls.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/fleet_mtls.rs) | integration test | Live fleet-heartbeat coverage: dispatches a default heartbeat at an | Prove the fleet mtls behavior at a crate or process boundary instead of relying only on unit tests. | Protecting fleet mtls boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 6 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/integrity_sweeper.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/integrity_sweeper.rs) | integration test | Live integrity-sweeper coverage: IntegrityStatus probe and | Prove the integrity sweeper behavior at a crate or process boundary instead of relying only on unit tests. | Protecting integrity sweeper boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 5 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/mount_linux.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/mount_linux.rs) | integration test | Live mounted-drive coverage (Linux only): mount a fresh pCloud FUSE | Prove the mount linux behavior at a crate or process boundary instead of relying only on unit tests. | Protecting mount linux boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 8 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/public_links.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/public_links.rs) | integration test | Live public-link coverage: create file/folder link, list, change | Prove the public links behavior at a crate or process boundary instead of relying only on unit tests. | Protecting public links boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 7 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/rate_limit.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/rate_limit.rs) | integration test | Live IPC rate-limit coverage: send more expensive requests than the | Prove the rate limit behavior at a crate or process boundary instead of relying only on unit tests. | Protecting rate limit boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 2 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/shares.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/shares.rs) | integration test | Live share-lifecycle coverage: ShareFolder invitation to a second | Prove the shares behavior at a crate or process boundary instead of relying only on unit tests. | Protecting shares boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 7 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/shares_a_to_b.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/shares_a_to_b.rs) | integration test | Live A→B share lifecycle: account A creates a folder, invites account B, | Prove the shares a to b behavior at a crate or process boundary instead of relying only on unit tests. | Protecting shares a to b boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 12 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/shares_active_a_to_b.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/shares_active_a_to_b.rs) | integration test | Live A↔B active-share visibility proof. | Prove the shares active a to b behavior at a crate or process boundary instead of relying only on unit tests. | Protecting shares active a to b boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 9 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/snapshot_pipeline.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/snapshot_pipeline.rs) | integration test | Live snapshot-pipeline coverage: create (default zstd), verify | Prove the snapshot pipeline behavior at a crate or process boundary instead of relying only on unit tests. | Protecting snapshot pipeline boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 6 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/snapshot_prune.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/snapshot_prune.rs) | integration test | Live snapshot-prune coverage: seed a directory with 10 fake snapshot | Prove the snapshot prune behavior at a crate or process boundary instead of relying only on unit tests. | Protecting snapshot prune boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 4 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/sync_loop_live.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/sync_loop_live.rs) | integration test | Live end-to-end test for the background sync loop. | Prove the sync loop live behavior at a crate or process boundary instead of relying only on unit tests. | Protecting sync loop live boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 3 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/sync_roots.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/sync_roots.rs) | integration test | Live sync-root lifecycle coverage: add (Full / UploadOnly / | Prove the sync roots behavior at a crate or process boundary instead of relying only on unit tests. | Protecting sync roots boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 6 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/team_share_verb.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/team_share_verb.rs) | integration test | Live coverage for plain (non-crypto) team-share verbs (CLAUDEREV | Prove the team share verb behavior at a crate or process boundary instead of relying only on unit tests. | Protecting team share verb boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 5 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/tfa_lifecycle.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/tfa_lifecycle.rs) | integration test | Live coverage for TFA verbs (rows 19-22 of the parity matrix). | Prove the tfa lifecycle behavior at a crate or process boundary instead of relying only on unit tests. | Protecting tfa lifecycle boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 7 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/transfers.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/transfers.rs) | integration test | Live transfer harness: upload_create/write/save, get_file_link, download. | Prove the transfers behavior at a crate or process boundary instead of relying only on unit tests. | Protecting transfers boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 3 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/tree_link_from_paths.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/tree_link_from_paths.rs) | integration test | Live coverage: `ptree_public_link` path-based IPC variant (row 149). | Prove the tree link from paths behavior at a crate or process boundary instead of relying only on unit tests. | Protecting tree link from paths boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 10 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/upload_writefromfile.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/upload_writefromfile.rs) | integration test | Live coverage for `Request::UploadWriteFromFile` (parity matrix row 93, | Prove the upload writefromfile behavior at a crate or process boundary instead of relying only on unit tests. | Protecting upload writefromfile boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 3 indexed Rust declarations. |
| [`crates/pcloud-live-e2e/tests/windows_liveness.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-live-e2e/tests/windows_liveness.rs) | integration test | Live Windows-liveness probe: daemon ↔ real pCloud API round-trip. | Prove the windows liveness behavior at a crate or process boundary instead of relying only on unit tests. | Protecting windows liveness boundary behavior from regressions. Ignored-by-default destructive gates, disposable-account conventions, unique scratch objects, and serial execution reduce accidental production impact. This unit contains 12 indexed Rust declarations. |

All declarations: [`pcloud-live-e2e` generated crate page](../crates/pcloud-live-e2e.md).

## `pcloud-mockserver` (2 units)

Package context: Local pCloud-like HTTP responses, error injection, request assertions, and repeatable integration flows.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-mockserver/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-mockserver/src/lib.rs) | runtime/library unit | `pcloud-mockserver` — a tiny, in-process HTTP mock of the pCloud REST API. | Declare and compose the `pcloud-mockserver` crate boundary. Make protocol and backend integration deterministic without network access or production secrets. | The focused `pcloud-mockserver` responsibility described as: `pcloud-mockserver` — a tiny, in-process HTTP mock of the pCloud REST API. An in-process server gives tests realistic I/O boundaries while retaining exact control of timing and responses. This unit contains 54 indexed Rust declarations. |
| [`crates/pcloud-mockserver/tests/mock_flows.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-mockserver/tests/mock_flows.rs) | integration test | Mock-backed integration flows. | Prove the mock flows behavior at a crate or process boundary instead of relying only on unit tests. | Protecting mock flows boundary behavior from regressions. An in-process server gives tests realistic I/O boundaries while retaining exact control of timing and responses. This unit contains 9 indexed Rust declarations. |

All declarations: [`pcloud-mockserver` generated crate page](../crates/pcloud-mockserver.md).

## `pcloud-model` (11 units)

Package context: Typed IDs, users, auth, files, folders, shares, links, sync state, transfers, conflicts, Crypto, and health payloads.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-model/src/auth.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-model/src/auth.rs) | runtime/library unit | Client-visible authentication state machine. | Keep this responsibility explicit: Client-visible authentication state machine. A dedicated auth unit localizes dependencies, review, and regression testing inside `pcloud-model`. | The focused `pcloud-model` responsibility described as: Client-visible authentication state machine. Small serializable types and newtype IDs prevent cross-layer duplication and accidental identifier mixing. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-model/src/conflict.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-model/src/conflict.rs) | runtime/library unit | Taxonomy of sync conflicts surfaced by the engine planner. | Keep this responsibility explicit: Taxonomy of sync conflicts surfaced by the engine planner. A dedicated conflict unit localizes dependencies, review, and regression testing inside `pcloud-model`. | The focused `pcloud-model` responsibility described as: Taxonomy of sync conflicts surfaced by the engine planner. Small serializable types and newtype IDs prevent cross-layer duplication and accidental identifier mixing. This unit contains 2 indexed Rust declarations. |
| [`crates/pcloud-model/src/crypto.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-model/src/crypto.rs) | runtime/library unit | Client-visible crypto subsystem state. | Keep this responsibility explicit: Client-visible crypto subsystem state. A dedicated crypto unit localizes dependencies, review, and regression testing inside `pcloud-model`. | The focused `pcloud-model` responsibility described as: Client-visible crypto subsystem state. Small serializable types and newtype IDs prevent cross-layer duplication and accidental identifier mixing. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-model/src/health.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-model/src/health.rs) | runtime/library unit | Coarse-grained client health classification. | Keep this responsibility explicit: Coarse-grained client health classification. A dedicated health unit localizes dependencies, review, and regression testing inside `pcloud-model`. | The focused `pcloud-model` responsibility described as: Coarse-grained client health classification. Small serializable types and newtype IDs prevent cross-layer duplication and accidental identifier mixing. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-model/src/ids.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-model/src/ids.rs) | runtime/library unit | Raw `u64` value. Exposed as `pub` for ergonomic pattern | Keep this responsibility explicit: Raw `u64` value. Exposed as `pub` for ergonomic pattern. A dedicated ids unit localizes dependencies, review, and regression testing inside `pcloud-model`. | The focused `pcloud-model` responsibility described as: Raw `u64` value. Exposed as `pub` for ergonomic pattern. Small serializable types and newtype IDs prevent cross-layer duplication and accidental identifier mixing. This unit contains 9 indexed Rust declarations. |
| [`crates/pcloud-model/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-model/src/lib.rs) | runtime/library unit | pcloud-model | Declare and compose the `pcloud-model` crate boundary. Keep shared domain data independent from transport, storage, UI, and platform code. | The focused `pcloud-model` responsibility described as: pcloud-model. Small serializable types and newtype IDs prevent cross-layer duplication and accidental identifier mixing. This unit contains 14 indexed Rust declarations. |
| [`crates/pcloud-model/src/public_links.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-model/src/public_links.rs) | runtime/library unit | Summary row for an existing public link, as returned by | Keep this responsibility explicit: Summary row for an existing public link, as returned by. A dedicated public links unit localizes dependencies, review, and regression testing inside `pcloud-model`. | The focused `pcloud-model` responsibility described as: Summary row for an existing public link, as returned by. Small serializable types and newtype IDs prevent cross-layer duplication and accidental identifier mixing. This unit contains 10 indexed Rust declarations. |
| [`crates/pcloud-model/src/shares.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-model/src/shares.rs) | runtime/library unit | Bitwise permission flags mirroring the legacy C `PSYNC_PERM_*` | Keep this responsibility explicit: Bitwise permission flags mirroring the legacy C `PSYNC_PERM_*`. A dedicated shares unit localizes dependencies, review, and regression testing inside `pcloud-model`. | The focused `pcloud-model` responsibility described as: Bitwise permission flags mirroring the legacy C `PSYNC_PERM_*`. Small serializable types and newtype IDs prevent cross-layer duplication and accidental identifier mixing. This unit contains 20 indexed Rust declarations. |
| [`crates/pcloud-model/src/sync.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-model/src/sync.rs) | runtime/library unit | Lifecycle state of a single sync root. | Keep this responsibility explicit: Lifecycle state of a single sync root. A dedicated sync unit localizes dependencies, review, and regression testing inside `pcloud-model`. | The focused `pcloud-model` responsibility described as: Lifecycle state of a single sync root. Small serializable types and newtype IDs prevent cross-layer duplication and accidental identifier mixing. This unit contains 22 indexed Rust declarations. |
| [`crates/pcloud-model/src/transfer.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-model/src/transfer.rs) | runtime/library unit | Lifecycle state of an individual transfer task as it moves through | Keep this responsibility explicit: Lifecycle state of an individual transfer task as it moves through. A dedicated transfer unit localizes dependencies, review, and regression testing inside `pcloud-model`. | The focused `pcloud-model` responsibility described as: Lifecycle state of an individual transfer task as it moves through. Small serializable types and newtype IDs prevent cross-layer duplication and accidental identifier mixing. This unit contains 5 indexed Rust declarations. |
| [`crates/pcloud-model/tests/transfer_coverage.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-model/tests/transfer_coverage.rs) | integration test | Public transfer-model constructor coverage. | Prove the transfer coverage behavior at a crate or process boundary instead of relying only on unit tests. | Protecting transfer coverage boundary behavior from regressions. Small serializable types and newtype IDs prevent cross-layer duplication and accidental identifier mixing. This unit contains 1 indexed Rust declaration. |

All declarations: [`pcloud-model` generated crate page](../crates/pcloud-model.md).

## `pcloud-observability` (11 units)

Package context: Structured logs, audit events, health/build reports, metrics, SLOs, Prometheus export, poison-safe locks, and optional OTLP traces.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-observability/src/audit.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-observability/src/audit.rs) | runtime/library unit | Audit event envelope. | Keep this responsibility explicit: Audit event envelope. A dedicated audit unit localizes dependencies, review, and regression testing inside `pcloud-observability`. | The focused `pcloud-observability` responsibility described as: Audit event envelope. A zero-dependency metrics core and feature-gated exporters keep the default lean while preserving consistent names and redaction. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-observability/src/exporter.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-observability/src/exporter.rs) | runtime/library unit | Zero-dependency HTTP responder for Prometheus scraping. | Keep this responsibility explicit: Zero-dependency HTTP responder for Prometheus scraping. A dedicated exporter unit localizes dependencies, review, and regression testing inside `pcloud-observability`. | The focused `pcloud-observability` responsibility described as: Zero-dependency HTTP responder for Prometheus scraping. A zero-dependency metrics core and feature-gated exporters keep the default lean while preserving consistent names and redaction. This unit contains 30 indexed Rust declarations. |
| [`crates/pcloud-observability/src/health.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-observability/src/health.rs) | runtime/library unit | Build information and health-report payloads. | Keep this responsibility explicit: Build information and health-report payloads. A dedicated health unit localizes dependencies, review, and regression testing inside `pcloud-observability`. | The focused `pcloud-observability` responsibility described as: Build information and health-report payloads. A zero-dependency metrics core and feature-gated exporters keep the default lean while preserving consistent names and redaction. This unit contains 9 indexed Rust declarations. |
| [`crates/pcloud-observability/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-observability/src/lib.rs) | runtime/library unit | pcloud-observability | Declare and compose the `pcloud-observability` crate boundary. Make daemon behavior diagnosable and auditable without leaking credentials or binding the core to one telemetry vendor. | The focused `pcloud-observability` responsibility described as: pcloud-observability. A zero-dependency metrics core and feature-gated exporters keep the default lean while preserving consistent names and redaction. This unit contains 18 indexed Rust declarations. |
| [`crates/pcloud-observability/src/lock_ext.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-observability/src/lock_ext.rs) | runtime/library unit | Extension trait that turns silent `.lock().unwrap()` panics on | Keep this responsibility explicit: Extension trait that turns silent `.lock().unwrap()` panics on. A dedicated lock ext unit localizes dependencies, review, and regression testing inside `pcloud-observability`. | The focused `pcloud-observability` responsibility described as: Extension trait that turns silent `.lock().unwrap()` panics on. A zero-dependency metrics core and feature-gated exporters keep the default lean while preserving consistent names and redaction. This unit contains 20 indexed Rust declarations. |
| [`crates/pcloud-observability/src/logging.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-observability/src/logging.rs) | runtime/library unit | Structured logging surface. | Keep this responsibility explicit: Structured logging surface. A dedicated logging unit localizes dependencies, review, and regression testing inside `pcloud-observability`. | The focused `pcloud-observability` responsibility described as: Structured logging surface. A zero-dependency metrics core and feature-gated exporters keep the default lean while preserving consistent names and redaction. This unit contains 13 indexed Rust declarations. |
| [`crates/pcloud-observability/src/metrics.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-observability/src/metrics.rs) | runtime/library unit | Metric families and a self-contained Prometheus text-format exporter. | Keep this responsibility explicit: Metric families and a self-contained Prometheus text-format exporter. A dedicated metrics unit localizes dependencies, review, and regression testing inside `pcloud-observability`. | The focused `pcloud-observability` responsibility described as: Metric families and a self-contained Prometheus text-format exporter. A zero-dependency metrics core and feature-gated exporters keep the default lean while preserving consistent names and redaction. This unit contains 39 indexed Rust declarations. |
| [`crates/pcloud-observability/src/slo.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-observability/src/slo.rs) | runtime/library unit | Canonical Service-Level Objectives (SLOs) for the pcloud-rs daemon. | Keep this responsibility explicit: Canonical Service-Level Objectives (SLOs) for the pcloud-rs daemon. A dedicated slo unit localizes dependencies, review, and regression testing inside `pcloud-observability`. | The focused `pcloud-observability` responsibility described as: Canonical Service-Level Objectives (SLOs) for the pcloud-rs daemon. A zero-dependency metrics core and feature-gated exporters keep the default lean while preserving consistent names and redaction. This unit contains 65 indexed Rust declarations. |
| [`crates/pcloud-observability/src/tracing.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-observability/src/tracing.rs) | runtime/library unit | OpenTelemetry tracing integration (H13a, feature `tracing-otlp`). | Keep this responsibility explicit: OpenTelemetry tracing integration (H13a, feature `tracing-otlp`). A dedicated tracing unit localizes dependencies, review, and regression testing inside `pcloud-observability`. | The focused `pcloud-observability` responsibility described as: OpenTelemetry tracing integration (H13a, feature `tracing-otlp`). A zero-dependency metrics core and feature-gated exporters keep the default lean while preserving consistent names and redaction. This unit contains 19 indexed Rust declarations. |
| [`crates/pcloud-observability/tests/exporter_coverage.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-observability/tests/exporter_coverage.rs) | integration test | \[test\] | Prove the exporter coverage behavior at a crate or process boundary instead of relying only on unit tests. | Protecting exporter coverage boundary behavior from regressions. A zero-dependency metrics core and feature-gated exporters keep the default lean while preserving consistent names and redaction. This unit contains 2 indexed Rust declarations. |
| [`crates/pcloud-observability/tests/otlp_live_interop.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-observability/tests/otlp_live_interop.rs) | integration test | Live OTLP interop integration test (feature `tracing-otlp`). | Prove the otlp live interop behavior at a crate or process boundary instead of relying only on unit tests. | Protecting otlp live interop boundary behavior from regressions. A zero-dependency metrics core and feature-gated exporters keep the default lean while preserving consistent names and redaction. This unit contains 9 indexed Rust declarations. |

All declarations: [`pcloud-observability` generated crate page](../crates/pcloud-observability.md).

## `pcloud-p2p` (4 units)

Package context: Design exploration and compile-time/API compatibility only: the current runtime opens no mDNS socket, advertises nothing, returns no peers, and transfers no bytes.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-p2p/src/discovery.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-p2p/src/discovery.rs) | runtime/library unit | Inert discovery contract: opens no socket, advertises nothing, and always reports an empty peer list. | Keep this responsibility explicit: Inert discovery contract: opens no socket, advertises nothing, and always reports an empty peer list. A dedicated discovery unit localizes dependencies, review, and regression testing inside `pcloud-p2p`. | The focused `pcloud-p2p` responsibility described as: Inert discovery contract: opens no socket, advertises nothing, and always reports an empty peer list. The inert scaffold prevents an unfinished peer path from becoming a trusted metadata or data source and is not wired into pcloudd. This unit contains 10 indexed Rust declarations. |
| [`crates/pcloud-p2p/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-p2p/src/lib.rs) | runtime/library unit | Experimental P2P shell around an inert discovery runtime; no current peer networking, planning, or transfer path. | Declare and compose the `pcloud-p2p` crate boundary. Reserve typed policy and lifecycle seams for possible LAN-assisted transfer while preserving the cloud as authority. | The focused `pcloud-p2p` responsibility described as: Experimental P2P shell around an inert discovery runtime; no current peer networking, planning, or transfer path. The inert scaffold prevents an unfinished peer path from becoming a trusted metadata or data source and is not wired into pcloudd. This unit contains 31 indexed Rust declarations. |
| [`crates/pcloud-p2p/src/policy.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-p2p/src/policy.rs) | runtime/library unit | Global on/off policy for the P2P subsystem. | Keep this responsibility explicit: Global on/off policy for the P2P subsystem. A dedicated policy unit localizes dependencies, review, and regression testing inside `pcloud-p2p`. | The focused `pcloud-p2p` responsibility described as: Global on/off policy for the P2P subsystem. The inert scaffold prevents an unfinished peer path from becoming a trusted metadata or data source and is not wired into pcloudd. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-p2p/src/transfer.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-p2p/src/transfer.rs) | runtime/library unit | Peer-to-peer transfer tuning knobs. | Keep this responsibility explicit: Peer-to-peer transfer tuning knobs. A dedicated transfer unit localizes dependencies, review, and regression testing inside `pcloud-p2p`. | The focused `pcloud-p2p` responsibility described as: Peer-to-peer transfer tuning knobs. The inert scaffold prevents an unfinished peer path from becoming a trusted metadata or data source and is not wired into pcloudd. This unit contains 2 indexed Rust declarations. |

All declarations: [`pcloud-p2p` generated crate page](../crates/pcloud-p2p.md).

## `pcloud-plugin-api` (1 units)

Package context: Signed manifests, registry/lifecycle metadata, operation/response messages, capabilities, and audit events.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-plugin-api/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-plugin-api/src/lib.rs) | runtime/library unit | pcloud-plugin-api | Declare and compose the `pcloud-plugin-api` crate boundary. Define a capability-limited extension contract before loading third-party code. | The focused `pcloud-plugin-api` responsibility described as: pcloud-plugin-api. Ed25519 verification, explicit capability grants, size/version checks, and secret-free messages make extension authority reviewable. This unit contains 91 indexed Rust declarations. |

All declarations: [`pcloud-plugin-api` generated crate page](../crates/pcloud-plugin-api.md).

## `pcloud-plugin-autoheal` (2 units)

Package context: Checksum scans, quarantine requests, retry tracking, and full-sync pause escalation.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-plugin-autoheal/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-plugin-autoheal/src/lib.rs) | runtime/library unit | Auto-Heal Checksum Scanner Plugin | Declare and compose the `pcloud-plugin-autoheal` crate boundary. Detect local integrity drift and escalate corruption through a bounded plugin workflow. | The focused `pcloud-plugin-autoheal` responsibility described as: Auto-Heal Checksum Scanner Plugin. A small state machine separates detection from privileged remediation and caps repeated failure behavior. This unit contains 31 indexed Rust declarations. |
| [`crates/pcloud-plugin-autoheal/tests/behaviour.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-plugin-autoheal/tests/behaviour.rs) | integration test | Behavioural tests for the auto-heal plugin. All tests inject a | Prove the behaviour behavior at a crate or process boundary instead of relying only on unit tests. | Protecting behaviour boundary behavior from regressions. A small state machine separates detection from privileged remediation and caps repeated failure behavior. This unit contains 16 indexed Rust declarations. |

All declarations: [`pcloud-plugin-autoheal` generated crate page](../crates/pcloud-plugin-autoheal.md).

## `pcloud-plugin-backup-schedule` (2 units)

Package context: Cron and natural-language schedules that emit backup-cycle operations on time ticks.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-plugin-backup-schedule/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-plugin-backup-schedule/src/lib.rs) | runtime/library unit | Backup scheduler plugin. | Declare and compose the `pcloud-plugin-backup-schedule` crate boundary. Add user-controlled backup timing without embedding cron parsing into the daemon core. | The focused `pcloud-plugin-backup-schedule` responsibility described as: Backup scheduler plugin. Pure schedule parsing plus deterministic tick handling makes time behavior independently testable. This unit contains 59 indexed Rust declarations. |
| [`crates/pcloud-plugin-backup-schedule/tests/coverage_surface.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-plugin-backup-schedule/tests/coverage_surface.rs) | integration test | \[derive(Clone)\] | Prove the coverage surface behavior at a crate or process boundary instead of relying only on unit tests. | Protecting coverage surface boundary behavior from regressions. Pure schedule parsing plus deterministic tick handling makes time behavior independently testable. This unit contains 5 indexed Rust declarations. |

All declarations: [`pcloud-plugin-backup-schedule` generated crate page](../crates/pcloud-plugin-backup-schedule.md).

## `pcloud-plugin-dlp` (1 units)

Package context: Built-in regex and Shannon-entropy scanning with findings and policy decisions.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-plugin-dlp/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-plugin-dlp/src/lib.rs) | runtime/library unit | Built-in pre-upload DLP (data-loss-prevention) scanner plugin. | Declare and compose the `pcloud-plugin-dlp` crate boundary. Inspect outbound content before upload so obvious secrets can be blocked or reviewed. | The focused `pcloud-plugin-dlp` responsibility described as: Built-in pre-upload DLP (data-loss-prevention) scanner plugin. Bounded scanning, explicit rules, and pre-upload placement provide useful local detection without granting network access. This unit contains 36 indexed Rust declarations. |

All declarations: [`pcloud-plugin-dlp` generated crate page](../crates/pcloud-plugin-dlp.md).

## `pcloud-plugin-host` (1 units)

Package context: Plugin lifecycle, operation dispatch, resource limits, audit hooks, and pluggable sandbox backends.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-plugin-host/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-plugin-host/src/lib.rs) | runtime/library unit | T2.5 — sandboxed plugin host scaffold. | Declare and compose the `pcloud-plugin-host` crate boundary. Run extensions behind a capability and message boundary instead of exposing daemon memory directly. | The focused `pcloud-plugin-host` responsibility described as: T2.5 — sandboxed plugin host scaffold. The dependency-light host core separates policy from the runtime engine and defaults to denying ungranted capabilities. This unit contains 40 indexed Rust declarations. |

All declarations: [`pcloud-plugin-host` generated crate page](../crates/pcloud-plugin-host.md).

## `pcloud-plugin-publink-expiry` (2 units)

Package context: Single-user desktop notifications with persistent per-link rate limiting.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-plugin-publink-expiry/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-plugin-publink-expiry/src/lib.rs) | runtime/library unit | pcloud-plugin-publink-expiry | Declare and compose the `pcloud-plugin-publink-expiry` crate boundary. Warn users before expiring public links become an operational surprise. | The focused `pcloud-plugin-publink-expiry` responsibility described as: pcloud-plugin-publink-expiry. A clock/notifier abstraction and atomic owner-only state make behavior testable and restart-safe without auto-mutating links. This unit contains 59 indexed Rust declarations. |
| [`crates/pcloud-plugin-publink-expiry/tests/coverage_surface.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-plugin-publink-expiry/tests/coverage_surface.rs) | integration test | \[test\] | Prove the coverage surface behavior at a crate or process boundary instead of relying only on unit tests. | Protecting coverage surface boundary behavior from regressions. A clock/notifier abstraction and atomic owner-only state make behavior testable and restart-safe without auto-mutating links. This unit contains 3 indexed Rust declarations. |

All declarations: [`pcloud-plugin-publink-expiry` generated crate page](../crates/pcloud-plugin-publink-expiry.md).

## `pcloud-plugin-wasmtime` (1 units)

Package context: Experimental execution of capability-bounded plugins with fuel and memory limits.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-plugin-wasmtime/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-plugin-wasmtime/src/lib.rs) | runtime/library unit | T2.5.b — wasmtime-backed `PluginBackend` for `pcloud-plugin-host`. | Declare and compose the `pcloud-plugin-wasmtime` crate boundary. Provide a concrete WebAssembly sandbox without forcing Wasmtime into the core plugin host dependency graph. | The focused `pcloud-plugin-wasmtime` responsibility described as: T2.5.b — wasmtime-backed `PluginBackend` for `pcloud-plugin-host`. A separate backend crate contains the large runtime dependency and translates only the narrow PluginBackend contract. This unit contains 19 indexed Rust declarations. |

All declarations: [`pcloud-plugin-wasmtime` generated crate page](../crates/pcloud-plugin-wasmtime.md).

## `pcloud-policy` (1 units)

Package context: Default-deny Rego evaluation, policy bundle loading/hot reload, contextual allow/deny decisions, and null single-user policy.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-policy/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-policy/src/lib.rs) | runtime/library unit | pcloud-policy | Declare and compose the `pcloud-policy` crate boundary. Apply organization rules before sensitive operations rather than relying only on server-side administration. | The focused `pcloud-policy` responsibility described as: pcloud-policy. Fail-closed evaluation, owner-only policy files, deterministic inputs, and a null default preserve security and single-user simplicity. This unit contains 31 indexed Rust declarations. |

All declarations: [`pcloud-policy` generated crate page](../crates/pcloud-policy.md).

## `pcloud-proto` (48 units)

Package context: Auth, account, folders, transfers, diff, backup, notifications, shares, business/team, public links, Crypto, binary framing, TLS, and downloads.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-proto/benches/proto_dispatch.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/benches/proto_dispatch.rs) | benchmark | Proto transport in-process dispatch benchmarks. | Measure the proto dispatch path so performance and capacity decisions have executable evidence. | Measuring proto dispatch changes under a stable harness. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 3 indexed Rust declarations. |
| [`crates/pcloud-proto/fuzz/fuzz_targets/fuzz_auth_flow_state.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/fuzz/fuzz_targets/fuzz_auth_flow_state.rs) | fuzz target/helper | Fuzz target: model the auth flow as a state machine and feed random event | Drive malformed or adversarial inputs through the fuzz auth flow state boundary to find parser and invariant failures. | Finding malformed-input and invariant failures around fuzz auth flow state. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 4 indexed Rust declarations. |
| [`crates/pcloud-proto/fuzz/fuzz_targets/fuzz_binary_request_roundtrip.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/fuzz/fuzz_targets/fuzz_binary_request_roundtrip.rs) | fuzz target/helper | Fuzz target: construct random binary-protocol requests and ensure the | Drive malformed or adversarial inputs through the fuzz binary request roundtrip boundary to find parser and invariant failures. | Finding malformed-input and invariant failures around fuzz binary request roundtrip. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 0 indexed Rust declarations. |
| [`crates/pcloud-proto/fuzz/fuzz_targets/fuzz_ipc_method_decode.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/fuzz/fuzz_targets/fuzz_ipc_method_decode.rs) | fuzz target/helper | Fuzz target: feed arbitrary bytes into the `pcloud-ipc` frame decoder. | Drive malformed or adversarial inputs through the fuzz ipc method decode boundary to find parser and invariant failures. | Finding malformed-input and invariant failures around fuzz ipc method decode. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-proto/fuzz/fuzz_targets/fuzz_json_response.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/fuzz/fuzz_targets/fuzz_json_response.rs) | fuzz target/helper | Fuzz target: feed arbitrary bytes into the response-parsing entry point | Drive malformed or adversarial inputs through the fuzz json response boundary to find parser and invariant failures. | Finding malformed-input and invariant failures around fuzz json response. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 2 indexed Rust declarations. |
| [`crates/pcloud-proto/fuzz/fuzz_targets/fuzz_listfolder_response.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/fuzz/fuzz_targets/fuzz_listfolder_response.rs) | fuzz target/helper | Fuzz target: structured-parser fuzzing of `listfolder` responses. | Drive malformed or adversarial inputs through the fuzz listfolder response boundary to find parser and invariant failures. | Finding malformed-input and invariant failures around fuzz listfolder response. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 10 indexed Rust declarations. |
| [`crates/pcloud-proto/fuzz/fuzz_targets/fuzz_path_canonicalize.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/fuzz/fuzz_targets/fuzz_path_canonicalize.rs) | fuzz target/helper | Fuzz target: feed adversarial path strings into every proto surface that | Drive malformed or adversarial inputs through the fuzz path canonicalize boundary to find parser and invariant failures. | Finding malformed-input and invariant failures around fuzz path canonicalize. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-proto/fuzz/fuzz_targets/fuzz_response_parser.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/fuzz/fuzz_targets/fuzz_response_parser.rs) | fuzz target/helper | Fuzz target: malformed / random response frames fed to the pcloud-proto | Drive malformed or adversarial inputs through the fuzz response parser boundary to find parser and invariant failures. | Finding malformed-input and invariant failures around fuzz response parser. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 0 indexed Rust declarations. |
| [`crates/pcloud-proto/src/account_api.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/account_api.rs) | runtime/library unit | Account protocol client: typed builders and parsers for | Keep this responsibility explicit: Account protocol client: typed builders and parsers for. A dedicated account api unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Account protocol client: typed builders and parsers for. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 29 indexed Rust declarations. |
| [`crates/pcloud-proto/src/async_transfer.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/async_transfer.rs) | runtime/library unit | Async transfer protocol support: typed envelopes and helpers for | Keep this responsibility explicit: Async transfer protocol support: typed envelopes and helpers for. A dedicated async transfer unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Async transfer protocol support: typed envelopes and helpers for. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-proto/src/auth_api.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/auth_api.rs) | runtime/library unit | Auth protocol client: password and token login, TFA code / | Keep this responsibility explicit: Auth protocol client: password and token login, TFA code /. A dedicated auth api unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Auth protocol client: password and token login, TFA code /. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 54 indexed Rust declarations. |
| [`crates/pcloud-proto/src/backup_api.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/backup_api.rs) | runtime/library unit | Backup / device API wrapper. | Keep this responsibility explicit: Backup / device API wrapper. A dedicated backup api unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Backup / device API wrapper. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 20 indexed Rust declarations. |
| [`crates/pcloud-proto/src/binary_api.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/binary_api.rs) | runtime/library unit | pCloud binary protocol framer: encodes typed parameters into wire | Keep this responsibility explicit: pCloud binary protocol framer: encodes typed parameters into wire. A dedicated binary api unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: pCloud binary protocol framer: encodes typed parameters into wire. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 36 indexed Rust declarations. |
| [`crates/pcloud-proto/src/crypto_api.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/crypto_api.rs) | runtime/library unit | High-level transport shim for the crypto password-change family. | Keep this responsibility explicit: High-level transport shim for the crypto password-change family. A dedicated crypto api unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: High-level transport shim for the crypto password-change family. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 38 indexed Rust declarations. |
| [`crates/pcloud-proto/src/diff_api.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/diff_api.rs) | runtime/library unit | Diff protocol client: fetches remote changes since a cursor via the | Keep this responsibility explicit: Diff protocol client: fetches remote changes since a cursor via the. A dedicated diff api unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Diff protocol client: fetches remote changes since a cursor via the. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 48 indexed Rust declarations. |
| [`crates/pcloud-proto/src/folder_api.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/folder_api.rs) | runtime/library unit | Folder protocol client: listing, metadata, create/rename/delete, and | Keep this responsibility explicit: Folder protocol client: listing, metadata, create/rename/delete, and. A dedicated folder api unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Folder protocol client: listing, metadata, create/rename/delete, and. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 51 indexed Rust declarations. |
| [`crates/pcloud-proto/src/http_download.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/http_download.rs) | runtime/library unit | Signed-HTTP download executor: resolves file-link URLs from the | Keep this responsibility explicit: Signed-HTTP download executor: resolves file-link URLs from the. A dedicated http download unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Signed-HTTP download executor: resolves file-link URLs from the. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 51 indexed Rust declarations. |
| [`crates/pcloud-proto/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/lib.rs) | runtime/library unit | pcloud-proto | Declare and compose the `pcloud-proto` crate boundary. Represent pCloud wire operations as typed clients and builders rather than stringly-typed calls throughout the product. | The focused `pcloud-proto` responsibility described as: pcloud-proto. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 23 indexed Rust declarations. |
| [`crates/pcloud-proto/src/methods/account.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/methods/account.rs) | runtime/library unit | Wire-level method builders for the account family (verify/change | Keep this responsibility explicit: Wire-level method builders for the account family (verify/change. A dedicated account unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Wire-level method builders for the account family (verify/change. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 35 indexed Rust declarations. |
| [`crates/pcloud-proto/src/methods/auth.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/methods/auth.rs) | runtime/library unit | Wire-level method builders for auth (login, TFA, userinfo, | Keep this responsibility explicit: Wire-level method builders for auth (login, TFA, userinfo,. A dedicated auth unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Wire-level method builders for auth (login, TFA, userinfo,. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 42 indexed Rust declarations. |
| [`crates/pcloud-proto/src/methods/backup.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/methods/backup.rs) | runtime/library unit | Backup/device method request types. | Keep this responsibility explicit: Backup/device method request types. A dedicated backup unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Backup/device method request types. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 19 indexed Rust declarations. |
| [`crates/pcloud-proto/src/methods/crypto.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/methods/crypto.rs) | runtime/library unit | Binary protocol DTOs for the crypto-password-change family. | Keep this responsibility explicit: Binary protocol DTOs for the crypto-password-change family. A dedicated crypto unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Binary protocol DTOs for the crypto-password-change family. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 56 indexed Rust declarations. |
| [`crates/pcloud-proto/src/methods/diff.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/methods/diff.rs) | runtime/library unit | Wire-level method builders for the `diff` long-poll stream used by | Keep this responsibility explicit: Wire-level method builders for the `diff` long-poll stream used by. A dedicated diff unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Wire-level method builders for the `diff` long-poll stream used by. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 5 indexed Rust declarations. |
| [`crates/pcloud-proto/src/methods/download.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/methods/download.rs) | runtime/library unit | Wire-level method builders for download (getfilelink, thumb, | Keep this responsibility explicit: Wire-level method builders for download (getfilelink, thumb,. A dedicated download unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Wire-level method builders for download (getfilelink, thumb,. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 5 indexed Rust declarations. |
| [`crates/pcloud-proto/src/methods/folder.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/methods/folder.rs) | runtime/library unit | Wire-level method builders for folder operations (list, create, | Keep this responsibility explicit: Wire-level method builders for folder operations (list, create,. A dedicated folder unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Wire-level method builders for folder operations (list, create,. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 47 indexed Rust declarations. |
| [`crates/pcloud-proto/src/methods/mod.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/methods/mod.rs) | runtime/library unit | Per-method builders grouped by subsystem (account, auth, backup, | Keep this responsibility explicit: Per-method builders grouped by subsystem (account, auth, backup,. A dedicated mod unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Per-method builders grouped by subsystem (account, auth, backup,. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 24 indexed Rust declarations. |
| [`crates/pcloud-proto/src/methods/notifications.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/methods/notifications.rs) | runtime/library unit | Notifications method request types. | Keep this responsibility explicit: Notifications method request types. A dedicated notifications unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Notifications method request types. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 14 indexed Rust declarations. |
| [`crates/pcloud-proto/src/methods/public_links.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/methods/public_links.rs) | runtime/library unit | Wire-level method builders for public-link operations (file/folder | Keep this responsibility explicit: Wire-level method builders for public-link operations (file/folder. A dedicated public links unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Wire-level method builders for public-link operations (file/folder. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 66 indexed Rust declarations. |
| [`crates/pcloud-proto/src/methods/shares.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/methods/shares.rs) | runtime/library unit | Wire-level method builders for share operations (list, add, remove, | Keep this responsibility explicit: Wire-level method builders for share operations (list, add, remove,. A dedicated shares unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Wire-level method builders for share operations (list, add, remove,. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 45 indexed Rust declarations. |
| [`crates/pcloud-proto/src/methods/upload.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/methods/upload.rs) | runtime/library unit | Upload wire methods. C citations use `pclsync/…:&lt;line&gt;` and are verified | Keep this responsibility explicit: Upload wire methods. C citations use `pclsync/…:&lt;line&gt;` and are verified. A dedicated upload unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Upload wire methods. C citations use `pclsync/…:&lt;line&gt;` and are verified. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 91 indexed Rust declarations. |
| [`crates/pcloud-proto/src/notifications_api.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/notifications_api.rs) | runtime/library unit | Notifications API wrapper. | Keep this responsibility explicit: Notifications API wrapper. A dedicated notifications api unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Notifications API wrapper. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 21 indexed Rust declarations. |
| [`crates/pcloud-proto/src/parallel_download.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/parallel_download.rs) | runtime/library unit | T2.2 — multi-range HTTP download planner. | Keep this responsibility explicit: T2.2 — multi-range HTTP download planner. A dedicated parallel download unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: T2.2 — multi-range HTTP download planner. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 19 indexed Rust declarations. |
| [`crates/pcloud-proto/src/public_links_api.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/public_links_api.rs) | runtime/library unit | Public-links protocol client: file/folder link CRUD, `changepublink` | Keep this responsibility explicit: Public-links protocol client: file/folder link CRUD, `changepublink`. A dedicated public links api unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Public-links protocol client: file/folder link CRUD, `changepublink`. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 71 indexed Rust declarations. |
| [`crates/pcloud-proto/src/redacted.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/redacted.rs) | runtime/library unit | Secret-bearing wire-type wrapper for proto method structs. | Keep this responsibility explicit: Secret-bearing wire-type wrapper for proto method structs. A dedicated redacted unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Secret-bearing wire-type wrapper for proto method structs. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 21 indexed Rust declarations. |
| [`crates/pcloud-proto/src/resilient_transport.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/resilient_transport.rs) | runtime/library unit | Resilient wrapper over any \[`ProtocolTransport`\]. | Keep this responsibility explicit: Resilient wrapper over any \[`ProtocolTransport`\]. A dedicated resilient transport unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Resilient wrapper over any \[`ProtocolTransport`\]. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 44 indexed Rust declarations. |
| [`crates/pcloud-proto/src/response.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/response.rs) | runtime/library unit | Response envelope and parsing primitives shared by every API module | Keep this responsibility explicit: Response envelope and parsing primitives shared by every API module. A dedicated response unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Response envelope and parsing primitives shared by every API module. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 46 indexed Rust declarations. |
| [`crates/pcloud-proto/src/shares_api.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/shares_api.rs) | runtime/library unit | Shares / business / teams protocol client: share request list, share | Keep this responsibility explicit: Shares / business / teams protocol client: share request list, share. A dedicated shares api unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Shares / business / teams protocol client: share request list, share. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 25 indexed Rust declarations. |
| [`crates/pcloud-proto/src/sync_api.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/sync_api.rs) | runtime/library unit | Sync protocol client: add/list/remove remote sync roots, syncability | Keep this responsibility explicit: Sync protocol client: add/list/remove remote sync roots, syncability. A dedicated sync api unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Sync protocol client: add/list/remove remote sync roots, syncability. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 20 indexed Rust declarations. |
| [`crates/pcloud-proto/src/tls.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/tls.rs) | runtime/library unit | Shared rustls \[`ClientConfig`\] for all pCloud TLS clients. | Keep this responsibility explicit: Shared rustls \[`ClientConfig`\] for all pCloud TLS clients. A dedicated tls unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Shared rustls \[`ClientConfig`\] for all pCloud TLS clients. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 8 indexed Rust declarations. |
| [`crates/pcloud-proto/src/transfer_api.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/transfer_api.rs) | runtime/library unit | Transfer protocol client: `getfilelink`, `upload_create`, | Keep this responsibility explicit: Transfer protocol client: `getfilelink`, `upload_create`,. A dedicated transfer api unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Transfer protocol client: `getfilelink`, `upload_create`,. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 52 indexed Rust declarations. |
| [`crates/pcloud-proto/src/transport.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/src/transport.rs) | runtime/library unit | Transport traits and type shims over `pcloud-transport`: the typed | Keep this responsibility explicit: Transport traits and type shims over `pcloud-transport`: the typed. A dedicated transport unit localizes dependencies, review, and regression testing inside `pcloud-proto`. | The focused `pcloud-proto` responsibility described as: Transport traits and type shims over `pcloud-transport`: the typed. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 37 indexed Rust declarations. |
| [`crates/pcloud-proto/tests/folder_api_coverage.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/tests/folder_api_coverage.rs) | integration test | Public folder API coverage for date parsing and server-hint variants. | Prove the folder api coverage behavior at a crate or process boundary instead of relying only on unit tests. | Protecting folder api coverage boundary behavior from regressions. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 9 indexed Rust declarations. |
| [`crates/pcloud-proto/tests/http_download_integrity.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/tests/http_download_integrity.rs) | integration test | Integration tests covering the optional SHA-256 integrity | Prove the http download integrity behavior at a crate or process boundary instead of relying only on unit tests. | Protecting http download integrity boundary behavior from regressions. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 22 indexed Rust declarations. |
| [`crates/pcloud-proto/tests/proptest_framer.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/tests/proptest_framer.rs) | integration test | Property tests for the pcloud-proto binary framer. | Prove the proptest framer behavior at a crate or process boundary instead of relying only on unit tests. | Protecting proptest framer boundary behavior from regressions. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 9 indexed Rust declarations. |
| [`crates/pcloud-proto/tests/proptest_response_and_frames.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/tests/proptest_response_and_frames.rs) | integration test | Property tests for the pcloud-proto binary response parser and | Prove the proptest response and frames behavior at a crate or process boundary instead of relying only on unit tests. | Protecting proptest response and frames boundary behavior from regressions. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 8 indexed Rust declarations. |
| [`crates/pcloud-proto/tests/smoke_fuzz_arbitrary.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/tests/smoke_fuzz_arbitrary.rs) | integration test | CI-friendly smoke fuzzing driven by the `arbitrary` crate. | Prove the smoke fuzz arbitrary behavior at a crate or process boundary instead of relying only on unit tests. | Protecting smoke fuzz arbitrary boundary behavior from regressions. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 7 indexed Rust declarations. |
| [`crates/pcloud-proto/tests/transfer_and_public_link_roundtrip.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/tests/transfer_and_public_link_roundtrip.rs) | integration test | Integration tests for pcloud-proto transfer and public-link round-trips. | Prove the transfer and public link roundtrip behavior at a crate or process boundary instead of relying only on unit tests. | Protecting transfer and public link roundtrip boundary behavior from regressions. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 21 indexed Rust declarations. |
| [`crates/pcloud-proto/tests/value_coverage.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-proto/tests/value_coverage.rs) | integration test | Public value-wrapper and response-parser edge coverage. | Prove the value coverage behavior at a crate or process boundary instead of relying only on unit tests. | Protecting value coverage boundary behavior from regressions. Typed DTOs, bounded parsers, shared TLS, redacted secrets, resilient wrappers, and synchronous/async transfer helpers isolate protocol risk. This unit contains 3 indexed Rust declarations. |

All declarations: [`pcloud-proto` generated crate page](../crates/pcloud-proto.md).

## `pcloud-resilience` (13 units)

Package context: Retry/backoff, global retry budgets, circuit breakers, rate limits, pacing, metered-network awareness, and optional async timeouts.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-resilience/src/circuit_breaker.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-resilience/src/circuit_breaker.rs) | runtime/library unit | Three-state circuit breaker. | Keep this responsibility explicit: Three-state circuit breaker. A dedicated circuit breaker unit localizes dependencies, review, and regression testing inside `pcloud-resilience`. | The focused `pcloud-resilience` responsibility described as: Three-state circuit breaker. Injectable clocks, explicit state machines, token budgets, jitter, and metrics make failure policy observable and testable. This unit contains 32 indexed Rust declarations. |
| [`crates/pcloud-resilience/src/clock.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-resilience/src/clock.rs) | runtime/library unit | Injectable monotonic clock abstraction. | Keep this responsibility explicit: Injectable monotonic clock abstraction. A dedicated clock unit localizes dependencies, review, and regression testing inside `pcloud-resilience`. | The focused `pcloud-resilience` responsibility described as: Injectable monotonic clock abstraction. Injectable clocks, explicit state machines, token budgets, jitter, and metrics make failure policy observable and testable. This unit contains 15 indexed Rust declarations. |
| [`crates/pcloud-resilience/src/global_budget.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-resilience/src/global_budget.rs) | runtime/library unit | Global retry budget — shared token pool that limits retries across all | Keep this responsibility explicit: Global retry budget — shared token pool that limits retries across all. A dedicated global budget unit localizes dependencies, review, and regression testing inside `pcloud-resilience`. | The focused `pcloud-resilience` responsibility described as: Global retry budget — shared token pool that limits retries across all. Injectable clocks, explicit state machines, token budgets, jitter, and metrics make failure policy observable and testable. This unit contains 15 indexed Rust declarations. |
| [`crates/pcloud-resilience/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-resilience/src/lib.rs) | runtime/library unit | pcloud-resilience | Declare and compose the `pcloud-resilience` crate boundary. Keep transient faults from becoming retry storms, indefinite hangs, or cascading outages. | The focused `pcloud-resilience` responsibility described as: pcloud-resilience. Injectable clocks, explicit state machines, token budgets, jitter, and metrics make failure policy observable and testable. This unit contains 9 indexed Rust declarations. |
| [`crates/pcloud-resilience/src/metered.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-resilience/src/metered.rs) | runtime/library unit | Best-effort metered-network detection using only the standard library. | Keep this responsibility explicit: Best-effort metered-network detection using only the standard library. A dedicated metered unit localizes dependencies, review, and regression testing inside `pcloud-resilience`. | The focused `pcloud-resilience` responsibility described as: Best-effort metered-network detection using only the standard library. Injectable clocks, explicit state machines, token budgets, jitter, and metrics make failure policy observable and testable. This unit contains 9 indexed Rust declarations. |
| [`crates/pcloud-resilience/src/pacing.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-resilience/src/pacing.rs) | runtime/library unit | Bandwidth pacing via a simple token bucket. | Keep this responsibility explicit: Bandwidth pacing via a simple token bucket. A dedicated pacing unit localizes dependencies, review, and regression testing inside `pcloud-resilience`. | The focused `pcloud-resilience` responsibility described as: Bandwidth pacing via a simple token bucket. Injectable clocks, explicit state machines, token budgets, jitter, and metrics make failure policy observable and testable. This unit contains 17 indexed Rust declarations. |
| [`crates/pcloud-resilience/src/rate_limit.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-resilience/src/rate_limit.rs) | runtime/library unit | Token-bucket rate limiter. | Keep this responsibility explicit: Token-bucket rate limiter. A dedicated rate limit unit localizes dependencies, review, and regression testing inside `pcloud-resilience`. | The focused `pcloud-resilience` responsibility described as: Token-bucket rate limiter. Injectable clocks, explicit state machines, token budgets, jitter, and metrics make failure policy observable and testable. This unit contains 25 indexed Rust declarations. |
| [`crates/pcloud-resilience/src/retry.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-resilience/src/retry.rs) | runtime/library unit | Retry policy with pluggable backoff schedules. | Keep this responsibility explicit: Retry policy with pluggable backoff schedules. A dedicated retry unit localizes dependencies, review, and regression testing inside `pcloud-resilience`. | The focused `pcloud-resilience` responsibility described as: Retry policy with pluggable backoff schedules. Injectable clocks, explicit state machines, token budgets, jitter, and metrics make failure policy observable and testable. This unit contains 30 indexed Rust declarations. |
| [`crates/pcloud-resilience/src/timeout.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-resilience/src/timeout.rs) | runtime/library unit | Tokio-backed, cancellation-safe timeout helper. | Keep this responsibility explicit: Tokio-backed, cancellation-safe timeout helper. A dedicated timeout unit localizes dependencies, review, and regression testing inside `pcloud-resilience`. | The focused `pcloud-resilience` responsibility described as: Tokio-backed, cancellation-safe timeout helper. Injectable clocks, explicit state machines, token budgets, jitter, and metrics make failure policy observable and testable. This unit contains 8 indexed Rust declarations. |
| [`crates/pcloud-resilience/src/transport.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-resilience/src/transport.rs) | runtime/library unit | `ResilientTransport` — retry-aware HTTP request executor. | Keep this responsibility explicit: `ResilientTransport` — retry-aware HTTP request executor. A dedicated transport unit localizes dependencies, review, and regression testing inside `pcloud-resilience`. | The focused `pcloud-resilience` responsibility described as: `ResilientTransport` — retry-aware HTTP request executor. Injectable clocks, explicit state machines, token budgets, jitter, and metrics make failure policy observable and testable. This unit contains 75 indexed Rust declarations. |
| [`crates/pcloud-resilience/tests/circuit_breaker_proptest.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-resilience/tests/circuit_breaker_proptest.rs) | integration test | Property tests for `CircuitBreaker`: drive a random event stream | Prove the circuit breaker proptest behavior at a crate or process boundary instead of relying only on unit tests. | Protecting circuit breaker proptest boundary behavior from regressions. Injectable clocks, explicit state machines, token budgets, jitter, and metrics make failure policy observable and testable. This unit contains 3 indexed Rust declarations. |
| [`crates/pcloud-resilience/tests/retry_coverage.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-resilience/tests/retry_coverage.rs) | integration test | Public retry-policy edge coverage. | Prove the retry coverage behavior at a crate or process boundary instead of relying only on unit tests. | Protecting retry coverage boundary behavior from regressions. Injectable clocks, explicit state machines, token budgets, jitter, and metrics make failure policy observable and testable. This unit contains 2 indexed Rust declarations. |
| [`crates/pcloud-resilience/tests/transport_coverage.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-resilience/tests/transport_coverage.rs) | integration test | \[test\] | Prove the transport coverage behavior at a crate or process boundary instead of relying only on unit tests. | Protecting transport coverage boundary behavior from regressions. Injectable clocks, explicit state machines, token budgets, jitter, and metrics make failure policy observable and testable. This unit contains 5 indexed Rust declarations. |

All declarations: [`pcloud-resilience` generated crate page](../crates/pcloud-resilience.md).

## `pcloud-rsync` (4 units)

Package context: Rolling weak hashes, strong block signatures, delta planning, and differential-upload strategy inputs.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-rsync/src/delta.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-rsync/src/delta.rs) | runtime/library unit | Delta encoder: walk a local file against a remote | Keep this responsibility explicit: Delta encoder: walk a local file against a remote. A dedicated delta unit localizes dependencies, review, and regression testing inside `pcloud-rsync`. | The focused `pcloud-rsync` responsibility described as: Delta encoder: walk a local file against a remote. Rsync-style weak/strong matching finds reusable blocks in one pass while strong hashes protect against weak-hash collisions. This unit contains 17 indexed Rust declarations. |
| [`crates/pcloud-rsync/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-rsync/src/lib.rs) | runtime/library unit | T2.1 — block signatures + rolling-hash primitives for | Declare and compose the `pcloud-rsync` crate boundary. Avoid retransmitting unchanged blocks when a large file changes locally. | The focused `pcloud-rsync` responsibility described as: T2.1 — block signatures + rolling-hash primitives for. Rsync-style weak/strong matching finds reusable blocks in one pass while strong hashes protect against weak-hash collisions. This unit contains 3 indexed Rust declarations. |
| [`crates/pcloud-rsync/src/rolling.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-rsync/src/rolling.rs) | runtime/library unit | Adler-32-style rolling hash. | Keep this responsibility explicit: Adler-32-style rolling hash. A dedicated rolling unit localizes dependencies, review, and regression testing inside `pcloud-rsync`. | The focused `pcloud-rsync` responsibility described as: Adler-32-style rolling hash. Rsync-style weak/strong matching finds reusable blocks in one pass while strong hashes protect against weak-hash collisions. This unit contains 18 indexed Rust declarations. |
| [`crates/pcloud-rsync/src/signature.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-rsync/src/signature.rs) | runtime/library unit | Block-signature builder for differential sync. | Keep this responsibility explicit: Block-signature builder for differential sync. A dedicated signature unit localizes dependencies, review, and regression testing inside `pcloud-rsync`. | The focused `pcloud-rsync` responsibility described as: Block-signature builder for differential sync. Rsync-style weak/strong matching finds reusable blocks in one pass while strong hashes protect against weak-hash collisions. This unit contains 20 indexed Rust declarations. |

All declarations: [`pcloud-rsync` generated crate page](../crates/pcloud-rsync.md).

## `pcloud-sdk` (1 units)

Package context: Blocking RemoteDrive operations for listing, metadata, directories, copy/move, upload/download, delete, public links, and sharing through pcloudd.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-sdk-public/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-sdk-public/src/lib.rs) | runtime/library unit | Stable, filesystem-focused pCloud SDK. | Declare and compose the `pcloud-sdk` crate boundary. Give third-party Rust programs a small stable API without exposing daemon internals or credential handling. | The focused `pcloud-sdk` responsibility described as: Stable, filesystem-focused pCloud SDK. SDK-owned SemVer types and authenticated local IPC keep the public contract narrow and reuse the daemon's policy, durability, and security. This unit contains 52 indexed Rust declarations. |

All declarations: [`pcloud-sdk` generated crate page](../crates/pcloud-sdk.md).

## `pcloud-secret` (9 units)

Package context: Passwords, tokens, private keys, PINs, and binary secret buffers across all crates.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-secret/benches/secret_ct_eq.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-secret/benches/secret_ct_eq.rs) | benchmark | Constant-time equality micro-benchmarks for `SecretString`. | Measure the secret ct eq path so performance and capacity decisions have executable evidence. | Measuring secret ct eq changes under a stable harness. Redacted Debug, no Serialize, zeroize-on-drop, explicit exposure, and constant-time equality force deliberate secret handling. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-secret/examples/roundtrip.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-secret/examples/roundtrip.rs) | example | Demonstrates a `SecretString` round-trip: construct, expose, audit-visible | Show a runnable roundtrip workflow and keep the intended call sequence discoverable. | Teaching and smoke-testing the roundtrip call path. Redacted Debug, no Serialize, zeroize-on-drop, explicit exposure, and constant-time equality force deliberate secret handling. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-secret/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-secret/src/lib.rs) | runtime/library unit | pcloud-secret | Declare and compose the `pcloud-secret` crate boundary. Make accidental logging, cloning, serialization, comparison leakage, and residual memory less likely for credentials and key material. | The focused `pcloud-secret` responsibility described as: pcloud-secret. Redacted Debug, no Serialize, zeroize-on-drop, explicit exposure, and constant-time equality force deliberate secret handling. This unit contains 8 indexed Rust declarations. |
| [`crates/pcloud-secret/src/redact.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-secret/src/redact.rs) | runtime/library unit | Format a `key=&lt;redacted&gt;` token for inclusion in log lines. | Keep this responsibility explicit: Format a `key=&lt;redacted&gt;` token for inclusion in log lines. A dedicated redact unit localizes dependencies, review, and regression testing inside `pcloud-secret`. | The focused `pcloud-secret` responsibility described as: Format a `key=&lt;redacted&gt;` token for inclusion in log lines. Redacted Debug, no Serialize, zeroize-on-drop, explicit exposure, and constant-time equality force deliberate secret handling. This unit contains 1 indexed Rust declaration. |
| [`crates/pcloud-secret/src/secret_bytes.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-secret/src/secret_bytes.rs) | runtime/library unit | `SecretBytes` — an audit-hardened wrapper around a heap-allocated binary | Keep this responsibility explicit: `SecretBytes` — an audit-hardened wrapper around a heap-allocated binary. A dedicated secret bytes unit localizes dependencies, review, and regression testing inside `pcloud-secret`. | The focused `pcloud-secret` responsibility described as: `SecretBytes` — an audit-hardened wrapper around a heap-allocated binary. Redacted Debug, no Serialize, zeroize-on-drop, explicit exposure, and constant-time equality force deliberate secret handling. This unit contains 9 indexed Rust declarations. |
| [`crates/pcloud-secret/src/secret_string.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-secret/src/secret_string.rs) | runtime/library unit | `SecretString` — an audit-hardened wrapper around a heap-allocated UTF-8 | Keep this responsibility explicit: `SecretString` — an audit-hardened wrapper around a heap-allocated UTF-8. A dedicated secret string unit localizes dependencies, review, and regression testing inside `pcloud-secret`. | The focused `pcloud-secret` responsibility described as: `SecretString` — an audit-hardened wrapper around a heap-allocated UTF-8. Redacted Debug, no Serialize, zeroize-on-drop, explicit exposure, and constant-time equality force deliberate secret handling. This unit contains 9 indexed Rust declarations. |
| [`crates/pcloud-secret/tests/proptest_zeroize_invariants.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-secret/tests/proptest_zeroize_invariants.rs) | integration test | Property tests for `SecretBytes` / `SecretString` zeroize invariants. | Prove the proptest zeroize invariants behavior at a crate or process boundary instead of relying only on unit tests. | Protecting proptest zeroize invariants boundary behavior from regressions. Redacted Debug, no Serialize, zeroize-on-drop, explicit exposure, and constant-time equality force deliberate secret handling. This unit contains 8 indexed Rust declarations. |
| [`crates/pcloud-secret/tests/redaction_and_zeroize.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-secret/tests/redaction_and_zeroize.rs) | integration test | Integration tests for pcloud-secret wrappers. | Prove the redaction and zeroize behavior at a crate or process boundary instead of relying only on unit tests. | Protecting redaction and zeroize boundary behavior from regressions. Redacted Debug, no Serialize, zeroize-on-drop, explicit exposure, and constant-time equality force deliberate secret handling. This unit contains 13 indexed Rust declarations. |
| [`crates/pcloud-secret/tests/serialize_is_forbidden.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-secret/tests/serialize_is_forbidden.rs) | integration test | Enforces that `SecretString` / `SecretBytes` do NOT implement any | Prove the serialize is forbidden behavior at a crate or process boundary instead of relying only on unit tests. | Protecting serialize is forbidden boundary behavior from regressions. Redacted Debug, no Serialize, zeroize-on-drop, explicit exposure, and constant-time equality force deliberate secret handling. This unit contains 4 indexed Rust declarations. |

All declarations: [`pcloud-secret` generated crate page](../crates/pcloud-secret.md).

## `pcloud-session` (3 units)

Package context: Refresh loops, session lifecycle glue, persisted-token synchronization, and authentication expiry handling.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-session/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-session/src/lib.rs) | runtime/library unit | pcloud-session | Declare and compose the `pcloud-session` crate boundary. Keep daemon session refresh and vault synchronization separate from both protocol mechanics and process dispatch. | The focused `pcloud-session` responsibility described as: pcloud-session. A narrow lifecycle layer coordinates timers and vault actions while reusing the typed auth state machine. This unit contains 2 indexed Rust declarations. |
| [`crates/pcloud-session/src/refresh_loop.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-session/src/refresh_loop.rs) | runtime/library unit | Session refresh loop (sub-task 3). | Keep this responsibility explicit: Session refresh loop (sub-task 3). A dedicated refresh loop unit localizes dependencies, review, and regression testing inside `pcloud-session`. | The focused `pcloud-session` responsibility described as: Session refresh loop (sub-task 3). A narrow lifecycle layer coordinates timers and vault actions while reusing the typed auth state machine. This unit contains 12 indexed Rust declarations. |
| [`crates/pcloud-session/src/session_lifecycle.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-session/src/session_lifecycle.rs) | runtime/library unit | Daemon-side glue for session lifecycle management. | Keep this responsibility explicit: Daemon-side glue for session lifecycle management. A dedicated session lifecycle unit localizes dependencies, review, and regression testing inside `pcloud-session`. | The focused `pcloud-session` responsibility described as: Daemon-side glue for session lifecycle management. A narrow lifecycle layer coordinates timers and vault actions while reusing the typed auth state machine. This unit contains 17 indexed Rust declarations. |

All declarations: [`pcloud-session` generated crate page](../crates/pcloud-session.md).

## `pcloud-store` (18 units)

Package context: SQLite schema/migrations, settings, preferences, sync graph, diff cursors, metadata, upload resume, account state, audit chain, and typed values.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-store/benches/store_kv.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-store/benches/store_kv.rs) | benchmark | SQLite-backed KV throughput benchmarks. | Measure the store kv path so performance and capacity decisions have executable evidence. | Measuring store kv changes under a stable harness. Transactions, versioned migrations, busy retries, HMAC-indexed sensitive keys, integrity checks, and repository boundaries protect consistency. This unit contains 4 indexed Rust declarations. |
| [`crates/pcloud-store/src/integrity.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-store/src/integrity.rs) | runtime/library unit | Outcome of a startup/integrity probe against the backing SQLite store. | Keep this responsibility explicit: Outcome of a startup/integrity probe against the backing SQLite store. A dedicated integrity unit localizes dependencies, review, and regression testing inside `pcloud-store`. | The focused `pcloud-store` responsibility described as: Outcome of a startup/integrity probe against the backing SQLite store. Transactions, versioned migrations, busy retries, HMAC-indexed sensitive keys, integrity checks, and repository boundaries protect consistency. This unit contains 3 indexed Rust declarations. |
| [`crates/pcloud-store/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-store/src/lib.rs) | runtime/library unit | pcloud-store | Declare and compose the `pcloud-store` crate boundary. Persist durable local state transactionally across crashes and upgrades. | The focused `pcloud-store` responsibility described as: pcloud-store. Transactions, versioned migrations, busy retries, HMAC-indexed sensitive keys, integrity checks, and repository boundaries protect consistency. This unit contains 86 indexed Rust declarations. |
| [`crates/pcloud-store/src/migrations.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-store/src/migrations.rs) | runtime/library unit | Forward-only migration plan to bring the store up to a target schema version. | Keep this responsibility explicit: Forward-only migration plan to bring the store up to a target schema version. A dedicated migrations unit localizes dependencies, review, and regression testing inside `pcloud-store`. | The focused `pcloud-store` responsibility described as: Forward-only migration plan to bring the store up to a target schema version. Transactions, versioned migrations, busy retries, HMAC-indexed sensitive keys, integrity checks, and repository boundaries protect consistency. This unit contains 4 indexed Rust declarations. |
| [`crates/pcloud-store/src/repositories/account.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-store/src/repositories/account.rs) | runtime/library unit | Primary account row read from / written to the `account` table. | Keep this responsibility explicit: Primary account row read from / written to the `account` table. A dedicated account unit localizes dependencies, review, and regression testing inside `pcloud-store`. | The focused `pcloud-store` responsibility described as: Primary account row read from / written to the `account` table. Transactions, versioned migrations, busy retries, HMAC-indexed sensitive keys, integrity checks, and repository boundaries protect consistency. This unit contains 4 indexed Rust declarations. |
| [`crates/pcloud-store/src/repositories/audit.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-store/src/repositories/audit.rs) | runtime/library unit | Tamper-evident audit event repository. | Keep this responsibility explicit: Tamper-evident audit event repository. A dedicated audit unit localizes dependencies, review, and regression testing inside `pcloud-store`. | The focused `pcloud-store` responsibility described as: Tamper-evident audit event repository. Transactions, versioned migrations, busy retries, HMAC-indexed sensitive keys, integrity checks, and repository boundaries protect consistency. This unit contains 28 indexed Rust declarations. |
| [`crates/pcloud-store/src/repositories/diff_state.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-store/src/repositories/diff_state.rs) | runtime/library unit | Per-sync-root diff cursor state (`sync_diff_state` table, schema v10). | Keep this responsibility explicit: Per-sync-root diff cursor state (`sync_diff_state` table, schema v10). A dedicated diff state unit localizes dependencies, review, and regression testing inside `pcloud-store`. | The focused `pcloud-store` responsibility described as: Per-sync-root diff cursor state (`sync_diff_state` table, schema v10). Transactions, versioned migrations, busy retries, HMAC-indexed sensitive keys, integrity checks, and repository boundaries protect consistency. This unit contains 8 indexed Rust declarations. |
| [`crates/pcloud-store/src/repositories/file_metadata.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-store/src/repositories/file_metadata.rs) | runtime/library unit | Local file/folder metadata cache (`file_metadata` table, schema v11). | Keep this responsibility explicit: Local file/folder metadata cache (`file_metadata` table, schema v11). A dedicated file metadata unit localizes dependencies, review, and regression testing inside `pcloud-store`. | The focused `pcloud-store` responsibility described as: Local file/folder metadata cache (`file_metadata` table, schema v11). Transactions, versioned migrations, busy retries, HMAC-indexed sensitive keys, integrity checks, and repository boundaries protect consistency. This unit contains 22 indexed Rust declarations. |
| [`crates/pcloud-store/src/repositories/mod.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-store/src/repositories/mod.rs) | runtime/library unit | Single-row primary account repository (`account` table). | Keep this responsibility explicit: Single-row primary account repository (`account` table). A dedicated mod unit localizes dependencies, review, and regression testing inside `pcloud-store`. | The focused `pcloud-store` responsibility described as: Single-row primary account repository (`account` table). Transactions, versioned migrations, busy retries, HMAC-indexed sensitive keys, integrity checks, and repository boundaries protect consistency. This unit contains 13 indexed Rust declarations. |
| [`crates/pcloud-store/src/repositories/preferences.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-store/src/repositories/preferences.rs) | runtime/library unit | Remote folder id for the per-device backup root. Mirrors the legacy | Keep this responsibility explicit: Remote folder id for the per-device backup root. Mirrors the legacy. A dedicated preferences unit localizes dependencies, review, and regression testing inside `pcloud-store`. | The focused `pcloud-store` responsibility described as: Remote folder id for the per-device backup root. Mirrors the legacy. Transactions, versioned migrations, busy retries, HMAC-indexed sensitive keys, integrity checks, and repository boundaries protect consistency. This unit contains 7 indexed Rust declarations. |
| [`crates/pcloud-store/src/repositories/settings.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-store/src/repositories/settings.rs) | runtime/library unit | Typed settings helpers mirroring the C | Keep this responsibility explicit: Typed settings helpers mirroring the C. A dedicated settings unit localizes dependencies, review, and regression testing inside `pcloud-store`. | The focused `pcloud-store` responsibility described as: Typed settings helpers mirroring the C. Transactions, versioned migrations, busy retries, HMAC-indexed sensitive keys, integrity checks, and repository boundaries protect consistency. This unit contains 25 indexed Rust declarations. |
| [`crates/pcloud-store/src/repositories/sync_graph.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-store/src/repositories/sync_graph.rs) | runtime/library unit | One row of the `sync_root_records` table: a persisted sync-root definition. | Keep this responsibility explicit: One row of the `sync_root_records` table: a persisted sync-root definition. A dedicated sync graph unit localizes dependencies, review, and regression testing inside `pcloud-store`. | The focused `pcloud-store` responsibility described as: One row of the `sync_root_records` table: a persisted sync-root definition. Transactions, versioned migrations, busy retries, HMAC-indexed sensitive keys, integrity checks, and repository boundaries protect consistency. This unit contains 4 indexed Rust declarations. |
| [`crates/pcloud-store/src/repositories/upload_resume.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-store/src/repositories/upload_resume.rs) | runtime/library unit | Persisted state for in-flight chunked uploads. | Keep this responsibility explicit: Persisted state for in-flight chunked uploads. A dedicated upload resume unit localizes dependencies, review, and regression testing inside `pcloud-store`. | The focused `pcloud-store` responsibility described as: Persisted state for in-flight chunked uploads. Transactions, versioned migrations, busy retries, HMAC-indexed sensitive keys, integrity checks, and repository boundaries protect consistency. This unit contains 16 indexed Rust declarations. |
| [`crates/pcloud-store/src/repositories/values.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-store/src/repositories/values.rs) | runtime/library unit | Typed key/value helpers that mirror the C `psync_{get,set,has}_*_value` | Keep this responsibility explicit: Typed key/value helpers that mirror the C `psync_{get,set,has}_*_value`. A dedicated values unit localizes dependencies, review, and regression testing inside `pcloud-store`. | The focused `pcloud-store` responsibility described as: Typed key/value helpers that mirror the C `psync_{get,set,has}_*_value`. Transactions, versioned migrations, busy retries, HMAC-indexed sensitive keys, integrity checks, and repository boundaries protect consistency. This unit contains 20 indexed Rust declarations. |
| [`crates/pcloud-store/src/retry.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-store/src/retry.rs) | runtime/library unit | `SQLITE_BUSY` / `SQLITE_LOCKED` classification + exponential-backoff retry | Keep this responsibility explicit: `SQLITE_BUSY` / `SQLITE_LOCKED` classification + exponential-backoff retry. A dedicated retry unit localizes dependencies, review, and regression testing inside `pcloud-store`. | The focused `pcloud-store` responsibility described as: `SQLITE_BUSY` / `SQLITE_LOCKED` classification + exponential-backoff retry. Transactions, versioned migrations, busy retries, HMAC-indexed sensitive keys, integrity checks, and repository boundaries protect consistency. This unit contains 15 indexed Rust declarations. |
| [`crates/pcloud-store/src/schema.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-store/src/schema.rs) | runtime/library unit | Bootstrap schema version: creates `account`, `audit_events`, and `sync_roots`. | Keep this responsibility explicit: Bootstrap schema version: creates `account`, `audit_events`, and `sync_roots`. A dedicated schema unit localizes dependencies, review, and regression testing inside `pcloud-store`. | The focused `pcloud-store` responsibility described as: Bootstrap schema version: creates `account`, `audit_events`, and `sync_roots`. Transactions, versioned migrations, busy retries, HMAC-indexed sensitive keys, integrity checks, and repository boundaries protect consistency. This unit contains 28 indexed Rust declarations. |
| [`crates/pcloud-store/src/tx.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-store/src/tx.rs) | runtime/library unit | Wraps a closure in a `BEGIN IMMEDIATE` / `COMMIT` pair with automatic `ROLLBACK` on error. | Keep this responsibility explicit: Wraps a closure in a `BEGIN IMMEDIATE` / `COMMIT` pair with automatic `ROLLBACK` on error. A dedicated tx unit localizes dependencies, review, and regression testing inside `pcloud-store`. | The focused `pcloud-store` responsibility described as: Wraps a closure in a `BEGIN IMMEDIATE` / `COMMIT` pair with automatic `ROLLBACK` on error. Transactions, versioned migrations, busy retries, HMAC-indexed sensitive keys, integrity checks, and repository boundaries protect consistency. This unit contains 3 indexed Rust declarations. |
| [`crates/pcloud-store/tests/store_basics.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-store/tests/store_basics.rs) | integration test | Integration tests for `pcloud-store`. | Prove the store basics behavior at a crate or process boundary instead of relying only on unit tests. | Protecting store basics boundary behavior from regressions. Transactions, versioned migrations, busy retries, HMAC-indexed sensitive keys, integrity checks, and repository boundaries protect consistency. This unit contains 19 indexed Rust declarations. |

All declarations: [`pcloud-store` generated crate page](../crates/pcloud-store.md).

## `pcloud-supervisor` (2 units)

Package context: Experimental account registry, account selection, IPC routing metadata, and per-account sub-daemon spawning.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-supervisor/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-supervisor/src/lib.rs) | runtime/library unit | T2.8 — multi-account supervisor scaffold. | Declare and compose the `pcloud-supervisor` crate boundary. Model multiple isolated accounts without merging credentials, state directories, or daemon authority. | The focused `pcloud-supervisor` responsibility described as: T2.8 — multi-account supervisor scaffold. Process and path isolation make account boundaries explicit; the scaffold remains separate until routing and lifecycle are production-wired. This unit contains 37 indexed Rust declarations. |
| [`crates/pcloud-supervisor/src/spawner.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-supervisor/src/spawner.rs) | runtime/library unit | T2.8.c — sub-daemon spawning helper. | Keep this responsibility explicit: T2.8.c — sub-daemon spawning helper. A dedicated spawner unit localizes dependencies, review, and regression testing inside `pcloud-supervisor`. | The focused `pcloud-supervisor` responsibility described as: T2.8.c — sub-daemon spawning helper. Process and path isolation make account boundaries explicit; the scaffold remains separate until routing and lifecycle are production-wired. This unit contains 13 indexed Rust declarations. |

All declarations: [`pcloud-supervisor` generated crate page](../crates/pcloud-supervisor.md).

## `pcloud-web` (8 units)

Package context: Loopback-first health, status, sync, and simple UI routes backed by daemon IPC.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-web/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-web/src/lib.rs) | runtime/library unit | pcloud-web | Declare and compose the `pcloud-web` crate boundary. Offer a browser-readable local status/control surface without giving the browser direct access to pCloud credentials. | The focused `pcloud-web` responsibility described as: pcloud-web. Host validation, loopback defaults, limited routes, and IPC delegation keep the MVP surface small and authority in pcloudd. This unit contains 26 indexed Rust declarations. |
| [`crates/pcloud-web/src/main.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-web/src/main.rs) | binary entrypoint/helper | \[derive(Debug)\] | Own the main executable lifecycle without mixing process concerns into reusable library code. | Process startup, argument/lifecycle ownership, and operator integration. Host validation, loopback defaults, limited routes, and IPC delegation keep the MVP surface small and authority in pcloudd. This unit contains 15 indexed Rust declarations. |
| [`crates/pcloud-web/src/routes.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-web/src/routes.rs) | runtime/library unit | HTTP routes for the single-user Web UI. | Keep this responsibility explicit: HTTP routes for the single-user Web UI. A dedicated routes unit localizes dependencies, review, and regression testing inside `pcloud-web`. | The focused `pcloud-web` responsibility described as: HTTP routes for the single-user Web UI. Host validation, loopback defaults, limited routes, and IPC delegation keep the MVP surface small and authority in pcloudd. This unit contains 70 indexed Rust declarations. |
| [`crates/pcloud-web/src/templates.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-web/src/templates.rs) | runtime/library unit | Inline HTML rendering for the MVP web UI. | Keep this responsibility explicit: Inline HTML rendering for the MVP web UI. A dedicated templates unit localizes dependencies, review, and regression testing inside `pcloud-web`. | The focused `pcloud-web` responsibility described as: Inline HTML rendering for the MVP web UI. Host validation, loopback defaults, limited routes, and IPC delegation keep the MVP surface small and authority in pcloudd. This unit contains 6 indexed Rust declarations. |
| [`crates/pcloud-web/tests/binary_coverage.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-web/tests/binary_coverage.rs) | integration test | \[test\] | Prove the binary coverage behavior at a crate or process boundary instead of relying only on unit tests. | Protecting binary coverage boundary behavior from regressions. Host validation, loopback defaults, limited routes, and IPC delegation keep the MVP surface small and authority in pcloudd. This unit contains 5 indexed Rust declarations. |
| [`crates/pcloud-web/tests/health.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-web/tests/health.rs) | integration test | Integration test: start the MVP web server on an ephemeral loopback | Prove the health behavior at a crate or process boundary instead of relying only on unit tests. | Protecting health boundary behavior from regressions. Host validation, loopback defaults, limited routes, and IPC delegation keep the MVP surface small and authority in pcloudd. This unit contains 2 indexed Rust declarations. |
| [`crates/pcloud-web/tests/serve_coverage.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-web/tests/serve_coverage.rs) | integration test | \[tokio::test\] | Prove the serve coverage behavior at a crate or process boundary instead of relying only on unit tests. | Protecting serve coverage boundary behavior from regressions. Host validation, loopback defaults, limited routes, and IPC delegation keep the MVP surface small and authority in pcloudd. This unit contains 2 indexed Rust declarations. |
| [`crates/pcloud-web/tests/ui.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-web/tests/ui.rs) | integration test | Integration tests for the expanded pcloud-web UI (P4.5+). | Prove the ui behavior at a crate or process boundary instead of relying only on unit tests. | Protecting ui boundary behavior from regressions. Host validation, loopback defaults, limited routes, and IPC delegation keep the MVP surface small and authority in pcloudd. This unit contains 17 indexed Rust declarations. |

All declarations: [`pcloud-web` generated crate page](../crates/pcloud-web.md).

## `pcloud-webdav` (7 units)

Package context: Experimental local HTTP parsing, PROPFIND/multistatus, method dispatch, and daemon-IPC-backed file operations.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`crates/pcloud-webdav/src/handler.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-webdav/src/handler.rs) | runtime/library unit | T1.6.b.2 — verb dispatch for the WebDAV gateway. | Keep this responsibility explicit: T1.6.b.2 — verb dispatch for the WebDAV gateway. A dedicated handler unit localizes dependencies, review, and regression testing inside `pcloud-webdav`. | The focused `pcloud-webdav` responsibility described as: T1.6.b.2 — verb dispatch for the WebDAV gateway. A minimal dependency-light codec and canonical IPC backend avoid creating a second remote implementation; RFC completeness is intentionally not claimed. This unit contains 54 indexed Rust declarations. |
| [`crates/pcloud-webdav/src/http.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-webdav/src/http.rs) | runtime/library unit | T1.6.b.1 — minimal HTTP/1.1 request/response codec for the WebDAV gateway. | Keep this responsibility explicit: T1.6.b.1 — minimal HTTP/1.1 request/response codec for the WebDAV gateway. A dedicated http unit localizes dependencies, review, and regression testing inside `pcloud-webdav`. | The focused `pcloud-webdav` responsibility described as: T1.6.b.1 — minimal HTTP/1.1 request/response codec for the WebDAV gateway. A minimal dependency-light codec and canonical IPC backend avoid creating a second remote implementation; RFC completeness is intentionally not claimed. This unit contains 30 indexed Rust declarations. |
| [`crates/pcloud-webdav/src/ipc_backend.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-webdav/src/ipc_backend.rs) | runtime/library unit | Canonical daemon-IPC backend for the experimental WebDAV subset. | Keep this responsibility explicit: Canonical daemon-IPC backend for the experimental WebDAV subset. A dedicated ipc backend unit localizes dependencies, review, and regression testing inside `pcloud-webdav`. | The focused `pcloud-webdav` responsibility described as: Canonical daemon-IPC backend for the experimental WebDAV subset. A minimal dependency-light codec and canonical IPC backend avoid creating a second remote implementation; RFC completeness is intentionally not claimed. This unit contains 24 indexed Rust declarations. |
| [`crates/pcloud-webdav/src/lib.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-webdav/src/lib.rs) | runtime/library unit | Experimental local HTTP/WebDAV subset. | Declare and compose the `pcloud-webdav` crate boundary. Explore compatibility with applications that understand WebDAV but not the native SDK or CLI. | The focused `pcloud-webdav` responsibility described as: Experimental local HTTP/WebDAV subset. A minimal dependency-light codec and canonical IPC backend avoid creating a second remote implementation; RFC completeness is intentionally not claimed. This unit contains 17 indexed Rust declarations. |
| [`crates/pcloud-webdav/src/propfind.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-webdav/src/propfind.rs) | runtime/library unit | T1.6.a — minimal RFC 4918 `PROPFIND` parser + `multistatus` builder. | Keep this responsibility explicit: T1.6.a — minimal RFC 4918 `PROPFIND` parser + `multistatus` builder. A dedicated propfind unit localizes dependencies, review, and regression testing inside `pcloud-webdav`. | The focused `pcloud-webdav` responsibility described as: T1.6.a — minimal RFC 4918 `PROPFIND` parser + `multistatus` builder. A minimal dependency-light codec and canonical IPC backend avoid creating a second remote implementation; RFC completeness is intentionally not claimed. This unit contains 24 indexed Rust declarations. |
| [`crates/pcloud-webdav/src/server.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-webdav/src/server.rs) | runtime/library unit | T1.6.c — TcpListener accept loop that drives the dispatcher. | Keep this responsibility explicit: T1.6.c — TcpListener accept loop that drives the dispatcher. A dedicated server unit localizes dependencies, review, and regression testing inside `pcloud-webdav`. | The focused `pcloud-webdav` responsibility described as: T1.6.c — TcpListener accept loop that drives the dispatcher. A minimal dependency-light codec and canonical IPC backend avoid creating a second remote implementation; RFC completeness is intentionally not claimed. This unit contains 30 indexed Rust declarations. |
| [`crates/pcloud-webdav/tests/server_coverage.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-webdav/tests/server_coverage.rs) | integration test | \[derive(Default)\] | Prove the server coverage behavior at a crate or process boundary instead of relying only on unit tests. | Protecting server coverage boundary behavior from regressions. A minimal dependency-light codec and canonical IPC backend avoid creating a second remote implementation; RFC completeness is intentionally not claimed. This unit contains 11 indexed Rust declarations. |

All declarations: [`pcloud-webdav` generated crate page](../crates/pcloud-webdav.md).

## `xtask` (1 units)

Package context: Format, lint, test, coverage, audit, packaging, Docker, native mount, Windows remote, release, and cleanup orchestration.

| Unit / feature | Kind | What it does | Why it exists | Good at / for, and why |
|---|---|---|---|---|
| [`xtask/src/main.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/xtask/src/main.rs) | binary entrypoint/helper | Repository-owned local CI/CD orchestration. | Own the main executable lifecycle without mixing process concerns into reusable library code. | Process startup, argument/lifecycle ownership, and operator integration. One Rust entrypoint pins command order, fail/skip policy, toolchain use, and cross-platform evidence so developer and release gates match. This unit contains 41 indexed Rust declarations. |

All declarations: [`xtask` generated crate page](../crates/xtask.md).
