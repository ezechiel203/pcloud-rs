# Complete current CLI, IPC, SDK, and binary surface

This catalog is generated from the current Rust enums and Cargo metadata. It
complements the legacy C-parity catalog: the latter answers compatibility
decisions; this page answers what today's pcloud-rs clients can name and route.

**Coverage: 139 of 139 CLI commands, 44 of 44 argumentless IPC methods, 113 of 113 argument-bearing IPC requests, and 6 of 6 Cargo binaries.**

> “Wired runtime arm” means `pcloudd::RuntimeShell::handle_request` has a direct
> match arm. It does not prove live pCloud success, native qualification, stable-SDK
> exposure, or that optional standalone enterprise crates are composed into pcloudd.

## Executable entrypoints

| Binary | Owning package | Why it exists / good for | Source |
|---|---|---|---|
| `pcloud-compat-shm-peek` | [`pcloud-compat`](../crates/pcloud-compat.md) | Isolate the small legacy C-client ABI surfaces that are still useful during migration. Good for Decoding legacy rpc_message_t frames and, when explicitly enabled, producing the old SysV shared-memory folder-list layout. | [`crates/pcloud-compat/src/bin/shm_peek.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-compat/src/bin/shm_peek.rs) |
| `pcloud-web` | [`pcloud-web`](../crates/pcloud-web.md) | Offer a browser-readable local status/control surface without giving the browser direct access to pCloud credentials. Good for Loopback-first health, status, sync, and simple UI routes backed by daemon IPC. | [`crates/pcloud-web/src/main.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-web/src/main.rs) |
| `pcloudc` | [`pcloud-cli`](../crates/pcloud-cli.md) | Expose daemon capabilities to humans and automation without duplicating remote or security logic in a short-lived process. Good for Login, status, file operations, sync, backup, Crypto, account administration, diagnostics, migration, JSON output, and shell completion. | [`crates/pcloud-cli/src/main.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/main.rs) |
| `pcloudd` | [`pcloud-daemon`](../crates/pcloud-daemon.md) | Own long-lived credentials, validated request dispatch, state, background work, and native resources in one per-user composition root. Good for Running pcloudd as the authoritative local service behind CLI, SDK, web, sync, mount, backup, health, and metrics. | [`crates/pcloud-daemon/src/main.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/main.rs) |
| `pcloudd-svc` | [`pcloud-daemon-win`](../crates/pcloud-daemon-win.md) | Explore Windows Service Control Manager hosting without contaminating the portable per-user daemon. Good for Experimental Windows service installation and SCM lifecycle integration. | [`crates/pcloud-daemon-win/src/main.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon-win/src/main.rs) |
| `xtask` | [`xtask`](../crates/xtask.md) | Keep CI/CD policy versioned and runnable locally instead of depending on opaque hosted-workflow behavior. Good for Format, lint, test, coverage, audit, packaging, Docker, native mount, Windows remote, release, and cleanup orchestration. | [`xtask/src/main.rs`](https://github.com/ezechiel203/pcloud-rs/blob/main/xtask/src/main.rs) |

## CLI `Command` (139)

Each command is parsed once and lowered by `Command::into_request`. A CLI-local
command may show a harmless fallback route that is not its real execution path.

| Command | What it does and why it exists | IPC route | Runtime owner / side effect | Good at / for | Platform and maturity |
|---|---|---|---|---|---|
| [`Help`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L42) | `pcloudc help` — print the man-page-style usage summary. Synopsis: no arguments. Daemon handler: `GetHealth` (we reuse the cheapest ping so the local CLI can still be rendered). Exit-code: always \[`crate::exit_code::ExitCode::Ok`\] after the help block is flushed. JSON: `{kind:"s… **Rationale:** Make process health, lifecycle, and operator control explicit instead of inferring them from a PID. | CLI-local; defensive fallback `Request::Plain` + `Method::GetHealth` | daemon runtime and observability; reads process/queue/health state or changes local lifecycle state | supervision, diagnosis, graceful maintenance, and automation | portable client surface; implemented local product surface |
| [`Status`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L47) | `pcloudc status` — fetch the daemon's top-level status snapshot. Daemon handler: \[`pcloud_ipc::Method::GetStatus`\]. Exit codes follow the standard \[`pcloud_ipc::ResponseStatus`\] mapping. JSON envelope carries the daemon's `status` + `message` fields. **Rationale:** Make process health, lifecycle, and operator control explicit instead of inferring them from a PID. | `Request::Plain` + `Method::GetStatus` | daemon runtime and observability; reads process/queue/health state or changes local lifecycle state | supervision, diagnosis, graceful maintenance, and automation | portable client surface; implemented local product surface |
| [`Health`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L52) | `pcloudc health` — cheap liveness probe used by doctor and supervisors. Daemon handler: \[`pcloud_ipc::Method::GetHealth`\]. Exit `0` on reachable, \[`crate::exit_code::ExitCode::Network`\] on transport failure. **Rationale:** Make process health, lifecycle, and operator control explicit instead of inferring them from a PID. | `Request::Plain` + `Method::GetHealth` | daemon runtime and observability; reads process/queue/health state or changes local lifecycle state | supervision, diagnosis, graceful maintenance, and automation | portable client surface; implemented local product surface |
| [`Pending`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L56) | `pcloudc pending` — enumerate in-flight transfer / sync work. Daemon handler: \[`pcloud_ipc::Method::GetPending`\]. Standard status-to-exit mapping. **Rationale:** Make process health, lifecycle, and operator control explicit instead of inferring them from a PID. | `Request::Plain` + `Method::GetPending` | daemon runtime and observability; reads process/queue/health state or changes local lifecycle state | supervision, diagnosis, graceful maintenance, and automation | portable client surface; implemented local product surface |
| [`Slo`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L65) | `pcloudc slo` — fetch the canonical Service-Level Objective report. Daemon handler: \[`pcloud_ipc::Method::GetSlo`\]. Response is a JSON \[`pcloud_ipc::SloReportPayload`\] and the command is field-selector-friendly: `pcloudc slo pass` extracts the aggregate bit, `pcloudc slo slos` r… **Rationale:** Make process health, lifecycle, and operator control explicit instead of inferring them from a PID. | `Request::Plain` + `Method::GetSlo` | daemon runtime and observability; reads process/queue/health state or changes local lifecycle state | supervision, diagnosis, graceful maintenance, and automation | portable client surface; implemented local product surface |
| [`ListLinks`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L68) | `pcloudc publink list` — list file/folder public links. Daemon handler: \[`pcloud_ipc::Method::ListPublicLinks`\]. **Rationale:** Represent public exposure as typed, revocable objects with explicit expiry/access policy. | `Request::Plain` + `Method::ListPublicLinks` | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | publishing files/folders, upload drop boxes, link hygiene, and controlled recipients | portable client surface; implemented API paths; live account qualification required |
| [`ListUploadLinks`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L71) | `pcloudc publink list-upload` — list upload-only public links. Daemon handler: \[`pcloud_ipc::Method::ListUploadLinks`\]. **Rationale:** Represent public exposure as typed, revocable objects with explicit expiry/access policy. | `Request::Plain` + `Method::ListUploadLinks` | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | publishing files/folders, upload drop boxes, link hygiene, and controlled recipients | portable client surface; implemented API paths; live account qualification required |
| [`ShowLink`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L74) | `pcloudc publink show &lt;code&gt;` — show metadata for one link. Daemon handler: `Request::ShowPublicLink`. **Rationale:** Represent public exposure as typed, revocable objects with explicit expiry/access policy. | `Request::ShowPublicLink` | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | publishing files/folders, upload drop boxes, link hygiene, and controlled recipients | portable client surface; implemented API paths; live account qualification required |
| [`DeleteLink`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L77) | `pcloudc publink delete &lt;link-id&gt;` — revoke a link. Daemon handler: `Request::DeletePublicLink`. **Rationale:** Represent public exposure as typed, revocable objects with explicit expiry/access policy. | `Request::DeletePublicLinkByCode` + `Request::DeletePublicLink` | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | publishing files/folders, upload drop boxes, link hygiene, and controlled recipients | portable client surface; implemented API paths; live account qualification required |
| [`CreateFileLink`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L80) | `pcloudc publink create-file &lt;path&gt;` — create a file link. Daemon handler: `Request::CreateFilePublicLink`. **Rationale:** Represent public exposure as typed, revocable objects with explicit expiry/access policy. | `Request::CreateFilePublicLink` | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | publishing files/folders, upload drop boxes, link hygiene, and controlled recipients | portable client surface; implemented API paths; live account qualification required |
| [`CreateFolderLink`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L83) | `pcloudc publink create-folder &lt;path&gt;` — create a folder link. Daemon handler: `Request::CreateFolderPublicLink`. **Rationale:** Represent public exposure as typed, revocable objects with explicit expiry/access policy. | `Request::CreateFolderPublicLink` | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | publishing files/folders, upload drop boxes, link hygiene, and controlled recipients | portable client surface; implemented API paths; live account qualification required |
| [`ChangeLinkExpire`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L86) | `pcloudc publink change-expire &lt;id&gt; \[unix-ts\]` — set/clear link expiry. Daemon handler: `Request::ChangePublicLinkExpire`. **Rationale:** Represent public exposure as typed, revocable objects with explicit expiry/access policy. | `Request::ChangePublicLinkExpire` | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | publishing files/folders, upload drop boxes, link hygiene, and controlled recipients | portable client surface; implemented API paths; live account qualification required |
| [`ChangeLinkPassword`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L90) | `pcloudc publink change-password &lt;id&gt; \[password\]` — set or clear a link password. Secret flows via `SecretString`; daemon handler: `Request::ChangePublicLinkPassword`. **Rationale:** Represent public exposure as typed, revocable objects with explicit expiry/access policy. | `Request::ChangePublicLinkPassword` | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | publishing files/folders, upload drop boxes, link hygiene, and controlled recipients | portable client surface; implemented API paths; live account qualification required |
| [`ChangeLinkUpload`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L94) | `pcloudc publink change-upload &lt;id&gt; &lt;policy&gt;` — change the upload policy. Daemon handler: `Request::ChangePublicLinkUpload`. **Rationale:** Represent public exposure as typed, revocable objects with explicit expiry/access policy. | `Request::ChangePublicLinkUpload` | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | publishing files/folders, upload drop boxes, link hygiene, and controlled recipients | portable client surface; implemented API paths; live account qualification required |
| [`CreateUploadLink`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L97) | `pcloudc publink create-upload &lt;path&gt; \[...\]` — create an upload link. Daemon handler: `Request::CreateUploadLink`. **Rationale:** Represent public exposure as typed, revocable objects with explicit expiry/access policy. | `Request::CreateUploadLink` | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | publishing files/folders, upload drop boxes, link hygiene, and controlled recipients | portable client surface; implemented API paths; live account qualification required |
| [`DeleteUploadLink`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L100) | `pcloudc publink delete-upload &lt;id&gt;` — delete an upload link. Daemon handler: `Request::DeleteUploadLink`. **Rationale:** Represent public exposure as typed, revocable objects with explicit expiry/access policy. | `Request::DeleteUploadLink` | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | publishing files/folders, upload drop boxes, link hygiene, and controlled recipients | portable client surface; implemented API paths; live account qualification required |
| [`CreateTreeLink`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L104) | `pcloudc publink create-tree &lt;name&gt; \[...\]` — create a tree (selection) link. Daemon handler: `Request::CreateTreePublicLink`. **Rationale:** Represent public exposure as typed, revocable objects with explicit expiry/access policy. | `Request::CreateTreePublicLink` | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | publishing files/folders, upload drop boxes, link hygiene, and controlled recipients | portable client surface; implemented API paths; live account qualification required |
| [`ListLinkAccess`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L107) | `pcloudc publink access-list &lt;id&gt;` — list grant entries. Daemon handler: `Request::ListPublicLinkAccess`. **Rationale:** Represent public exposure as typed, revocable objects with explicit expiry/access policy. | `Request::ListPublicLinkAccess` | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | publishing files/folders, upload drop boxes, link hygiene, and controlled recipients | portable client surface; implemented API paths; live account qualification required |
| [`AddLinkAccess`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L110) | `pcloudc publink access-add &lt;id&gt; &lt;email&gt;` — add grantee. Daemon handler: `Request::AddPublicLinkAccess`. **Rationale:** Represent public exposure as typed, revocable objects with explicit expiry/access policy. | `Request::AddPublicLinkAccess` | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | publishing files/folders, upload drop boxes, link hygiene, and controlled recipients | portable client surface; implemented API paths; live account qualification required |
| [`RemoveLinkAccess`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L113) | `pcloudc publink access-remove &lt;id&gt; &lt;receiver-id&gt;` — revoke grantee. Daemon handler: `Request::RemovePublicLinkAccess`. **Rationale:** Represent public exposure as typed, revocable objects with explicit expiry/access policy. | `Request::RemovePublicLinkAccess` | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | publishing files/folders, upload drop boxes, link hygiene, and controlled recipients | portable client surface; implemented API paths; live account qualification required |
| [`ListBookmarks`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L116) | `pcloudc bookmark list` — list public bookmarks. Daemon handler: `Request::ListBookmarks`. **Rationale:** Represent public exposure as typed, revocable objects with explicit expiry/access policy. | `Request::ListBookmarks` | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | publishing files/folders, upload drop boxes, link hygiene, and controlled recipients | portable client surface; implemented API paths; live account qualification required |
| [`RemoveBookmark`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L119) | `pcloudc bookmark remove &lt;code&gt; &lt;location-id&gt;`. Daemon handler: `Request::RemoveBookmark`. **Rationale:** Represent public exposure as typed, revocable objects with explicit expiry/access policy. | `Request::RemoveBookmark` | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | publishing files/folders, upload drop boxes, link hygiene, and controlled recipients | portable client surface; implemented API paths; live account qualification required |
| [`ChangeBookmark`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L122) | `pcloudc bookmark change &lt;code&gt; &lt;location-id&gt; &lt;name&gt; &lt;description&gt;`. Daemon handler: `Request::ChangeBookmark`. **Rationale:** Represent public exposure as typed, revocable objects with explicit expiry/access policy. | `Request::ChangeBookmark` | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | publishing files/folders, upload drop boxes, link hygiene, and controlled recipients | portable client surface; implemented API paths; live account qualification required |
| [`SyncList`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L125) | `pcloudc sync list` — list configured sync roots. Daemon handler: \[`pcloud_ipc::Method::GetSyncRoots`\]. **Rationale:** Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. | `Request::Plain` + `Method::GetSyncRoots` | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | continuous synchronization, selective sync, pause/resume, conflict handling, and recovery | portable client surface; evolving implemented product surface |
| [`SyncAdd`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L130) | `pcloudc sync add &lt;local&gt; &lt;remote&gt;` — register a new sync root. Daemon handler: `Request::SyncRootAdd` — rejects duplicate/nested roots with \[`crate::exit_code::ExitCode::Conflict`\]. **Rationale:** Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. | `Request::SyncRootAdd` | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | continuous synchronization, selective sync, pause/resume, conflict handling, and recovery | portable client surface; evolving implemented product surface |
| [`SyncRemove`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L133) | `pcloudc sync remove &lt;sync-id&gt;` — unregister a sync root. Daemon handler: `Request::SyncRootRemove`. **Rationale:** Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. | `Request::SyncRootRemove` | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | continuous synchronization, selective sync, pause/resume, conflict handling, and recovery | portable client surface; evolving implemented product surface |
| [`SyncStatus`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L136) | `pcloudc sync status` — report background sync loop status. Daemon handler: \[`pcloud_ipc::Method::GetSyncStatus`\]. **Rationale:** Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. | `Request::Plain` + `Method::GetSyncStatus` | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | continuous synchronization, selective sync, pause/resume, conflict handling, and recovery | portable client surface; evolving implemented product surface |
| [`SyncChangeType`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L149) | `pcloudc sync change-type &lt;sync-id&gt; &lt;flavor&gt;` — change the direction of an existing sync root. Daemon handler: `Request::SyncRootChangeType`. Accepted flavor aliases: `bilateral\|full\|both`, `mirror\|download-only\|down\|remote-to-local`, `backup\|upload-only\|up\|local-to-remote`. **P… **Rationale:** Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. | `Request::SyncRootChangeType` | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | continuous synchronization, selective sync, pause/resume, conflict handling, and recovery | portable client surface; evolving implemented product surface |
| [`SyncExcludeAdd`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L153) | `pcloudc sync exclude add &lt;sync-id&gt; &lt;pattern&gt;` — append a glob to a sync root's exclusion list (T1.1). Daemon handler: `Request::SyncExcludeAdd`. **Rationale:** Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. | `Request::SyncExcludeAdd` | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | continuous synchronization, selective sync, pause/resume, conflict handling, and recovery | portable client surface; evolving implemented product surface |
| [`SyncExcludeRemove`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L157) | `pcloudc sync exclude remove &lt;sync-id&gt; &lt;pattern&gt;` — drop a glob from a sync root's exclusion list (T1.1). Daemon handler: `Request::SyncExcludeRemove`. **Rationale:** Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. | `Request::SyncExcludeRemove` | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | continuous synchronization, selective sync, pause/resume, conflict handling, and recovery | portable client surface; evolving implemented product surface |
| [`SyncExcludeList`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L161) | `pcloudc sync exclude list &lt;sync-id&gt;` — list a sync root's exclusion globs (T1.1). Daemon handler: `Request::SyncExcludeList`. **Rationale:** Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. | `Request::SyncExcludeList` | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | continuous synchronization, selective sync, pause/resume, conflict handling, and recovery | portable client surface; evolving implemented product surface |
| [`UserInfo`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L164) | `pcloudc userinfo` — fetch authenticated-user profile. Daemon handler: \[`pcloud_ipc::Method::GetUserInfo`\]. **Rationale:** Keep account administration separate from authentication and file operations. | `Request::Plain` + `Method::GetUserInfo` | account backend, configuration/settings store, and typed pCloud account protocol; reads or mutates account/profile/API-region/language/value state | registration, verification, password recovery/change, preferences, region selection, and promotion data | portable client surface; implemented API paths; destructive operations require isolated live accounts |
| [`Pause`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L167) | `pcloudc pause` — pause all sync activity. Daemon handler: \[`pcloud_ipc::Method::PauseSync`\]. **Rationale:** Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. | `Request::Plain` + `Method::PauseSync` | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | continuous synchronization, selective sync, pause/resume, conflict handling, and recovery | portable client surface; evolving implemented product surface |
| [`Resume`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L170) | `pcloudc resume` — resume sync activity. Daemon handler: \[`pcloud_ipc::Method::ResumeSync`\]. **Rationale:** Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. | `Request::Plain` + `Method::ResumeSync` | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | continuous synchronization, selective sync, pause/resume, conflict handling, and recovery | portable client surface; evolving implemented product surface |
| [`LoginBegin`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L173) | `pcloudc login` (phase 1) — kick off the login state machine. Daemon handler: \[`pcloud_ipc::Method::LoginBegin`\]. **Rationale:** Keep secret submission and session transitions inside one typed state machine. | `Request::Plain` + `Method::LoginBegin` | pcloud-auth, session, auth backend, and token vault; changes or reports the authenticated session and may call pCloud auth APIs or the local vault | login, TFA, refresh, persistence opt-in, logout, and session diagnosis | portable client surface; implemented; real-account/TFA paths require live evidence |
| [`Logout`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L176) | `pcloudc logout` — drop the active session. Daemon handler: \[`pcloud_ipc::Method::Logout`\]. **Rationale:** Keep secret submission and session transitions inside one typed state machine. | `Request::Plain` + `Method::Logout` | pcloud-auth, session, auth backend, and token vault; changes or reports the authenticated session and may call pCloud auth APIs or the local vault | login, TFA, refresh, persistence opt-in, logout, and session diagnosis | portable client surface; implemented; real-account/TFA paths require live evidence |
| [`SendTwoFactorSms`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L180) | `pcloudc login --tfa-channel sms` — request a fresh SMS OTP. Daemon handler: \[`pcloud_ipc::Method::SendTwoFactorSms`\]. **Rationale:** Keep secret submission and session transitions inside one typed state machine. | `Request::Plain` + `Method::SendTwoFactorSms` | pcloud-auth, session, auth backend, and token vault; changes or reports the authenticated session and may call pCloud auth APIs or the local vault | login, TFA, refresh, persistence opt-in, logout, and session diagnosis | portable client surface; implemented; real-account/TFA paths require live evidence |
| [`SendTwoFactorNotification`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L185) | `pcloudc login --tfa-channel notification` — resend the push-notification challenge. Daemon handler: \[`pcloud_ipc::Method::SendTwoFactorNotification`\]. **Rationale:** Keep secret submission and session transitions inside one typed state machine. | `Request::Plain` + `Method::SendTwoFactorNotification` | pcloud-auth, session, auth backend, and token vault; changes or reports the authenticated session and may call pCloud auth APIs or the local vault | login, TFA, refresh, persistence opt-in, logout, and session diagnosis | portable client surface; implemented; real-account/TFA paths require live evidence |
| [`SubmitPassword`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L190) | Submit the user's password. Secret flows via \[`SecretInputs::password`\] (\[`SecretString`\]); daemon handler: `Request::PasswordSubmission`. Auth failure → \[`crate::exit_code::ExitCode::Auth`\]. **Rationale:** Keep secret submission and session transitions inside one typed state machine. | `Request::PasswordSubmission` | pcloud-auth, session, auth backend, and token vault; changes or reports the authenticated session and may call pCloud auth APIs or the local vault | login, TFA, refresh, persistence opt-in, logout, and session diagnosis | portable client surface; implemented; real-account/TFA paths require live evidence |
| [`SubmitAuthToken`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L194) | Submit a persisted auth token. Secret flows via \[`SecretInputs::auth_token`\]; daemon handler: `Request::AuthTokenSubmission`. **Rationale:** Keep secret submission and session transitions inside one typed state machine. | `Request::AuthTokenSubmission` | pcloud-auth, session, auth backend, and token vault; changes or reports the authenticated session and may call pCloud auth APIs or the local vault | login, TFA, refresh, persistence opt-in, logout, and session diagnosis | portable client surface; implemented; real-account/TFA paths require live evidence |
| [`SubmitTwoFactorCode`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L198) | Submit a 6-digit TFA code. Daemon handler: `Request::TwoFactorCodeSubmission` with `recovery_code = false`. **Rationale:** Keep secret submission and session transitions inside one typed state machine. | `Request::TwoFactorCodeSubmission` | pcloud-auth, session, auth backend, and token vault; changes or reports the authenticated session and may call pCloud auth APIs or the local vault | login, TFA, refresh, persistence opt-in, logout, and session diagnosis | portable client surface; implemented; real-account/TFA paths require live evidence |
| [`SubmitRecoveryCode`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L201) | Submit a one-shot TFA recovery code. Same daemon handler as `SubmitTwoFactorCode` but with `recovery_code = true`. **Rationale:** Keep secret submission and session transitions inside one typed state machine. | `Request::TwoFactorCodeSubmission` | pcloud-auth, session, auth backend, and token vault; changes or reports the authenticated session and may call pCloud auth APIs or the local vault | login, TFA, refresh, persistence opt-in, logout, and session diagnosis | portable client surface; implemented; real-account/TFA paths require live evidence |
| [`SubmitCryptoPassword`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L206) | Submit the crypto password to unlock the Crypto Folder. Secret flows via \[`SecretInputs::crypto_password`\]; daemon handler: `Request::CryptoUnlock`. Locked / wrong-password → \[`crate::exit_code::ExitCode::CryptoLocked`\]. **Rationale:** Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. | `Request::CryptoUnlock` | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset | portable client surface; implemented code; interoperability/provider/native evidence is separate |
| [`AuthSave`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L210) | `pcloudc authsave` — toggle durable auth-token vault persistence (opt-in; see CLAUDE.md security rules). Daemon handler: `Request::AuthPersistence`. **Rationale:** Keep secret submission and session transitions inside one typed state machine. | `Request::AuthPersistence` | pcloud-auth, session, auth backend, and token vault; changes or reports the authenticated session and may call pCloud auth APIs or the local vault | login, TFA, refresh, persistence opt-in, logout, and session diagnosis | portable client surface; implemented; real-account/TFA paths require live evidence |
| [`LockCrypto`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L213) | `pcloudc crypto lock` — re-lock the Crypto Folder. Daemon handler: \[`pcloud_ipc::Method::LockCrypto`\]. **Rationale:** Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. | `Request::Plain` + `Method::LockCrypto` | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset | portable client surface; implemented code; interoperability/provider/native evidence is separate |
| [`Shutdown`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L216) | `pcloudc shutdown` — request an orderly daemon shutdown. Daemon handler: \[`pcloud_ipc::Method::Shutdown`\]. **Rationale:** Make process health, lifecycle, and operator control explicit instead of inferring them from a PID. | `Request::Plain` + `Method::Shutdown` | daemon runtime and observability; reads process/queue/health state or changes local lifecycle state | supervision, diagnosis, graceful maintenance, and automation | portable client surface; implemented local product surface |
| [`Drain`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L229) | `pcloudc drain` — operator-facing graceful-drain command. Reads the daemon pidfile from `&lt;state_dir&gt;/daemon.pid`, dispatches SIGTERM to that pid, and polls \[`pcloud_ipc::Method::DrainStatus`\] every 500 ms until the daemon reports `state == "stopped"` or the configured `upgrade.h… **Rationale:** Make process health, lifecycle, and operator control explicit instead of inferring them from a PID. | CLI-local; defensive fallback `Request::Plain` + `Method::DrainStatus` | daemon runtime and observability; reads process/queue/health state or changes local lifecycle state | supervision, diagnosis, graceful maintenance, and automation | portable client surface; implemented local product surface |
| [`Start`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L237) | `pcloudc start` — local CLI-only command: spawn `pcloudd` in the background if not already running, redirect its stdio to `~/.pcloud/state/daemon.log`, and exit once the IPC socket is reachable. Handled entirely client-side; `into_request` maps this to a harmless `GetHealth` as… **Rationale:** Make process health, lifecycle, and operator control explicit instead of inferring them from a PID. | CLI-local; defensive fallback `Request::Plain` + `Method::GetHealth` | daemon runtime and observability; reads process/queue/health state or changes local lifecycle state | supervision, diagnosis, graceful maintenance, and automation | portable client surface; implemented local product surface |
| [`ListIncomingShares`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L241) | `pcloudc shares list-incoming` — \[`pcloud_ipc::Method::ListIncomingShares`\]. **Rationale:** Preserve each collaboration lifecycle and permission transition as an auditable typed operation. | `Request::Plain` + `Method::ListIncomingShares` | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | person-to-person sharing, team administration, invitations, and access reviews | portable client surface; implemented API paths; bilateral/business live fixtures required |
| [`ListOutgoingShares`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L244) | `pcloudc shares list-outgoing` — \[`pcloud_ipc::Method::ListOutgoingShares`\]. **Rationale:** Preserve each collaboration lifecycle and permission transition as an auditable typed operation. | `Request::Plain` + `Method::ListOutgoingShares` | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | person-to-person sharing, team administration, invitations, and access reviews | portable client surface; implemented API paths; bilateral/business live fixtures required |
| [`ListIncomingShareRequests`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L247) | `pcloudc shares list-incoming-requests` — \[`pcloud_ipc::Method::ListIncomingShareRequests`\]. **Rationale:** Preserve each collaboration lifecycle and permission transition as an auditable typed operation. | `Request::Plain` + `Method::ListIncomingShareRequests` | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | person-to-person sharing, team administration, invitations, and access reviews | portable client surface; implemented API paths; bilateral/business live fixtures required |
| [`ListOutgoingShareRequests`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L250) | `pcloudc shares list-outgoing-requests` — \[`pcloud_ipc::Method::ListOutgoingShareRequests`\]. **Rationale:** Preserve each collaboration lifecycle and permission transition as an auditable typed operation. | `Request::Plain` + `Method::ListOutgoingShareRequests` | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | person-to-person sharing, team administration, invitations, and access reviews | portable client surface; implemented API paths; bilateral/business live fixtures required |
| [`ListContacts`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L253) | `pcloudc contacts list` — \[`pcloud_ipc::Method::ListContacts`\]. **Rationale:** Preserve each collaboration lifecycle and permission transition as an auditable typed operation. | `Request::Plain` + `Method::ListContacts` | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | person-to-person sharing, team administration, invitations, and access reviews | portable client surface; implemented API paths; bilateral/business live fixtures required |
| [`ListMyTeams`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L255) | `pcloudc teams list` — \[`pcloud_ipc::Method::ListMyTeams`\]. **Rationale:** Preserve each collaboration lifecycle and permission transition as an auditable typed operation. | `Request::Plain` + `Method::ListMyTeams` | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | person-to-person sharing, team administration, invitations, and access reviews | portable client surface; implemented API paths; bilateral/business live fixtures required |
| [`ShareFolder`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L259) | `pcloudc shares create &lt;folder-id&gt; &lt;name&gt; &lt;mail&gt; \[...\]`. Daemon handler: `Request::ShareFolder`. Permission bits are the C-compatible bitmask. **Rationale:** Preserve each collaboration lifecycle and permission transition as an auditable typed operation. | `Request::ShareFolder` | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | person-to-person sharing, team administration, invitations, and access reviews | portable client surface; implemented API paths; bilateral/business live fixtures required |
| [`CancelShareRequest`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L262) | `pcloudc shares cancel-request &lt;id&gt;` — `Request::CancelShareRequest`. **Rationale:** Preserve each collaboration lifecycle and permission transition as an auditable typed operation. | `Request::CancelShareRequest` | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | person-to-person sharing, team administration, invitations, and access reviews | portable client surface; implemented API paths; bilateral/business live fixtures required |
| [`DeclineShareRequest`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L265) | `pcloudc shares decline-request &lt;id&gt;` — `Request::DeclineShareRequest`. **Rationale:** Preserve each collaboration lifecycle and permission transition as an auditable typed operation. | `Request::DeclineShareRequest` | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | person-to-person sharing, team administration, invitations, and access reviews | portable client surface; implemented API paths; bilateral/business live fixtures required |
| [`AcceptShareRequest`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L268) | `pcloudc shares accept-request &lt;id&gt; &lt;to-folder&gt; \[name\]` — `Request::AcceptShareRequest`. **Rationale:** Preserve each collaboration lifecycle and permission transition as an auditable typed operation. | `Request::AcceptShareRequest` | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | person-to-person sharing, team administration, invitations, and access reviews | portable client surface; implemented API paths; bilateral/business live fixtures required |
| [`RemoveShare`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L271) | `pcloudc shares remove &lt;share-id&gt;` — `Request::RemoveShare`. **Rationale:** Preserve each collaboration lifecycle and permission transition as an auditable typed operation. | `Request::RemoveShare` | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | person-to-person sharing, team administration, invitations, and access reviews | portable client surface; implemented API paths; bilateral/business live fixtures required |
| [`ModifyShare`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L274) | `pcloudc shares modify &lt;share-id&gt; &lt;perm-bits&gt;` — `Request::ModifyShare`. **Rationale:** Preserve each collaboration lifecycle and permission transition as an auditable typed operation. | `Request::ModifyShare` | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | person-to-person sharing, team administration, invitations, and access reviews | portable client surface; implemented API paths; bilateral/business live fixtures required |
| [`AccountStopShare`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L277) | `pcloudc account stop-share --users ... --teams ...` — `Request::AccountStopShare`. **Rationale:** Preserve each collaboration lifecycle and permission transition as an auditable typed operation. | `Request::AccountStopShare` | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | person-to-person sharing, team administration, invitations, and access reviews | portable client surface; implemented API paths; bilateral/business live fixtures required |
| [`AccountModifyShare`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L280) | `pcloudc account modify-share --user-mods ... --team-mods ...` — `Request::AccountModifyShare`. **Rationale:** Preserve each collaboration lifecycle and permission transition as an auditable typed operation. | `Request::AccountModifyShare` | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | person-to-person sharing, team administration, invitations, and access reviews | portable client surface; implemented API paths; bilateral/business live fixtures required |
| [`AccountTeamShare`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L283) | `pcloudc account team-share &lt;folder-id&gt; &lt;name&gt; &lt;team-id&gt; \[...\]` — `Request::AccountTeamShare`. **Rationale:** Preserve each collaboration lifecycle and permission transition as an auditable typed operation. | `Request::AccountTeamShare` | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | person-to-person sharing, team administration, invitations, and access reviews | portable client surface; implemented API paths; bilateral/business live fixtures required |
| [`ListNotifications`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L287) | List pCloud notifications for the authenticated user. `pcloud-rs notifications list` / `list-notifications`. Mirrors C `psync_get_notifications`. **Rationale:** Expose server-side events without coupling notification state to UI code. | `Request::Plain` + `Method::ListNotifications` | notifications backend and pCloud notification protocol; reads notification state or advances its remote read cursor | alerts, inbox workflows, plugins, and automation | portable client surface; implemented API path |
| [`MarkNotificationsRead`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L291) | Mark all pending notifications up to and including `upto_id` as read. `pcloud-rs notifications mark-read &lt;upto_id&gt;`. Mirrors C `psync_mark_notificaitons_read` (header typo preserved). **Rationale:** Expose server-side events without coupling notification state to UI code. | `Request::MarkNotificationsRead` | notifications backend and pCloud notification protocol; reads notification state or advances its remote read cursor | alerts, inbox workflows, plugins, and automation | portable client surface; implemented API path |
| [`CryptoStatus`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L295) | Report the daemon's current crypto lifecycle state. `pcloud-rs crypto status`. Mirrors C `psync_crypto_issetup` / `psync_crypto_isstarted`. **Rationale:** Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. | `Request::Plain` + `Method::GetCryptoStatus` | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset | portable client surface; implemented code; interoperability/provider/native evidence is separate |
| [`AuditVerify`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L297) | Verify the tamper-evident audit chain. **Rationale:** Make tamper, corruption, availability, and handoff state observable and verifiable. | `Request::AuditVerifyChain` | daemon integrity/audit/HA services and durable store; reads verification state or launches bounded local integrity/audit work | audit verification, integrity sweeps, HA status, diagnosis, and release evidence | portable client surface; implemented local controls; deployment/native evidence remains separate |
| [`SessionStatus`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L300) | Report session lifecycle status (expiry, last-used, refresh-in-flight). `pcloud-rs session status`. **Rationale:** Keep secret submission and session transitions inside one typed state machine. | `Request::Plain` + `Method::SessionStatus` | pcloud-auth, session, auth backend, and token vault; changes or reports the authenticated session and may call pCloud auth APIs or the local vault | login, TFA, refresh, persistence opt-in, logout, and session diagnosis | portable client surface; implemented; real-account/TFA paths require live evidence |
| [`Mount`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L302) | Mount the pCloud filesystem at a local path (`pcloud-rs mount &lt;path&gt;`). **Rationale:** Adapt the canonical remote namespace to kernels without creating a second source of truth. | `Request::Mount` | daemon mount runtime and pcloud-fs adapters; changes native mount state or inspects local filesystem integration | desktop drive letters/mountpoints, filesystem applications, cleanup, and diagnostics | native mount qualification required; platform-specific; native qualification required |
| [`MountForceUnmount`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L307) | Force-unmount an orphan pCloud FUSE mount at a local path (`pcloud-rs mount --force-umount &lt;path&gt;`). Used to recover after a crashed previous daemon left a mount entry behind. Tracker bd-1du.4 (P1.4). **Rationale:** Adapt the canonical remote namespace to kernels without creating a second source of truth. | `Request::MountForceUnmount` | daemon mount runtime and pcloud-fs adapters; changes native mount state or inspects local filesystem integration | desktop drive letters/mountpoints, filesystem applications, cleanup, and diagnostics | native mount qualification required; platform-specific; native qualification required |
| [`Unmount`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L309) | Unmount the active pCloud filesystem (`pcloud-rs unmount`). **Rationale:** Adapt the canonical remote namespace to kernels without creating a second source of truth. | `Request::MountForceUnmount` + `Request::Unmount` | daemon mount runtime and pcloud-fs adapters; changes native mount state or inspects local filesystem integration | desktop drive letters/mountpoints, filesystem applications, cleanup, and diagnostics | native mount qualification required; platform-specific; native qualification required |
| [`RunLocalScan`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L312) | Trigger an immediate local-scan wakeup on the daemon engine (`pcloud-rs sync localscan`). Mirrors C `psync_run_localscan`. **Rationale:** Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. | `Request::RunLocalScan` | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | continuous synchronization, selective sync, pause/resume, conflict handling, and recovery | portable client surface; evolving implemented product surface |
| [`SendPublink`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L316) | Mail an existing public-link `code` to one or more recipients (`pcloud-rs publink send &lt;code&gt; --to &lt;emails&gt; --message &lt;text&gt;`). Mirrors C `psync_send_publink`. **Rationale:** Represent public exposure as typed, revocable objects with explicit expiry/access policy. | `Request::SendPublink` | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | publishing files/folders, upload drop boxes, link hygiene, and controlled recipients | portable client surface; implemented API paths; live account qualification required |
| [`CreateRemoteFolder`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L323) | Create a remote folder by absolute path (`pcloud-rs folder create &lt;path&gt;`). Mirrors C `psync_create_remote_folder_by_path` (`pclsync/psynclib.c:1006`). The CLI surface always uses the path-based form; the daemon accepts the parent-id + name form via the IPC payload (used by the… **Rationale:** Give every drive-like consumer one cache-independent, ID-first namespace contract. | `Request::CreateRemoteFolder` | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access | portable client surface; implemented canonical product path |
| [`GetFolderIdByPath`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L327) | Resolve an absolute pCloud-drive path to its folder id. (`pcloud-rs folder id &lt;path&gt;`). Mirrors C `psync_get_fsfolderid_by_path` (`pclsync/psynclib.c:2170`). **Rationale:** Give every drive-like consumer one cache-independent, ID-first namespace contract. | `Request::GetFolderIdByPath` | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access | portable client surface; implemented canonical product path |
| [`GetFolderFlags`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L331) | Read folder flags/permissions for an absolute pCloud-drive path. (`pcloud-rs folder flags &lt;path&gt;`). Mirrors C `psync_get_fsfolderflags_by_id` (`pclsync/psynclib.c:2176`). **Rationale:** Give every drive-like consumer one cache-independent, ID-first namespace contract. | `Request::GetFolderFlags` | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access | portable client surface; implemented canonical product path |
| [`GetFolderOwnerId`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L335) | Read owner user id of a folder by absolute pCloud-drive path. (`pcloud-rs folder owner &lt;path&gt;`). Mirrors C `psync_get_folder_ownerid` (`pclsync/psynclib.c:2088`). **Rationale:** Give every drive-like consumer one cache-independent, ID-first namespace contract. | `Request::GetFolderOwnerId` | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access | portable client surface; implemented canonical product path |
| [`FilesystemStatus`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L339) | Classify a local path against the daemon's sync-root + engine state (`pcloud-rs fs status &lt;local-path&gt;`). Mirrors C `psync_filesystem_status` (`pclsync/psynclib.c:1903`). **Rationale:** Adapt the canonical remote namespace to kernels without creating a second source of truth. | `Request::FilesystemStatus` | daemon mount runtime and pcloud-fs adapters; changes native mount state or inspects local filesystem integration | desktop drive letters/mountpoints, filesystem applications, cleanup, and diagnostics | native mount qualification required; platform-specific; native qualification required |
| [`Stat`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L343) | Stat an absolute pCloud-drive path (`pcloudc stat &lt;path&gt;`). Mirrors C `psync_stat_path` (`pclsync/psynclib.h:743`). Resolves through local metadata cache, falls back to API. **Rationale:** Give every drive-like consumer one cache-independent, ID-first namespace contract. | `Request::StatPath` | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access | portable client surface; implemented canonical product path |
| [`RemoteLs`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L345) | `pcloudc ls \[REMOTE_PATH\]` — list direct remote children. **Rationale:** Give every drive-like consumer one cache-independent, ID-first namespace contract. | `Request::ListFolderByPath` | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access | portable client surface; implemented canonical product path |
| [`RemoteGet`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L347) | `pcloudc get &lt;REMOTE_PATH&gt; &lt;LOCAL_PATH&gt;` — streaming download. **Rationale:** Separate byte movement from namespace intent and persist resumable state before acknowledging progress. | `Request::DownloadFileByPath` | transfer backend, upload session registry/journal, and pCloud transfer protocol; moves bytes or controls durable upload/download session state | large files, signed downloads, range reads, resumable uploads, and progress control | portable client surface; implemented; real-service integrity/resume qualification required |
| [`RemotePut`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L349) | `pcloudc put &lt;LOCAL_PATH&gt; &lt;REMOTE_PATH&gt;` — streaming upload. **Rationale:** Separate byte movement from namespace intent and persist resumable state before acknowledging progress. | `Request::UploadFileByPath` | transfer backend, upload session registry/journal, and pCloud transfer protocol; moves bytes or controls durable upload/download session state | large files, signed downloads, range reads, resumable uploads, and progress control | portable client surface; implemented; real-service integrity/resume qualification required |
| [`RemoteCp`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L351) | `pcloudc cp &lt;REMOTE_FROM&gt; &lt;REMOTE_TO&gt;` — bounded remote copy. **Rationale:** Give every drive-like consumer one cache-independent, ID-first namespace contract. | `Request::CopyPath` | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access | portable client surface; implemented canonical product path |
| [`RemoteMv`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L353) | `pcloudc mv &lt;REMOTE_FROM&gt; &lt;REMOTE_TO&gt;` — remote move/rename. **Rationale:** Give every drive-like consumer one cache-independent, ID-first namespace contract. | `Request::RenamePath` | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access | portable client surface; implemented canonical product path |
| [`RemoteRm`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L355) | `pcloudc rm &lt;REMOTE_PATH&gt; \[-r\|--recursive\]` — remote deletion. **Rationale:** Give every drive-like consumer one cache-independent, ID-first namespace contract. | `Request::DeletePath` | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access | portable client surface; implemented canonical product path |
| [`RemoteMkdir`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L357) | `pcloudc mkdir &lt;REMOTE_PATH&gt;` — create a remote directory. **Rationale:** Give every drive-like consumer one cache-independent, ID-first namespace contract. | `Request::CreateFolderByPath` | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access | portable client surface; implemented canonical product path |
| [`RemoteCat`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L359) | `pcloudc cat &lt;REMOTE_PATH&gt;` — stream remote bytes to stdout. **Rationale:** Separate byte movement from namespace intent and persist resumable state before acknowledging progress. | `Request::ReadFileRange` | transfer backend, upload session registry/journal, and pCloud transfer protocol; moves bytes or controls durable upload/download session state | large files, signed downloads, range reads, resumable uploads, and progress control | portable client surface; implemented; real-service integrity/resume qualification required |
| [`Reload`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L365) | `pcloudc reload` — send SIGHUP to the daemon to trigger a config hot-reload. Looks up the daemon pidfile from `&lt;state_dir&gt;/daemon.pid`, sends SIGHUP via `kill(2)`, and exits. Handled entirely CLI-side; `into_request` maps to `GetHealth` as a defensive fallback. **Rationale:** Make process health, lifecycle, and operator control explicit instead of inferring them from a PID. | CLI-local; defensive fallback `Request::Plain` + `Method::GetHealth` | daemon runtime and observability; reads process/queue/health state or changes local lifecycle state | supervision, diagnosis, graceful maintenance, and automation | portable client surface; implemented local product surface |
| [`Doctor`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L368) | Run the `pcloudc doctor` self-diagnostic battery. Handled entirely CLI-side; never reaches \[`Command::into_request`\]. **Rationale:** Make process health, lifecycle, and operator control explicit instead of inferring them from a PID. | CLI-local; defensive fallback `Request::Plain` + `Method::GetHealth` | daemon runtime and observability; reads process/queue/health state or changes local lifecycle state | supervision, diagnosis, graceful maintenance, and automation | portable client surface; implemented local product surface |
| [`MigrateFromC`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L375) | Import legacy C `pcloud-rs` client state (`~/.pcloud/.pclouddb` plus associated files) into the canonical XDG layout consumed by the Rust daemon. Handled entirely CLI-side; never reaches \[`Command::into_request`\]. Identified as a GA blocker by the R8 release review — users migra… **Rationale:** Make tamper, corruption, availability, and handoff state observable and verifiable. | CLI-local; defensive fallback `Request::Plain` + `Request::VerifyPath` + `Method::GetHealth` | daemon integrity/audit/HA services and durable store; reads verification state or launches bounded local integrity/audit work | audit verification, integrity sweeps, HA status, diagnosis, and release evidence | portable client surface; implemented local controls; deployment/native evidence remains separate |
| [`Verify`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L395) | `pcloudc verify &lt;PATH&gt; \[--recursive\] \[--fix\] \[--yes\]` — walk a synced tree and cross-check the local SHA256 of each file against the server-reported digest. R9 enhancement #12. Handled CLI-side via `crate::verify::run`; the companion `Request::VerifyPath` is reserved for a futur… **Rationale:** Make tamper, corruption, availability, and handoff state observable and verifiable. | CLI-local; defensive fallback `Request::VerifyPath` | daemon integrity/audit/HA services and durable store; reads verification state or launches bounded local integrity/audit work | audit verification, integrity sweeps, HA status, diagnosis, and release evidence | portable client surface; implemented local controls; deployment/native evidence remains separate |
| [`SnapshotCreate`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L413) | `pcloudc snapshot create &lt;path&gt; \[--zstd-level N\] \[--gpg-recipient EMAIL\]` — default tar → zstd → SHA3-sealed-sidecar pipeline. When `--gpg-recipient` is set, the archive is wrapped in a GPG envelope and `&lt;path&gt;` must end with `.tar.zst.gpg`; otherwise it must end with `.tar.zst`. **Rationale:** Keep archival/device lifecycle distinct from bidirectional sync and make recovery evidence executable. | `Request::BackupSnapshot` | backup backend, snapshot helpers, store, and transfer path; creates, reports, stops, verifies, restores, or removes backup/device/snapshot state | device backup, portable snapshots, retention, verification, and disaster recovery | portable client surface; implemented components; destructive/live/restore drills required |
| [`SnapshotRestore`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L418) | `pcloudc snapshot restore &lt;path&gt; --yes` — destructive; requires `--yes` for non-interactive callers (or a TTY prompt in interactive mode). Accepts both new zstd archives and legacy `.tar.gpg` archives. **Rationale:** Keep archival/device lifecycle distinct from bidirectional sync and make recovery evidence executable. | `Request::BackupSnapshot` | backup backend, snapshot helpers, store, and transfer path; creates, reports, stops, verifies, restores, or removes backup/device/snapshot state | device backup, portable snapshots, retention, verification, and disaster recovery | portable client surface; implemented components; destructive/live/restore drills required |
| [`SnapshotVerify`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L422) | `pcloudc snapshot verify &lt;path&gt;` — non-mutating outer + inner integrity check (SHA3-sidecar, zstd decode, inner per-payload SHA-256). **Rationale:** Keep archival/device lifecycle distinct from bidirectional sync and make recovery evidence executable. | `Request::BackupSnapshot` | backup backend, snapshot helpers, store, and transfer path; creates, reports, stops, verifies, restores, or removes backup/device/snapshot state | device backup, portable snapshots, retention, verification, and disaster recovery | portable client surface; implemented components; destructive/live/restore drills required |
| [`SnapshotPrune`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L425) | `pcloudc snapshot prune &lt;dir&gt; --retention-days N \[--yes\]` — destructive retention sweep. `--retention-days` is required. **Rationale:** Keep archival/device lifecycle distinct from bidirectional sync and make recovery evidence executable. | `Request::BackupSnapshot` | backup backend, snapshot helpers, store, and transfer path; creates, reports, stops, verifies, restores, or removes backup/device/snapshot state | device backup, portable snapshots, retention, verification, and disaster recovery | portable client surface; implemented components; destructive/live/restore drills required |
| [`BackupSnapshotCreate`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L430) | Deprecated alias for \[`Self::SnapshotCreate`\]. The CLI still accepts `pcloudc backup snapshot-create` (and the single-token `backup-snapshot-create`) for one release cycle and prints a one-line stderr warning when used. **Rationale:** Keep archival/device lifecycle distinct from bidirectional sync and make recovery evidence executable. | `Request::BackupSnapshot` | backup backend, snapshot helpers, store, and transfer path; creates, reports, stops, verifies, restores, or removes backup/device/snapshot state | device backup, portable snapshots, retention, verification, and disaster recovery | portable client surface; implemented components; destructive/live/restore drills required |
| [`BackupSnapshotRestore`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L433) | Deprecated alias for \[`Self::SnapshotRestore`\]. See \[`Self::BackupSnapshotCreate`\]. **Rationale:** Keep archival/device lifecycle distinct from bidirectional sync and make recovery evidence executable. | `Request::BackupSnapshot` | backup backend, snapshot helpers, store, and transfer path; creates, reports, stops, verifies, restores, or removes backup/device/snapshot state | device backup, portable snapshots, retention, verification, and disaster recovery | portable client surface; implemented components; destructive/live/restore drills required |
| [`BackupSnapshotVerify`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L436) | Deprecated alias for \[`Self::SnapshotVerify`\]. See \[`Self::BackupSnapshotCreate`\]. **Rationale:** Keep archival/device lifecycle distinct from bidirectional sync and make recovery evidence executable. | `Request::BackupSnapshot` | backup backend, snapshot helpers, store, and transfer path; creates, reports, stops, verifies, restores, or removes backup/device/snapshot state | device backup, portable snapshots, retention, verification, and disaster recovery | portable client surface; implemented components; destructive/live/restore drills required |
| [`BackupSnapshotPrune`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L439) | Deprecated alias for \[`Self::SnapshotPrune`\]. See \[`Self::BackupSnapshotCreate`\]. **Rationale:** Keep archival/device lifecycle distinct from bidirectional sync and make recovery evidence executable. | `Request::BackupSnapshot` | backup backend, snapshot helpers, store, and transfer path; creates, reports, stops, verifies, restores, or removes backup/device/snapshot state | device backup, portable snapshots, retention, verification, and disaster recovery | portable client surface; implemented components; destructive/live/restore drills required |
| [`IntegrityStatus`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L443) | `pcloudc integrity status` — fetch sweeper progress JSON. Daemon handler: \[`pcloud_ipc::Method::IntegrityStatus`\]. H14 PR4. Tracker: bd-1du.4.6.1. **Rationale:** Make tamper, corruption, availability, and handoff state observable and verifiable. | `Request::Plain` + `Method::IntegrityStatus` | daemon integrity/audit/HA services and durable store; reads verification state or launches bounded local integrity/audit work | audit verification, integrity sweeps, HA status, diagnosis, and release evidence | portable client surface; implemented local controls; deployment/native evidence remains separate |
| [`IntegrityRunOnce`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L448) | `pcloudc integrity run-once` — synchronously trigger one background-integrity-sweeper cycle. Blocks until the cycle completes; renders the post-cycle progress summary. Daemon handler: \[`pcloud_ipc::Request::IntegrityRunOnce`\]. H14 PR4. **Rationale:** Make tamper, corruption, availability, and handoff state observable and verifiable. | `Request::IntegrityRunOnce` | daemon integrity/audit/HA services and durable store; reads verification state or launches bounded local integrity/audit work | audit verification, integrity sweeps, HA status, diagnosis, and release evidence | portable client surface; implemented local controls; deployment/native evidence remains separate |
| [`IntegritySkip`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L452) | `pcloudc integrity skip &lt;PATH&gt;` — append a glob pattern to the configured skip-list file. Daemon handler: \[`pcloud_ipc::Request::IntegritySkip`\]. H14 PR4. **Rationale:** Make tamper, corruption, availability, and handoff state observable and verifiable. | `Request::IntegritySkip` | daemon integrity/audit/HA services and durable store; reads verification state or launches bounded local integrity/audit work | audit verification, integrity sweeps, HA status, diagnosis, and release evidence | portable client surface; implemented local controls; deployment/native evidence remains separate |
| [`HaStatus`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L458) | `pcloudc ha status` — return the Tier-2 HA posture as `{mode, lease_owner, lease_age_s, lease_path}`. Daemon handler: \[`pcloud_ipc::Method::HaStatus`\]. See `docs/enterprise/ha.md` §4.2. Safe to call at any time; returns `mode = "disabled"` when HA is not configured. **Rationale:** Make tamper, corruption, availability, and handoff state observable and verifiable. | `Request::Plain` + `Method::HaStatus` | daemon integrity/audit/HA services and durable store; reads verification state or launches bounded local integrity/audit work | audit verification, integrity sweeps, HA status, diagnosis, and release evidence | portable client surface; implemented local controls; deployment/native evidence remains separate |
| [`AuditVerifierStatus`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L463) | `pcloudc audit-verifier status` — return the scheduled audit-chain verifier status as JSON. Daemon handler: \[`pcloud_ipc::Method::GetAuditVerifierStatus`\]. Safe to call at any time; returns `enabled = false` when the verifier is disabled. **Rationale:** Make tamper, corruption, availability, and handoff state observable and verifiable. | `Request::Plain` + `Method::GetAuditVerifierStatus` | daemon integrity/audit/HA services and durable store; reads verification state or launches bounded local integrity/audit work | audit verification, integrity sweeps, HA status, diagnosis, and release evidence | portable client surface; implemented local controls; deployment/native evidence remains separate |
| [`UploadCreate`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L468) | `pcloudc upload create &lt;LOCAL&gt; &lt;REMOTE_NAME&gt; \[--parent &lt;ID&gt;\] \[--total-bytes &lt;N&gt;\] \[--conflict error\|overwrite\|skip\|rename\]` — register a new operator-visible upload session in the daemon's in-memory registry. Daemon handler: `Request::UploadCreate`. **Rationale:** Separate byte movement from namespace intent and persist resumable state before acknowledging progress. | `Request::UploadCreate` | transfer backend, upload session registry/journal, and pCloud transfer protocol; moves bytes or controls durable upload/download session state | large files, signed downloads, range reads, resumable uploads, and progress control | portable client surface; implemented; real-service integrity/resume qualification required |
| [`UploadPause`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L471) | `pcloudc upload pause &lt;SESSION_ID&gt;` — pause an in-flight upload session. Daemon handler: `Request::UploadPause`. **Rationale:** Separate byte movement from namespace intent and persist resumable state before acknowledging progress. | `Request::UploadPause` | transfer backend, upload session registry/journal, and pCloud transfer protocol; moves bytes or controls durable upload/download session state | large files, signed downloads, range reads, resumable uploads, and progress control | portable client surface; implemented; real-service integrity/resume qualification required |
| [`UploadResume`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L474) | `pcloudc upload resume &lt;SESSION_ID&gt;` — resume a paused upload session. Daemon handler: `Request::UploadResume`. **Rationale:** Separate byte movement from namespace intent and persist resumable state before acknowledging progress. | `Request::UploadResume` | transfer backend, upload session registry/journal, and pCloud transfer protocol; moves bytes or controls durable upload/download session state | large files, signed downloads, range reads, resumable uploads, and progress control | portable client surface; implemented; real-service integrity/resume qualification required |
| [`UploadCancel`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L478) | `pcloudc upload cancel &lt;SESSION_ID&gt;` — cancel an upload session (non-terminal → Cancelled). Daemon handler: `Request::UploadCancel`. **Rationale:** Separate byte movement from namespace intent and persist resumable state before acknowledging progress. | `Request::UploadCancel` | transfer backend, upload session registry/journal, and pCloud transfer protocol; moves bytes or controls durable upload/download session state | large files, signed downloads, range reads, resumable uploads, and progress control | portable client surface; implemented; real-service integrity/resume qualification required |
| [`UploadList`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L481) | `pcloudc upload list` — enumerate every upload session known to the running daemon. Daemon handler: `Request::UploadList`. **Rationale:** Separate byte movement from namespace intent and persist resumable state before acknowledging progress. | `Request::UploadList` | transfer backend, upload session registry/journal, and pCloud transfer protocol; moves bytes or controls durable upload/download session state | large files, signed downloads, range reads, resumable uploads, and progress control | portable client surface; implemented; real-service integrity/resume qualification required |
| [`UploadWriteFromFile`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L489) | `pcloudc upload write-from-file &lt;UPLOAD_ID&gt; &lt;SOURCE_FILEID&gt; &lt;SOURCE_HASH&gt; &lt;UPLOAD_OFFSET&gt; &lt;SOURCE_OFFSET&gt; &lt;COUNT&gt;` — drive a server-side copy of `&lt;COUNT&gt;` bytes from `(SOURCE_FILEID, SOURCE_HASH)` into the open upload session `&lt;UPLOAD_ID&gt;` at offset `&lt;UPLOAD_OFFSET&gt;`, reading fr… **Rationale:** Separate byte movement from namespace intent and persist resumable state before acknowledging progress. | `Request::UploadWriteFromFile` | transfer backend, upload session registry/journal, and pCloud transfer protocol; moves bytes or controls durable upload/download session state | large files, signed downloads, range reads, resumable uploads, and progress control | portable client surface; implemented; real-service integrity/resume qualification required |
| [`ConflictList`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L493) | `pcloudc conflict list` — list unresolved sync conflicts queued in the engine scheduler. Daemon handler: \[`pcloud_ipc::Method::ListConflicts`\]. **Rationale:** Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. | `Request::ConflictList` | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | continuous synchronization, selective sync, pause/resume, conflict handling, and recovery | portable client surface; evolving implemented product surface |
| [`ConflictResolve`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L497) | `pcloudc conflict resolve &lt;path&gt; --prefer-local\|--prefer-remote\| --newest-wins\|--rename-both` — manually resolve a specific conflict. Daemon handler: `Request::ConflictResolve`. **Rationale:** Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. | `Request::ConflictResolve` | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | continuous synchronization, selective sync, pause/resume, conflict handling, and recovery | portable client surface; evolving implemented product surface |
| [`CryptoReset`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L501) | `pcloudc crypto reset` — wipe local crypto fingerprint and folder registry. Daemon handler: \[`pcloud_ipc::Method::CryptoReset`\]. **Rationale:** Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. | `Request::Plain` + `Method::CryptoReset` | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset | portable client surface; implemented code; interoperability/provider/native evidence is separate |
| [`CryptoPrivKeyFlags`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L505) | `pcloudc crypto priv-key-flags` — return current crypto private-key flags as a decimal integer. Daemon handler: \[`pcloud_ipc::Method::GetCryptoPrivKeyFlags`\]. **Rationale:** Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. | `Request::Plain` + `Method::GetCryptoPrivKeyFlags` | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset | portable client surface; implemented code; interoperability/provider/native evidence is separate |
| [`CryptoSendChangePrivate`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L509) | `pcloudc crypto send-change-private` — request a server-side code to authorise a subsequent crypto password rotation. Daemon handler: \[`pcloud_ipc::Method::SendCryptoChangeUserPrivate`\]. **Rationale:** Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. | `Request::Plain` + `Method::SendCryptoChangeUserPrivate` | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset | portable client surface; implemented code; interoperability/provider/native evidence is separate |
| [`CryptoChangePassword`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L514) | `pcloudc crypto change-password` — rotate the crypto passphrase. Requires old + new password + confirmation code (from `send-change-private`). Daemon handler: `Request::CryptoChangePassword`. **Rationale:** Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. | `Request::CryptoChangePassword` | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset | portable client surface; implemented code; interoperability/provider/native evidence is separate |
| [`CryptoChangePasswordUnlocked`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L518) | `pcloudc crypto change-password-unlocked` — rotate the crypto passphrase when the shell is already unlocked. Daemon handler: `Request::CryptoChangePasswordUnlocked`. **Rationale:** Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. | `Request::CryptoChangePasswordUnlocked` | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset | portable client surface; implemented code; interoperability/provider/native evidence is separate |
| [`CryptoHint`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L521) | `pcloudc crypto hint` — fetch the stored crypto passphrase hint. Daemon handler: \[`pcloud_ipc::Method::GetCryptoHint`\]. **Rationale:** Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. | `Request::Plain` + `Method::GetCryptoHint` | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset | portable client surface; implemented code; interoperability/provider/native evidence is separate |
| [`SyncSuggest`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L525) | `pcloudc sync suggest \[&lt;PATH&gt;\] \[--max N\]` — list candidate sync folders under PATH. Daemon handler: `Request::GetSyncSuggestions`. **Rationale:** Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. | `Request::GetSyncSuggestions` | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | continuous synchronization, selective sync, pause/resume, conflict handling, and recovery | portable client surface; evolving implemented product surface |
| [`SyncIsSyncable`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L528) | `pcloudc sync is-syncable &lt;PATH&gt;` — classify whether PATH can be added as a sync root. Daemon handler: `Request::IsFolderSyncable`. **Rationale:** Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. | `Request::IsFolderSyncable` | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | continuous synchronization, selective sync, pause/resume, conflict handling, and recovery | portable client surface; evolving implemented product surface |
| [`AccountVerifyEmail`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L533) | `pcloudc account verify-email` — trigger a server-side verification email for the active session. Daemon handler: \[`pcloud_ipc::Method::VerifyEmail`\]. **Rationale:** Keep account administration separate from authentication and file operations. | `Request::Plain` + `Method::VerifyEmail` | account backend, configuration/settings store, and typed pCloud account protocol; reads or mutates account/profile/API-region/language/value state | registration, verification, password recovery/change, preferences, region selection, and promotion data | portable client surface; implemented API paths; destructive operations require isolated live accounts |
| [`AccountVerifyEmailRestricted`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L537) | `pcloudc account verify-email-restricted &lt;TOKEN&gt;` — verify email via a restricted verify-token (no session auth required). Daemon handler: `Request::VerifyEmailRestricted`. **Rationale:** Keep account administration separate from authentication and file operations. | `Request::VerifyEmailRestricted` | account backend, configuration/settings store, and typed pCloud account protocol; reads or mutates account/profile/API-region/language/value state | registration, verification, password recovery/change, preferences, region selection, and promotion data | portable client surface; implemented API paths; destructive operations require isolated live accounts |
| [`AccountLostPassword`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L540) | `pcloudc account lost-password &lt;EMAIL&gt;` — send a password-reset email. No auth required. Daemon handler: `Request::LostPassword`. **Rationale:** Keep account administration separate from authentication and file operations. | `Request::LostPassword` | account backend, configuration/settings store, and typed pCloud account protocol; reads or mutates account/profile/API-region/language/value state | registration, verification, password recovery/change, preferences, region selection, and promotion data | portable client surface; implemented API paths; destructive operations require isolated live accounts |
| [`AccountChangePassword`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L544) | `pcloudc account change-password` — change the account password. Reads current + new password interactively. Daemon handler: `Request::AccountChangePassword`. **Rationale:** Keep account administration separate from authentication and file operations. | `Request::AccountChangePassword` | account backend, configuration/settings store, and typed pCloud account protocol; reads or mutates account/profile/API-region/language/value state | registration, verification, password recovery/change, preferences, region selection, and promotion data | portable client surface; implemented API paths; destructive operations require isolated live accounts |
| [`AccountRegister`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L547) | `pcloudc account register &lt;EMAIL&gt; \[--accept-terms\]` — register a new pCloud account. Daemon handler: `Request::AccountRegister`. **Rationale:** Keep account administration separate from authentication and file operations. | `Request::AccountRegister` | account backend, configuration/settings store, and typed pCloud account protocol; reads or mutates account/profile/API-region/language/value state | registration, verification, password recovery/change, preferences, region selection, and promotion data | portable client surface; implemented API paths; destructive operations require isolated live accounts |
| [`AccountApiServers`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L550) | `pcloudc account api-servers` — list available pCloud API server regions. Daemon handler: \[`pcloud_ipc::Method::GetApiServers`\]. **Rationale:** Keep account administration separate from authentication and file operations. | `Request::Plain` + `Method::GetApiServers` | account backend, configuration/settings store, and typed pCloud account protocol; reads or mutates account/profile/API-region/language/value state | registration, verification, password recovery/change, preferences, region selection, and promotion data | portable client surface; implemented API paths; destructive operations require isolated live accounts |
| [`AccountSetApiServer`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L554) | `pcloudc account set-api-server &lt;LOCATION_ID&gt; &lt;BINAPI&gt;` — pin the daemon to a specific API region. Daemon handler: `Request::SetApiServer`. **Rationale:** Keep account administration separate from authentication and file operations. | `Request::SetApiServer` | account backend, configuration/settings store, and typed pCloud account protocol; reads or mutates account/profile/API-region/language/value state | registration, verification, password recovery/change, preferences, region selection, and promotion data | portable client surface; implemented API paths; destructive operations require isolated live accounts |
| [`AccountSetLanguage`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L557) | `pcloudc account set-language &lt;LANG&gt;` — set the account language preference. Daemon handler: `Request::SetLanguage`. **Rationale:** Keep account administration separate from authentication and file operations. | `Request::SetLanguage` | account backend, configuration/settings store, and typed pCloud account protocol; reads or mutates account/profile/API-region/language/value state | registration, verification, password recovery/change, preferences, region selection, and promotion data | portable client surface; implemented API paths; destructive operations require isolated live accounts |
| [`AccountPromo`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L560) | `pcloudc account promo` — fetch the promotional URL for this platform. Daemon handler: \[`pcloud_ipc::Method::GetPromo`\]. **Rationale:** Keep account administration separate from authentication and file operations. | `Request::Plain` + `Method::GetPromo` | account backend, configuration/settings store, and typed pCloud account protocol; reads or mutates account/profile/API-region/language/value state | registration, verification, password recovery/change, preferences, region selection, and promotion data | portable client surface; implemented API paths; destructive operations require isolated live accounts |
| [`DownloadLink`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L564) | `pcloudc download link &lt;FILE_ID&gt;` — resolve the download URL for a remote file. Daemon handler: `Request::GetFileLink`. **Rationale:** Separate byte movement from namespace intent and persist resumable state before acknowledging progress. | `Request::GetFileLink` | transfer backend, upload session registry/journal, and pCloud transfer protocol; moves bytes or controls durable upload/download session state | large files, signed downloads, range reads, resumable uploads, and progress control | portable client surface; implemented; real-service integrity/resume qualification required |
| [`DownloadFile`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L567) | `pcloudc download file &lt;FILE_ID&gt; &lt;LOCAL_PATH&gt;` — download a remote file to a local path. Daemon handler: `Request::DownloadFile`. **Rationale:** Separate byte movement from namespace intent and persist resumable state before acknowledging progress. | `Request::DownloadFile` | transfer backend, upload session registry/journal, and pCloud transfer protocol; moves bytes or controls durable upload/download session state | large files, signed downloads, range reads, resumable uploads, and progress control | portable client surface; implemented; real-service integrity/resume qualification required |
| [`BackupDelete`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L571) | `pcloudc backup delete &lt;BACKUP_ID&gt;` — delete a backup by folder id. Daemon handler: `Request::DeleteBackup`. **Rationale:** Keep archival/device lifecycle distinct from bidirectional sync and make recovery evidence executable. | `Request::DeleteBackup` | backup backend, snapshot helpers, store, and transfer path; creates, reports, stops, verifies, restores, or removes backup/device/snapshot state | device backup, portable snapshots, retention, verification, and disaster recovery | portable client surface; implemented components; destructive/live/restore drills required |
| [`BackupCreate`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L575) | `pcloudc backup create &lt;NAME&gt; &lt;ROOT_FOLDER_ID&gt; &lt;LOCAL_PATH&gt;` — create a new backup and register the local folder as an upload-only sync root. Daemon handler: `Request::CreateBackup`. **Rationale:** Keep archival/device lifecycle distinct from bidirectional sync and make recovery evidence executable. | `Request::CreateBackup` | backup backend, snapshot helpers, store, and transfer path; creates, reports, stops, verifies, restores, or removes backup/device/snapshot state | device backup, portable snapshots, retention, verification, and disaster recovery | portable client surface; implemented components; destructive/live/restore drills required |
| [`BackupStopDevice`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L579) | `pcloudc backup stop-device &lt;DEVICE_FOLDER_ID&gt;` — stop a device backup and remove the matching local sync root. Daemon handler: `Request::StopDevice`. **Rationale:** Keep archival/device lifecycle distinct from bidirectional sync and make recovery evidence executable. | `Request::StopDevice` | backup backend, snapshot helpers, store, and transfer path; creates, reports, stops, verifies, restores, or removes backup/device/snapshot state | device backup, portable snapshots, retention, verification, and disaster recovery | portable client surface; implemented components; destructive/live/restore drills required |
| [`BackupDeleteDevice`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L583) | `pcloudc backup delete-device` — clear the local device backup registration (local-only, no network call). Daemon handler: `Request::DeleteBackupDevice`. **Rationale:** Keep archival/device lifecycle distinct from bidirectional sync and make recovery evidence executable. | `Request::DeleteBackupDevice` | backup backend, snapshot helpers, store, and transfer path; creates, reports, stops, verifies, restores, or removes backup/device/snapshot state | device backup, portable snapshots, retention, verification, and disaster recovery | portable client surface; implemented components; destructive/live/restore drills required |
| [`CreateTreeLinkFromPaths`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L589) | `pcloudc create-tree-link-from-paths &lt;NAME&gt; \[--root PATH\] \[--folder PATH\] \[--file PATH\]...` — create a tree link by resolving root/folder/file paths to ids via the daemon path resolver. Bare paths remain folder targets for compatibility. Daemon handler: `Request::CreateTreePubli… **Rationale:** Represent public exposure as typed, revocable objects with explicit expiry/access policy. | `Request::CreateTreePublicLinkFromPathTargets` | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | publishing files/folders, upload drop boxes, link hygiene, and controlled recipients | portable client surface; implemented API paths; live account qualification required |
| [`CryptoSetupV2`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L600) | `pcloudc crypto setup \[--backend {pclsync-compat\|enhanced}\] \[--acknowledge-not-interop\] \[--hint &lt;TEXT&gt;\]` — set up the crypto profile using the selected backend. If `--backend` is absent and stdin is a tty the CLI drops into an interactive picker; if stdin is not a tty the parser… **Rationale:** Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. | `Request::CryptoSetupV2` | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset | portable client surface; implemented code; interoperability/provider/native evidence is separate |
| [`CryptoGetFolderKey`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L604) | `pcloudc crypto get-folder-key &lt;FOLDER_ID&gt;` — thin pass-through dispatching \[`pcloud_ipc::Request::CryptoGetFolderKey`\]. Primarily used for testing / debugging the crypto cache-population path. **Rationale:** Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. | `Request::CryptoGetFolderKey` | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset | portable client surface; implemented code; interoperability/provider/native evidence is separate |
| [`CryptoGetFileKey`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-cli/src/commands.rs#L608) | `pcloudc crypto get-file-key &lt;FILE_ID&gt;` — thin pass-through dispatching \[`pcloud_ipc::Request::CryptoGetFileKey`\]. Primarily used for testing / debugging the crypto cache-population path. **Rationale:** Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. | `Request::CryptoGetFileKey` | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset | portable client surface; implemented code; interoperability/provider/native evidence is separate |

## IPC `Method` (44)

Argumentless operations travel as `Request::Plain { method }`.

| Method | Full behavior | Why it exists / good for | Owner and side effect | Direct client exposure | pcloudd reachability |
|---|---|---|---|---|---|
| [`GetStatus`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L42) | Return a short, human-readable daemon status line (`running \| paused \| ...`). Mirrors `pcloud-rs status`. | Make process health, lifecycle, and operator control explicit instead of inferring them from a PID. Good for supervision, diagnosis, graceful maintenance, and automation. | daemon runtime and observability; reads process/queue/health state or changes local lifecycle state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L475) |
| [`GetHealth`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L46) | Basic liveness probe. Returns `ok` when the daemon is running; callers who need structured diagnostics should use \[`Method::Health`\] instead. | Make process health, lifecycle, and operator control explicit instead of inferring them from a PID. Good for supervision, diagnosis, graceful maintenance, and automation. | daemon runtime and observability; reads process/queue/health state or changes local lifecycle state | CLI, embedded SDK | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L460) |
| [`Health`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L51) | Enterprise health probe: returns daemon build info, uptime, the last event summary, and — when the `metrics` feature is enabled — a Prometheus text-format snapshot of the metric families. Distinct from `GetHealth` (which returns a short human summary). | Make process health, lifecycle, and operator control explicit instead of inferring them from a PID. Good for supervision, diagnosis, graceful maintenance, and automation. | daemon runtime and observability; reads process/queue/health state or changes local lifecycle state | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L461) |
| [`GetPending`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L53) | List pending transfers currently queued by the engine. | Make process health, lifecycle, and operator control explicit instead of inferring them from a PID. Good for supervision, diagnosis, graceful maintenance, and automation. | daemon runtime and observability; reads process/queue/health state or changes local lifecycle state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L509) |
| [`GetSyncRoots`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L55) | List all registered sync roots. | Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. Good for continuous synchronization, selective sync, pause/resume, conflict handling, and recovery. | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L510) |
| [`ListPublicLinks`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L57) | List active public links created by this account. | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L511) |
| [`ListUploadLinks`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L59) | List active upload-only links. | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L512) |
| [`GetUserInfo`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L62) | Return the authenticated user's account summary (quota, email, user id, plan). | Keep account administration separate from authentication and file operations. Good for registration, verification, password recovery/change, preferences, region selection, and promotion data. | account backend, configuration/settings store, and typed pCloud account protocol; reads or mutates account/profile/API-region/language/value state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L513) |
| [`PauseSync`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L65) | Pause the sync engine globally. Existing transfers are drained; new work is held until a matching \[`Method::ResumeSync`\]. | Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. Good for continuous synchronization, selective sync, pause/resume, conflict handling, and recovery. | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L514) |
| [`ResumeSync`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L67) | Resume a previously paused sync engine. | Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. Good for continuous synchronization, selective sync, pause/resume, conflict handling, and recovery. | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L515) |
| [`LoginBegin`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L71) | Begin an interactive login flow: the daemon prepares the auth state machine and advertises which credential submission is expected next. | Keep secret submission and session transitions inside one typed state machine. Good for login, TFA, refresh, persistence opt-in, logout, and session diagnosis. | pcloud-auth, session, auth backend, and token vault; changes or reports the authenticated session and may call pCloud auth APIs or the local vault | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L516) |
| [`Logout`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L74) | Log the authenticated session out and destroy any in-memory credential material. Persisted tokens (opt-in) are also removed. | Keep secret submission and session transitions inside one typed state machine. Good for login, TFA, refresh, persistence opt-in, logout, and session diagnosis. | pcloud-auth, session, auth backend, and token vault; changes or reports the authenticated session and may call pCloud auth APIs or the local vault | CLI, embedded SDK | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L526) |
| [`SendTwoFactorSms`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L76) | Request a TFA code delivery via SMS. | Keep secret submission and session transitions inside one typed state machine. Good for login, TFA, refresh, persistence opt-in, logout, and session diagnosis. | pcloud-auth, session, auth backend, and token vault; changes or reports the authenticated session and may call pCloud auth APIs or the local vault | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L527) |
| [`SendTwoFactorNotification`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L78) | Request a TFA push notification to the account's trusted device. | Keep secret submission and session transitions inside one typed state machine. Good for login, TFA, refresh, persistence opt-in, logout, and session diagnosis. | pcloud-auth, session, auth backend, and token vault; changes or reports the authenticated session and may call pCloud auth APIs or the local vault | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L528) |
| [`SubmitPassword`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L81) | Submit a previously staged password (via \[`Request::PasswordSubmission`\]) to the auth state machine. | Make process health, lifecycle, and operator control explicit instead of inferring them from a PID. Good for supervision, diagnosis, graceful maintenance, and automation. | daemon runtime and observability; reads process/queue/health state or changes local lifecycle state | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L585) |
| [`SubmitTwoFactorCode`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L84) | Submit a previously staged two-factor code (via \[`Request::TwoFactorCodeSubmission`\]) to the auth state machine. | Keep secret submission and session transitions inside one typed state machine. Good for login, TFA, refresh, persistence opt-in, logout, and session diagnosis. | pcloud-auth, session, auth backend, and token vault; changes or reports the authenticated session and may call pCloud auth APIs or the local vault | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L585) |
| [`UnlockCrypto`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L87) | Unlock an already-set-up crypto shell with the passphrase from a prior \[`Request::CryptoUnlock`\]. | Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. Good for Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset. | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L531) |
| [`LockCrypto`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L89) | Lock the crypto shell and zero in-memory key material. | Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. Good for Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset. | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L541) |
| [`GetCryptoStatus`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L91) | Report crypto setup / started / folder-count state. | Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. Good for Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset. | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L539) |
| [`CryptoReset`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L93) | Reset crypto: wipes local fingerprint and folder registry. | Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. Good for Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset. | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L540) |
| [`GetCryptoPrivKeyFlags`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L97) | Read the current crypto private-key flags (mirrors `psync_crypto_priv_key_flags`). The flags value is returned in the response message as a decimal integer. | Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. Good for Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset. | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L542) |
| [`SendCryptoChangeUserPrivate`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L101) | Request a server-side confirmation code to authorize a subsequent crypto password-change (mirrors `psync_crypto_crypto_send_change_user_private`). | Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. Good for Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset. | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L543) |
| [`Shutdown`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L104) | Cleanly shut down the daemon. All in-flight transfers are drained, caches flushed, and the IPC socket is removed. | Make process health, lifecycle, and operator control explicit instead of inferring them from a PID. Good for supervision, diagnosis, graceful maintenance, and automation. | daemon runtime and observability; reads process/queue/health state or changes local lifecycle state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L65) |
| [`SetAuthPersistence`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L107) | Toggle durable auth-token persistence per the flag carried in \[`Request::AuthPersistence`\]. | Keep secret submission and session transitions inside one typed state machine. Good for login, TFA, refresh, persistence opt-in, logout, and session diagnosis. | pcloud-auth, session, auth backend, and token vault; changes or reports the authenticated session and may call pCloud auth APIs or the local vault | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L535) |
| [`ListIncomingShares`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L109) | List pending share invitations the user has received. | Preserve each collaboration lifecycle and permission transition as an auditable typed operation. Good for person-to-person sharing, team administration, invitations, and access reviews. | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L547) |
| [`ListOutgoingShares`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L111) | List pending share invitations the user has sent. | Preserve each collaboration lifecycle and permission transition as an auditable typed operation. Good for person-to-person sharing, team administration, invitations, and access reviews. | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L548) |
| [`ListIncomingShareRequests`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L113) | List *accepted* incoming share requests. | Preserve each collaboration lifecycle and permission transition as an auditable typed operation. Good for person-to-person sharing, team administration, invitations, and access reviews. | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L549) |
| [`ListOutgoingShareRequests`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L115) | List *accepted* outgoing share requests. | Preserve each collaboration lifecycle and permission transition as an auditable typed operation. Good for person-to-person sharing, team administration, invitations, and access reviews. | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L550) |
| [`ListContacts`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L117) | List the user's address-book contacts. | Preserve each collaboration lifecycle and permission transition as an auditable typed operation. Good for person-to-person sharing, team administration, invitations, and access reviews. | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L551) |
| [`ListMyTeams`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L119) | List the business teams the user belongs to. | Preserve each collaboration lifecycle and permission transition as an auditable typed operation. Good for person-to-person sharing, team administration, invitations, and access reviews. | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L552) |
| [`ListNotifications`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L122) | List pending account notifications. Mirrors C `psync_get_notifications` (pclsync/psynclib.c:248). | Expose server-side events without coupling notification state to UI code. Good for alerts, inbox workflows, plugins, and automation. | notifications backend and pCloud notification protocol; reads notification state or advances its remote read cursor | CLI, embedded SDK | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L553) |
| [`SessionStatus`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L127) | Report session lifecycle status. See `Request::SessionStatus`-style documentation on the companion `Request` variant. Added as a `Method` variant so the daemon's `Plain { method }` dispatch can resolve it alongside other status probes. | Keep secret submission and session transitions inside one typed state machine. Good for login, TFA, refresh, persistence opt-in, logout, and session diagnosis. | pcloud-auth, session, auth backend, and token vault; changes or reports the authenticated session and may call pCloud auth APIs or the local vault | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L202) |
| [`IntegrityStatus`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L132) | H14 PR4 — return the background-integrity-sweeper progress as JSON in \[`Response::message`\]. Payload is a \[`IntegrityStatusPayload`\]. Always safe to call; returns zero progress when the sweeper has never run. Tracker: bd-1du.4.6.1. | Make tamper, corruption, availability, and handoff state observable and verifiable. Good for audit verification, integrity sweeps, HA status, diagnosis, and release evidence. | daemon integrity/audit/HA services and durable store; reads verification state or launches bounded local integrity/audit work | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L556) |
| [`HaStatus`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L140) | Tier-2 HA status probe. Returns a JSON-serialised `HaStatusPayload` (`{mode, lease_owner, lease_age_s, HA is disabled the payload simply reports `mode = "disabled"`. Rendered by `pcloudc ha status`. See `docs/enterprise/ha.md` §4.2 (Tier 2) and `crates/pcloud-daemon/src/ha_lease… | Make tamper, corruption, availability, and handoff state observable and verifiable. Good for audit verification, integrity sweeps, HA status, diagnosis, and release evidence. | daemon integrity/audit/HA services and durable store; reads verification state or launches bounded local integrity/audit work | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L459) |
| [`DrainStatus`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L151) | Report the daemon's current drain state. JSON payload in \[`Response::message`\] follows \[`DrainStatusPayload`\]: `{state: "running"\|"draining"\|"stopped", in_flight: N, Always safe to call, including while the daemon is draining — the drain gate explicitly admits this method so ope… | Make process health, lifecycle, and operator control explicit instead of inferring them from a PID. Good for supervision, diagnosis, graceful maintenance, and automation. | daemon runtime and observability; reads process/queue/health state or changes local lifecycle state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L570) |
| [`GetSlo`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L159) | Return the canonical Service-Level Objective report as a JSON \[`SloReportPayload`\] in \[`Response::message`\]. Always safe to call; SLOs without enough observations are reported with `status = "no_data"` so callers do not conflate "quiet" with "healthy". Rendered by `pcloudc slo`.… | Make process health, lifecycle, and operator control explicit instead of inferring them from a PID. Good for supervision, diagnosis, graceful maintenance, and automation. | daemon runtime and observability; reads process/queue/health state or changes local lifecycle state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L393) |
| [`GetAuditVerifierStatus`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L166) | Return the scheduled audit-chain verifier status as a JSON \[`AuditVerifierStatusPayload`\] in \[`Response::message`\]. Always safe to call; when the verifier is disabled the payload reports `enabled = false` and `last_result = "never_run"`. Rendered by `pcloudc audit-verifier statu… | Make tamper, corruption, availability, and handoff state observable and verifiable. Good for audit verification, integrity sweeps, HA status, diagnosis, and release evidence. | daemon integrity/audit/HA services and durable store; reads verification state or launches bounded local integrity/audit work | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L214) |
| [`GetSyncStatus`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L173) | Return the background sync loop status as a JSON payload in \[`Response::message`\]. Reports the loop state (`running\|paused\|idle\|disabled`), active root count, last cycle timestamp, duration, and pending transfer counts. Always safe to call; returns `disabled` when the sync loop… | Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. Good for continuous synchronization, selective sync, pause/resume, conflict handling, and recovery. | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L575) |
| [`ListConflicts`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L178) | List unresolved sync conflicts from the engine scheduler. Returns a JSON array of \[`ConflictEntry`\] in \[`Response::message`\]. Always safe to call; returns an empty array when no conflicts are queued. Rendered by `pcloudc conflict list`. | Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. Good for continuous synchronization, selective sync, pause/resume, conflict handling, and recovery. | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L576) |
| [`StatPath`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L186) | Stat an absolute pCloud-drive path against the local metadata cache populated by the diff engine, with API fallback. Mirrors C `psync_stat_path` (`pclsync/psynclib.h:743`, `pfolder.c:734`). Response message is a JSON \[`StatPathPayload`\]. Arguments are carried on \[`Request::StatP… | Give every drive-like consumer one cache-independent, ID-first namespace contract. Good for stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access. | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L577) |
| [`GetApiServers`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L191) | Return the list of available pCloud API server regions. Mirrors C `psync_get_api_servers`. No auth required. Response is a JSON array of `{label, api, binapi, location_id}` objects in \[`Response::message`\]. | Keep account administration separate from authentication and file operations. Good for registration, verification, password recovery/change, preferences, region selection, and promotion data. | account backend, configuration/settings store, and typed pCloud account protocol; reads or mutates account/profile/API-region/language/value state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L581) |
| [`GetPromo`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L196) | Fetch the promotional URL for this platform. Mirrors C `psync_get_promo`. Requires an authenticated session. Response is a JSON `{url,width,height}` object in \[`Response::message`\], or `"no promo"` when `haspromo` is false. | Keep account administration separate from authentication and file operations. Good for registration, verification, password recovery/change, preferences, region selection, and promotion data. | account backend, configuration/settings store, and typed pCloud account protocol; reads or mutates account/profile/API-region/language/value state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L582) |
| [`GetCryptoHint`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L200) | Fetch the crypto passphrase hint stored at first-time setup. Mirrors C `psync_crypto_hint`. Requires crypto to be set up; returns the hint string in \[`Response::message`\]. | Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. Good for Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset. | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L583) |
| [`VerifyEmail`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L204) | Trigger a server-side verification email send for the active authenticated session. Mirrors C `psync_verify_email`. Requires an authenticated session. | Keep account administration separate from authentication and file operations. Good for registration, verification, password recovery/change, preferences, region selection, and promotion data. | account backend, configuration/settings store, and typed pCloud account protocol; reads or mutates account/profile/API-region/language/value state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L584) |

## IPC `Request` (113)

Argument-bearing requests are the complete current local wire vocabulary.
Secret-bearing variants use redacted transit wrappers and the whole enum has a
manual redacted `Debug`; local IPC serialization is still a real trust boundary.

| Request | Full behavior | Why it exists / good for | Owner and side effect | Direct client exposure | pcloudd reachability |
|---|---|---|---|---|---|
| [`Plain`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L254) | Argumentless dispatch wrapper: every \[`Method`\] variant that does not carry parameters is sent as `Plain { method: … }`. | Make process health, lifecycle, and operator control explicit instead of inferring them from a PID. Good for supervision, diagnosis, graceful maintenance, and automation. | daemon runtime and observability; reads process/queue/health state or changes local lifecycle state | CLI, embedded SDK | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L458) |
| [`PasswordSubmission`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L261) | Stage a username+password pair for later \[`Method::SubmitPassword`\]. See the audit H1 note above — `value` is intentionally a transit-only `String`. | Keep secret submission and session transitions inside one typed state machine. Good for login, TFA, refresh, persistence opt-in, logout, and session diagnosis. | pcloud-auth, session, auth backend, and token vault; changes or reports the authenticated session and may call pCloud auth APIs or the local vault | CLI, embedded SDK | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L599) |
| [`AuthTokenSubmission`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L271) | Stage a pre-obtained pCloud API auth token for persistence / use. | Keep secret submission and session transitions inside one typed state machine. Good for login, TFA, refresh, persistence opt-in, logout, and session diagnosis. | pcloud-auth, session, auth backend, and token vault; changes or reports the authenticated session and may call pCloud auth APIs or the local vault | CLI, embedded SDK | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L616) |
| [`TwoFactorCodeSubmission`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L277) | Submit a TFA code (or recovery code) to the auth state machine. | Keep secret submission and session transitions inside one typed state machine. Good for login, TFA, refresh, persistence opt-in, logout, and session diagnosis. | pcloud-auth, session, auth backend, and token vault; changes or reports the authenticated session and may call pCloud auth APIs or the local vault | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L625) |
| [`CryptoUnlock`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L287) | Unlock an already-set-up crypto shell. | Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. Good for Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset. | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L663) |
| [`CryptoSetup`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L293) | First-time crypto setup. Mirrors `psync_crypto_setup`. | Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. Good for Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset. | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L666) |
| [`CryptoMkdir`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L302) | Create an encrypted folder (local bookkeeping + deterministic encrypted name). Mirrors `psync_crypto_mkdir`. | Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. Good for Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset. | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L706) |
| [`CryptoChangePassword`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L322) | Rotate the crypto passphrase. Mirrors `psync_crypto_change_crypto_pass`. The shell must be set up but may be locked — the old password is checked in constant time. `code` is the confirmation code delivered via \[`Method::SendCryptoChangeUserPrivate`\]. `flags` mirrors the `PSYNC_C… | Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. Good for Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset. | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L682) |
| [`CryptoChangePasswordUnlocked`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L340) | Rotate the crypto passphrase without re-supplying the old one. Only valid when the shell is already unlocked. Mirrors `psync_crypto_change_crypto_pass_unlocked`. | Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. Good for Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset. | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L695) |
| [`AuthPersistence`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L352) | Toggle durable auth-token persistence. Secure default is `false`. | Keep secret submission and session transitions inside one typed state machine. Good for login, TFA, refresh, persistence opt-in, logout, and session diagnosis. | pcloud-auth, session, auth backend, and token vault; changes or reports the authenticated session and may call pCloud auth APIs or the local vault | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L711) |
| [`SyncRootAdd`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L365) | Register a new sync root (local ↔ remote binding). The optional `sync_type` selects the direction flavor. When absent on the wire, the daemon defaults to \[`SyncType::Full`\] (bidirectional) to preserve wire-compatibility with pre-flavor clients that do not serialize the field. Th… | Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. Good for continuous synchronization, selective sync, pause/resume, conflict handling, and recovery. | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L712) |
| [`SyncRootRemove`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L379) | Delete an existing sync root by id. Pending transfers for the removed root are evicted from the engine queue. | Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. Good for continuous synchronization, selective sync, pause/resume, conflict handling, and recovery. | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L717) |
| [`SyncRootPause`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L385) | Pause an existing sync root without deleting it. Mirrors the paused flag used by the legacy C sync lifecycle. | Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. Good for continuous synchronization, selective sync, pause/resume, conflict handling, and recovery. | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L718) |
| [`SyncRootResume`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L390) | Resume a previously paused sync root. | Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. Good for continuous synchronization, selective sync, pause/resume, conflict handling, and recovery. | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L719) |
| [`SyncRootChangeType`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L395) | Change the sync direction (mirrors C `psync_change_synctype`). | Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. Good for continuous synchronization, selective sync, pause/resume, conflict handling, and recovery. | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L720) |
| [`SyncExcludeAdd`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L406) | T1.1 selective sync: append a glob pattern to a sync root's exclusion list. Pattern is silently ignored if it duplicates an existing entry; an empty pattern returns \[`ResponseStatus::InvalidRequest`\]. The engine consults the updated list on the next planner pass. | Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. Good for continuous synchronization, selective sync, pause/resume, conflict handling, and recovery. | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L723) |
| [`SyncExcludeRemove`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L417) | T1.1 selective sync: remove a glob pattern from a sync root's exclusion list. Returns \[`ResponseStatus::Conflict`\] if the pattern was not present. | Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. Good for continuous synchronization, selective sync, pause/resume, conflict handling, and recovery. | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L724) |
| [`SyncExcludeList`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L426) | T1.1 selective sync: list a sync root's exclusion globs. Response message is the patterns joined by `'\n'`; an empty message means no patterns are configured. | Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. Good for continuous synchronization, selective sync, pause/resume, conflict handling, and recovery. | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L727) |
| [`GetSyncSuggestions`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L434) | Scan a local directory and return a list of candidate sync folders. Mirrors the C `psync_get_sync_suggestions` helper's shape (shallow top-level summary). The underlying heuristic differs from the C extension-based scorer but gives the CLI actionable suggestions. | Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. Good for continuous synchronization, selective sync, pause/resume, conflict handling, and recovery. | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L734) |
| [`IsFolderSyncable`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L442) | Classify whether a given local path can be added as a sync root. Mirrors C `psync_is_folder_syncable`: rejects nested/duplicate roots. | Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. Good for continuous synchronization, selective sync, pause/resume, conflict handling, and recovery. | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L737) |
| [`ShowPublicLink`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L447) | Show metadata for a public link by its short share code. | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L738) |
| [`DeletePublicLink`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L452) | Delete an existing public link by id. | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L739) |
| [`DeletePublicLinkByCode`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L464) | Delete an existing public link by its short share code. Convenience variant for callers (like the CLI) that only have the code-form URL segment. The daemon resolves `code` to a numeric link id by scanning `list_public_links` and dispatches the existing delete path. This keeps by… | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L740) |
| [`CreateFilePublicLink`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L469) | Create a download link for a single file at `path`. | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L741) |
| [`CreateFolderPublicLink`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L474) | Create a download link for a folder at `path`. | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L742) |
| [`CreateFolderPublicLinkWithOptions`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L482) | Create a download link for a folder at `path` with full expire / maxdownloads / maxtraffic / password options. Mirrors C `psync_folder_public_link_full`. CLAUDEREV iter-2 H-4 fix: closes parity row 147 reachability gap. | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L743) |
| [`CreateFolderUpDownLink`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L500) | Create an up/down (invitation) link for a folder and email it to the recipient. Mirrors C `psync_folder_updownlink_link`. CLAUDEREV iter-2 H-4 fix: closes parity row 148 reachability gap. | Give every drive-like consumer one cache-independent, ID-first namespace contract. Good for stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access. | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L756) |
| [`CreateScreenshotPublicLink`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L511) | Create a screenshot public link for the file at `path`. Mirrors C `psync_screenshot_public_link`. CLAUDEREV iter-2 H-4 fix: closes parity row 168 reachability gap. | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L761) |
| [`ChangePublicLinkExpire`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L521) | Set or clear the expiry on an existing public link. | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L766) |
| [`ChangePublicLinkPassword`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L528) | Set or clear the password on an existing public link. | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L769) |
| [`ChangePublicLinkUpload`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L536) | Change the upload policy on a folder public link. | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L779) |
| [`CreateUploadLink`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L543) | Create an upload-only link pointing at a folder. | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L782) |
| [`DeleteUploadLink`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L556) | Delete an existing upload link by id. | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L789) |
| [`CreateTreePublicLink`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L561) | Create a tree link spanning multiple folders and/or files. | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L790) |
| [`ListPublicLinkAccess`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L579) | Enumerate the access entries on a public link. | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L807) |
| [`AddPublicLinkAccess`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L584) | Grant a specific email recipient access to a public link. | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L808) |
| [`RemovePublicLinkAccess`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L591) | Revoke a previously granted public-link access entry. | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L811) |
| [`ListBookmarks`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L598) | List all pinned bookmark public links. | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L815) |
| [`RemoveBookmark`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L600) | Delete a pinned bookmark by link code. | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L816) |
| [`ChangeBookmark`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L607) | Mutate a pinned bookmark's display metadata. | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L819) |
| [`ShareFolder`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L618) | Send a share invitation for a folder to a recipient email. | Preserve each collaboration lifecycle and permission transition as an auditable typed operation. Good for person-to-person sharing, team administration, invitations, and access reviews. | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | CLI, stable SDK, embedded SDK | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L825) |
| [`CryptoShareFolder`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L639) | Send a *crypto* share invitation for an encrypted folder. Mirrors C `psync_crypto_share_folder`. CLAUDEREV iter-2 H-5 fix: closes parity row 138 reachability gap. Daemon-side this dispatches through `SharesRuntime::crypto_share_folder` which performs the temppass KEK-rewrap befo… | Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. Good for Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset. | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L833) |
| [`CancelShareRequest`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L658) | Cancel a pending outgoing share request. | Preserve each collaboration lifecycle and permission transition as an auditable typed operation. Good for person-to-person sharing, team administration, invitations, and access reviews. | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L860) |
| [`DeclineShareRequest`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L663) | Decline an incoming share request. | Preserve each collaboration lifecycle and permission transition as an auditable typed operation. Good for person-to-person sharing, team administration, invitations, and access reviews. | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L863) |
| [`AcceptShareRequest`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L668) | Accept an incoming share request and attach it under a local folder. | Preserve each collaboration lifecycle and permission transition as an auditable typed operation. Good for person-to-person sharing, team administration, invitations, and access reviews. | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L866) |
| [`RemoveShare`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L677) | Remove an active share by id. | Preserve each collaboration lifecycle and permission transition as an auditable typed operation. Good for person-to-person sharing, team administration, invitations, and access reviews. | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L871) |
| [`ModifyShare`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L682) | Change the permission bits on an active share. | Preserve each collaboration lifecycle and permission transition as an auditable typed operation. Good for person-to-person sharing, team administration, invitations, and access reviews. | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L872) |
| [`AccountStopShare`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L689) | Bulk-stop a set of user shares and team shares. | Preserve each collaboration lifecycle and permission transition as an auditable typed operation. Good for person-to-person sharing, team administration, invitations, and access reviews. | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L876) |
| [`AccountModifyShare`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L696) | Bulk-modify permissions on a mix of user shares and team shares. | Preserve each collaboration lifecycle and permission transition as an auditable typed operation. Good for person-to-person sharing, team administration, invitations, and access reviews. | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L880) |
| [`AccountTeamShare`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L703) | Share a folder with an entire business team. | Preserve each collaboration lifecycle and permission transition as an auditable typed operation. Good for person-to-person sharing, team administration, invitations, and access reviews. | shares/business backend and typed pCloud sharing protocol; reads or mutates user, contact, request, team, and business share state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L884) |
| [`CryptoShareFolderRsa`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L727) | RSA-4096-OAEP variant of \[`Request::CryptoShareFolder`\]. Mirrors C `psync_crypto_share_folder` on the `pclsync-v2` (full-RSA) wire shape. CLAUDEREV deferred-set D6 (fire 56): closes parity row 124 reachability gap. Daemon-side this dispatches through `SharesRuntime::crypto_share… | Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. Good for Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset. | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L850) |
| [`CryptoAccountTeamShare`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L751) | Crypto-aware variant of \[`Request::AccountTeamShare`\]. Mirrors C `psync_crypto_account_teamshare`. CLAUDEREV deferred-set D3 (fire 47): closes parity row 142 reachability gap. Daemon-side this dispatches through `SharesRuntime::crypto_account_teamshare` which performs the temppa… | Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. Good for Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset. | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L892) |
| [`ValueGet`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L772) | Typed key/value read. Mirrors the C `psync_get_{bool,int,uint,string}_value` helpers backed by the `setting` table. | Keep account administration separate from authentication and file operations. Good for registration, verification, password recovery/change, preferences, region selection, and promotion data. | account backend, configuration/settings store, and typed pCloud account protocol; reads or mutates account/profile/API-region/language/value state | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L909) |
| [`ValueSet`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L780) | Typed key/value write. Mirrors the C `psync_set_{bool,int,uint,string}_value` helpers. | Keep account administration separate from authentication and file operations. Good for registration, verification, password recovery/change, preferences, region selection, and promotion data. | account backend, configuration/settings store, and typed pCloud account protocol; reads or mutates account/profile/API-region/language/value state | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L910) |
| [`ValueHas`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L790) | Typed presence check. No direct C analogue (C callers test for non-zero reads); the Rust surface exposes a strict presence+kind match so callers do not need to round-trip. | Keep account administration separate from authentication and file operations. Good for registration, verification, password recovery/change, preferences, region selection, and promotion data. | account backend, configuration/settings store, and typed pCloud account protocol; reads or mutates account/profile/API-region/language/value state | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L911) |
| [`SessionStatus`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L805) | Report session lifecycle status — expiry timestamp, last observed activity, and whether a proactive refresh is currently in flight. Mirrors the timing state tracked by `pcloud_auth::SessionLifecycle` / `RefreshGuard`. The daemon serializes a \[`SessionStatusPayload`\] into \[`Respo… | Keep secret submission and session transitions inside one typed state machine. Good for login, TFA, refresh, persistence opt-in, logout, and session diagnosis. | pcloud-auth, session, auth backend, and token vault; changes or reports the authenticated session and may call pCloud auth APIs or the local vault | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L923) |
| [`MarkNotificationsRead`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L810) | Mark all account notifications up to and including `upto_id` as read. Mirrors C `psync_mark_notificaitons_read` (sic - pclsync/psynclib.c:324). The Rust identifier uses the corrected spelling; the wire command (`readnotifications`) preserves the C contract verbatim. | Expose server-side events without coupling notification state to UI code. Good for alerts, inbox workflows, plugins, and automation. | notifications backend and pCloud notification protocol; reads notification state or advances its remote read cursor | CLI, embedded SDK | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L912) |
| [`AuditVerifyChain`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L818) | Verify the tamper-evident audit chain over the optional id range. `from` defaults to the genesis row; `to` defaults to the latest row. Returns `Ok` with a short summary, or `InternalError` with the first-broken-entry detail on mismatch. | Make tamper, corruption, availability, and handoff state observable and verifiable. Good for audit verification, integrity sweeps, HA status, diagnosis, and release evidence. | daemon integrity/audit/HA services and durable store; reads verification state or launches bounded local integrity/audit work | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L913) |
| [`Mount`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L827) | Mount the pCloud filesystem at `path`. Mirrors the mounted-drive behaviour of the legacy C client. The daemon validates that the mountpoint exists, is owned by the daemon uid, and is not world-writable before handing off to the FUSE layer. `allow_other` is always rejected (secur… | Adapt the canonical remote namespace to kernels without creating a second source of truth. Good for desktop drive letters/mountpoints, filesystem applications, cleanup, and diagnostics. | daemon mount runtime and pcloud-fs adapters; changes native mount state or inspects local filesystem integration | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L914) |
| [`CreateRemoteFolder`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L839) | Create a remote folder. When `parent_folder_id` is `Some`, the daemon creates the leaf folder under that parent (mirrors C `psync_create_remote_folder`). When `parent_folder_id` is `None`, the daemon resolves `path` as an absolute remote path (mirrors C `psync_create_remote_fold… | Give every drive-like consumer one cache-independent, ID-first namespace contract. Good for stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access. | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L917) |
| [`Unmount`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L855) | Unmount the active filesystem mount (if any). Triggers the drain-on-unmount hook before tearing down the FUSE session. Tracker: bd-1du.4. | Adapt the canonical remote namespace to kernels without creating a second source of truth. Good for desktop drive letters/mountpoints, filesystem applications, cleanup, and diagnostics. | daemon mount runtime and pcloud-fs adapters; changes native mount state or inspects local filesystem integration | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L915) |
| [`MountForceUnmount`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L863) | Force-unmount a specific path that the daemon does not own. Intended for recovering from an orphan mount left behind by a previous daemon process (SIGKILL, crash, out-of-order shutdown). The daemon invokes `fusermount3 -u` / `fusermount -u` on the path and returns the outcome. R… | Adapt the canonical remote namespace to kernels without creating a second source of truth. Good for desktop drive letters/mountpoints, filesystem applications, cleanup, and diagnostics. | daemon mount runtime and pcloud-fs adapters; changes native mount state or inspects local filesystem integration | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L916) |
| [`RunLocalScan`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L872) | Trigger an immediate local-scan wakeup on the engine scheduler. Mirrors C `psync_run_localscan` (`pclsync/psynclib.c:886`), which in turn calls `psync_wake_localscan`. The Rust path bumps an in-memory wake counter on `EngineShell` so callers can correlate the request with the ac… | Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. Good for continuous synchronization, selective sync, pause/resume, conflict handling, and recovery. | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | CLI, embedded SDK | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L924) |
| [`SendPublink`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L876) | Mail an existing public-link `code` to one or more recipients. Mirrors C `psync_send_publink` (`pclsync/psynclib.c:2217`); wire command `sendpublink` with `source=1`. | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | CLI, embedded SDK | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L925) |
| [`GetFolderIdByPath`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L891) | Resolve an absolute pCloud-drive path to its folder id. Mirrors C `psync_get_fsfolderid_by_path` (`pclsync/psynclib.c:2170`). The daemon surface walks the canonical drive via authenticated `listfolder` so it never fabricates the `PSYNC_INVALID_FSFOLDERID` (`0`) sentinel on miss… | Give every drive-like consumer one cache-independent, ID-first namespace contract. Good for stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access. | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L930) |
| [`GetFolderFlags`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L902) | Read folder flags / permissions / sharing / encryption view for an absolute pCloud-drive path. Mirrors C `psync_get_fsfolderflags_by_id` (`pclsync/psynclib.c:2176`) and the `flags` + `permissions` out-params of `pfs_fldr_idperm_by_path` (`pfsfolder.c:342`). Answer is serialised… | Give every drive-like consumer one cache-independent, ID-first namespace contract. Good for stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access. | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L931) |
| [`GetFolderOwnerId`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L908) | Read the owner user id of a folder by absolute pCloud-drive path. Mirrors C `psync_get_folder_ownerid` (`pclsync/psynclib.c:2088`). | Give every drive-like consumer one cache-independent, ID-first namespace contract. Good for stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access. | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L932) |
| [`FilesystemStatus`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L917) | Classify an absolute local path against the daemon's sync-root + engine state. Mirrors C `psync_filesystem_status` (`pclsync/psynclib.c:1903`). Response message is one of `INSYNC`, `INPROG`, `NOSYNC`, `INVSYNC` — identical tokens to the C `external_status_t` enum. | Adapt the canonical remote namespace to kernels without creating a second source of truth. Good for desktop drive letters/mountpoints, filesystem applications, cleanup, and diagnostics. | daemon mount runtime and pcloud-fs adapters; changes native mount state or inspects local filesystem integration | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L933) |
| [`VerifyPath`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L927) | Walk a local path and cross-check each file's SHA256 against the server-reported checksum for its mapped remote counterpart. Mirrors the R9 `pcloudc verify` CLI surface. Per-file classification is one of `OK`, `MISMATCH`, `MISSING_LOCAL`, `MISSING_REMOTE`; the daemon's response… | Make tamper, corruption, availability, and handoff state observable and verifiable. Good for audit verification, integrity sweeps, HA status, diagnosis, and release evidence. | daemon integrity/audit/HA services and durable store; reads verification state or launches bounded local integrity/audit work | CLI | <span class="atlas-experimental">no direct pcloudd runtime arm found</span> |
| [`BackupSnapshot`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L950) | Backup-snapshot lifecycle dispatch (PR1: IPC envelope + CLI parse only). The daemon-side handler (tar \| gpg pipeline, signature verify, retention sweep) lands in PR2 (H12b); the daemon currently returns \[`ResponseStatus::Unavailable`\] with a tracker pointer. `gpg_recipient` is t… | Keep archival/device lifecycle distinct from bidirectional sync and make recovery evidence executable. Good for device backup, portable snapshots, retention, verification, and disaster recovery. | backup backend, snapshot helpers, store, and transfer path; creates, reports, stops, verifies, restores, or removes backup/device/snapshot state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L973) |
| [`IntegrityRunOnce`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L983) | H14 PR4 — synchronously trigger one background-integrity-sweeper cycle. The daemon blocks until the cycle completes (or until the configured rate limiter throttles every candidate, whichever is first) and returns the post-cycle progress snapshot as JSON in \[`Response::message`\].… | Make tamper, corruption, availability, and handoff state observable and verifiable. Good for audit verification, integrity sweeps, HA status, diagnosis, and release evidence. | daemon integrity/audit/HA services and durable store; reads verification state or launches bounded local integrity/audit work | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L990) |
| [`IntegritySkip`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L989) | H14 PR4 — append `path` to the configured skip-list file and reload the sweeper's in-memory glob set. `path` is a glob pattern (e.g. `**/*.tmp`). The daemon refuses the call when no `\[features.integrity_sweeper\] skip_list_path` is configured. Tracker: bd-1du.4.6.1. | Make tamper, corruption, availability, and handoff state observable and verifiable. Good for audit verification, integrity sweeps, HA status, diagnosis, and release evidence. | daemon integrity/audit/HA services and durable store; reads verification state or launches bounded local integrity/audit work | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L991) |
| [`UploadCreate`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1004) | Register a new operator-visible upload session. The daemon allocates a monotone session id, records it in the in-memory `SessionRegistry`, and returns the id as a JSON object `{"session_id": &lt;u64&gt;, "remote_name": "&lt;effective name&gt;"}` in \[`Response::message`\]. No bytes are transm… | Separate byte movement from namespace intent and persist resumable state before acknowledging progress. Good for large files, signed downloads, range reads, resumable uploads, and progress control. | transfer backend, upload session registry/journal, and pCloud transfer protocol; moves bytes or controls durable upload/download session state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L238) |
| [`UploadPause`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1024) | Pause an in-flight upload session. Idempotent against sessions already in the paused state; rejects terminal sessions with \[`ResponseStatus::Conflict`\]. | Separate byte movement from namespace intent and persist resumable state before acknowledging progress. Good for large files, signed downloads, range reads, resumable uploads, and progress control. | transfer backend, upload session registry/journal, and pCloud transfer protocol; moves bytes or controls durable upload/download session state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L1010) |
| [`UploadResume`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1030) | Resume a paused upload session. Rejects sessions that are not currently paused. | Separate byte movement from namespace intent and persist resumable state before acknowledging progress. Good for large files, signed downloads, range reads, resumable uploads, and progress control. | transfer backend, upload session registry/journal, and pCloud transfer protocol; moves bytes or controls durable upload/download session state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L1011) |
| [`UploadCancel`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1036) | Cancel an upload session. Non-terminal → Cancelled; idempotent against already-cancelled sessions. | Separate byte movement from namespace intent and persist resumable state before acknowledging progress. Good for large files, signed downloads, range reads, resumable uploads, and progress control. | transfer backend, upload session registry/journal, and pCloud transfer protocol; moves bytes or controls durable upload/download session state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L1012) |
| [`UploadList`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1043) | Enumerate all upload sessions known to the running daemon. The daemon serialises a `Vec&lt;UploadSessionView&gt;` JSON array into \[`Response::message`\]; the CLI renders it directly. | Separate byte movement from namespace intent and persist resumable state before acknowledging progress. Good for large files, signed downloads, range reads, resumable uploads, and progress control. | transfer backend, upload session registry/journal, and pCloud transfer protocol; moves bytes or controls durable upload/download session state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L1013) |
| [`ConflictList`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1047) | List unresolved sync conflicts currently queued in the engine scheduler. Returns a JSON array of `ConflictEntry` objects in \[`Response::message`\]. | Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. Good for continuous synchronization, selective sync, pause/resume, conflict handling, and recovery. | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L1014) |
| [`ConflictResolve`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1051) | Attempt to resolve a specific conflict by path. The daemon applies the requested `policy` and either promotes the conflict to a concrete operation or returns an error. | Centralize reconciliation so CLI, mount, and background work cannot invent competing sync semantics. Good for continuous synchronization, selective sync, pause/resume, conflict handling, and recovery. | sync backend, pcloud-engine, and pcloud-store; reads or mutates sync roots, plans, conflicts, excludes, cursors, and queued work | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L1015) |
| [`StatPath`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1063) | Stat an absolute pCloud-drive path through the live canonical remote-filesystem service. Mirrors C `psync_stat_path` (`pclsync/psynclib.h:743`). | Give every drive-like consumer one cache-independent, ID-first namespace contract. Good for stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access. | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | CLI, stable SDK, embedded SDK | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L934) |
| [`ListFolderByPath`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1082) | List the direct children of a folder identified by its absolute pCloud-drive path. Returns a JSON array of \[`ListFolderEntry`\] objects in \[`Response::message`\]. Used by the smbr `pcloud` VFS plugin (see `crates/smb-vfs/src/backends/PCLOUD_PLUGIN.md` in the smbr tree) and any oth… | Give every drive-like consumer one cache-independent, ID-first namespace contract. Good for stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access. | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | CLI, stable SDK, embedded SDK | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L942) |
| [`FileDeleteByPath`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1098) | Delete a remote file identified by absolute pCloud-drive path. Resolves `path` → `file_id` through live remote metadata and dispatches `deletefile`. Mirrors C `psync_delete_file`. Authenticated. Idempotent — `ResponseStatus::Ok` on success and on "already absent". Other failure… | Give every drive-like consumer one cache-independent, ID-first namespace contract. Good for stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access. | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | embedded SDK | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L943) |
| [`FolderDeleteByPath`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1114) | Delete a remote folder identified by absolute pCloud-drive path. With `recursive = false` the daemon dispatches `deletefolder` and the API rejects a non-empty folder (mirroring POSIX `rmdir`); with `recursive = true` it dispatches `deletefolderrecursive` which removes the entire… | Give every drive-like consumer one cache-independent, ID-first namespace contract. Good for stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access. | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L944) |
| [`FolderDeleteById`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1125) | Delete a remote folder by numeric pCloud folder id. Mirrors `deletefolder` / `deletefolderrecursive`. Authenticated. Idempotent on `Folder Not Found`. | Give every drive-like consumer one cache-independent, ID-first namespace contract. Good for stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access. | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L947) |
| [`WriteFileFresh`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1155) | Atomically (re)write the full contents of a remote file at absolute pCloud-drive `path` from the supplied base64-encoded `data`. Drives `upload_create` → `upload_write` → `upload_save` server-side in a single IPC call. If the file already exists, it is overwritten in place. **Sc… | Make process health, lifecycle, and operator control explicit instead of inferring them from a PID. Good for supervision, diagnosis, graceful maintenance, and automation. | daemon runtime and observability; reads process/queue/health state or changes local lifecycle state | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L957) |
| [`ReadFileRange`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1183) | Read a byte range from a remote file by absolute pCloud-drive `path`. The daemon resolves `path` to a numeric `file_id` through live remote metadata, fetches a signed download link via `getfilelink`, and issues a single `Range: bytes=&lt;offset&gt;-&lt;offset+length-1&gt;` HTTPS GET against… | Separate byte movement from namespace intent and persist resumable state before acknowledging progress. Good for large files, signed downloads, range reads, resumable uploads, and progress control. | transfer backend, upload session registry/journal, and pCloud transfer protocol; moves bytes or controls durable upload/download session state | CLI, stable SDK, embedded SDK | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L952) |
| [`CreateFolderByPath`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1208) | Create a remote folder identified by its absolute pCloud-drive `path`. The daemon splits `path` into parent + leaf, resolves the parent through live remote metadata, and dispatches `createfolder`. Idempotent on existing folder of the same name; conflicts only when the leaf exist… | Give every drive-like consumer one cache-independent, ID-first namespace contract. Good for stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access. | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | CLI, stable SDK, embedded SDK | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L951) |
| [`RenamePath`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1228) | Rename or move a file or folder identified by its absolute pCloud-drive `from` path to its new absolute path `to`. The daemon resolves both paths to ids, decides between `renamefile` and `renamefolder` based on the source kind, and dispatches a single API call. Cross-folder move… | Give every drive-like consumer one cache-independent, ID-first namespace contract. Good for stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access. | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | CLI, stable SDK, embedded SDK | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L958) |
| [`CopyPath`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1239) | Copy a remote file or folder tree between absolute drive paths using bounded streaming reads and chunked writes. The daemon returns a JSON \[`RemoteCopyPayload`\] in \[`Response::message`\]. | Give every drive-like consumer one cache-independent, ID-first namespace contract. Good for stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access. | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | CLI, stable SDK, embedded SDK | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L959) |
| [`DeletePath`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1247) | Delete either kind of remote entry through the canonical live path resolver. Missing paths are successful no-ops. | Give every drive-like consumer one cache-independent, ID-first namespace contract. Good for stat/list/read/mkdir/copy/move/delete and CLI/SDK drive access. | canonical RemoteFs and folder/transfer backends; resolves live remote IDs and reads or mutates the pCloud namespace | CLI, stable SDK, embedded SDK | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L960) |
| [`UploadFileByPath`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1255) | Stream a local file into a remote path without embedding its contents in the IPC frame. | Separate byte movement from namespace intent and persist resumable state before acknowledging progress. Good for large files, signed downloads, range reads, resumable uploads, and progress control. | transfer backend, upload session registry/journal, and pCloud transfer protocol; moves bytes or controls durable upload/download session state | CLI, stable SDK, embedded SDK | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L961) |
| [`DownloadFileByPath`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1263) | Stream a remote file into a local destination without buffering the body in IPC or daemon memory. | Separate byte movement from namespace intent and persist resumable state before acknowledging progress. Good for large files, signed downloads, range reads, resumable uploads, and progress control. | transfer backend, upload session registry/journal, and pCloud transfer protocol; moves bytes or controls durable upload/download session state | CLI, stable SDK, embedded SDK | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L965) |
| [`LostPassword`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1273) | Send a password-reset email for the given account. Mirrors C `psync_lost_password`. No auth required. | Keep account administration separate from authentication and file operations. Good for registration, verification, password recovery/change, preferences, region selection, and promotion data. | account backend, configuration/settings store, and typed pCloud account protocol; reads or mutates account/profile/API-region/language/value state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L1016) |
| [`VerifyEmailRestricted`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1279) | Verify email using a restricted verify-token (not the session auth token). Mirrors C `psync_verify_email_restricted`. | Keep account administration separate from authentication and file operations. Good for registration, verification, password recovery/change, preferences, region selection, and promotion data. | account backend, configuration/settings store, and typed pCloud account protocol; reads or mutates account/profile/API-region/language/value state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L1017) |
| [`AccountChangePassword`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1289) | Change the account password. Requires an authenticated session. Mirrors C `psync_change_password`. Transit-only secrets; see the audit H1 note on \[`Request`\]. | Keep account administration separate from authentication and file operations. Good for registration, verification, password recovery/change, preferences, region selection, and promotion data. | account backend, configuration/settings store, and typed pCloud account protocol; reads or mutates account/profile/API-region/language/value state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L1020) |
| [`AccountRegister`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1299) | Register a new pCloud account. Mirrors C `psync_register`. No auth required. Transit-only secret field. | Keep account administration separate from authentication and file operations. Good for registration, verification, password recovery/change, preferences, region selection, and promotion data. | account backend, configuration/settings store, and typed pCloud account protocol; reads or mutates account/profile/API-region/language/value state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L1027) |
| [`GetFileLink`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1312) | Get the download link for a remote file by numeric id. Mirrors C `psync_get_file_link`. Requires an authenticated session. Response is a JSON `{hosts:\[…\], path:"…", download_tag:"…"}` object in \[`Response::message`\]. | Separate byte movement from namespace intent and persist resumable state before acknowledging progress. Good for large files, signed downloads, range reads, resumable uploads, and progress control. | transfer backend, upload session registry/journal, and pCloud transfer protocol; moves bytes or controls durable upload/download session state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L1036) |
| [`DownloadFile`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1319) | Download a remote file by numeric id to a local path. Mirrors the C download-file flow (getfilelink → HTTP fetch → write). Requires an authenticated session. | Separate byte movement from namespace intent and persist resumable state before acknowledging progress. Good for large files, signed downloads, range reads, resumable uploads, and progress control. | transfer backend, upload session registry/journal, and pCloud transfer protocol; moves bytes or controls durable upload/download session state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L965) |
| [`DeleteBackup`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1329) | Delete a backup by numeric folder id. Mirrors C `psync_delete_backup`. Requires an authenticated session. Calls `backup/stopbackup` on the server side and removes the matching local sync root when one is registered. | Keep archival/device lifecycle distinct from bidirectional sync and make recovery evidence executable. Good for device backup, portable snapshots, retention, verification, and disaster recovery. | backup backend, snapshot helpers, store, and transfer path; creates, reports, stops, verifies, restores, or removes backup/device/snapshot state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L1041) |
| [`CreateBackup`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1337) | Create a new backup for a local folder at a remote root. Mirrors C `psync_create_backup`. Requires an authenticated session. Calls `backup/createbackup` and registers the local folder as an upload-only sync root via the SyncRootCascade adapter. | Keep archival/device lifecycle distinct from bidirectional sync and make recovery evidence executable. Good for device backup, portable snapshots, retention, verification, and disaster recovery. | backup backend, snapshot helpers, store, and transfer path; creates, reports, stops, verifies, restores, or removes backup/device/snapshot state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L1075) |
| [`StopDevice`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1350) | Stop a device backup by its device folder id. Mirrors C `psync_stop_device`. Requires an authenticated session. Calls `backup/stopdevice` and removes the matching local sync root. | Keep archival/device lifecycle distinct from bidirectional sync and make recovery evidence executable. Good for device backup, portable snapshots, retention, verification, and disaster recovery. | backup backend, snapshot helpers, store, and transfer path; creates, reports, stops, verifies, restores, or removes backup/device/snapshot state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L1081) |
| [`DeleteBackupDevice`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1358) | Delete the local backup-device registration. Mirrors C `psync_delete_backup_device`. Local-only: clears the persisted device backup-root folder id so the next `CreateBackup` allocates a fresh device. Does not talk to the backend. | Keep archival/device lifecycle distinct from bidirectional sync and make recovery evidence executable. Good for device backup, portable snapshots, retention, verification, and disaster recovery. | backup backend, snapshot helpers, store, and transfer path; creates, reports, stops, verifies, restores, or removes backup/device/snapshot state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L1082) |
| [`SetApiServer`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1364) | Pin the daemon to a specific API server region. Mirrors C `psync_set_api_server`. Persists to the store and updates all live protocol transports. Requires no auth but is silently rejected (InvalidRequest) when the `data_residency` policy denies the target region. | Keep account administration separate from authentication and file operations. Good for registration, verification, password recovery/change, preferences, region selection, and promotion data. | account backend, configuration/settings store, and typed pCloud account protocol; reads or mutates account/profile/API-region/language/value state | CLI, embedded SDK | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L1042) |
| [`SetLanguage`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1372) | Set the account language preference. Mirrors C `psync_set_language`. Requires an authenticated session. | Keep account administration separate from authentication and file operations. Good for registration, verification, password recovery/change, preferences, region selection, and promotion data. | account backend, configuration/settings store, and typed pCloud account protocol; reads or mutates account/profile/API-region/language/value state | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L1046) |
| [`UploadWriteFromFile`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1384) | Server-side copy: copy bytes from a remote pCloud file (`fileid`) into an in-progress upload session. Mirrors the C `upload_writefromfile` wire primitive at `pclsync/pupload.c:843-859` (`pupload.c:1125-1131` for the caller-side chunk loop). The C params are `uploadid` / `fileid`… | Separate byte movement from namespace intent and persist resumable state before acknowledging progress. Good for large files, signed downloads, range reads, resumable uploads, and progress control. | transfer backend, upload session registry/journal, and pCloud transfer protocol; moves bytes or controls durable upload/download session state | CLI, embedded SDK | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L1047) |
| [`CreateTreePublicLinkFromPaths`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1417) | Create a tree public link by resolving a list of absolute pCloud-drive paths to their remote folder ids on the daemon side. Mirrors the C `ptree_public_link` path-based variant: the daemon walks each path via `listfolder` under the authenticated session, collects the resulting f… | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L1062) |
| [`CreateTreePublicLinkFromPathTargets`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1431) | Create a tree public link by resolving the exact C target shape on the daemon side: optional root folder path, folder paths, and file paths. This is the file/root-capable row 149 route; the older \[`Request::CreateTreePublicLinkFromPaths`\] variant remains as a folder-only compati… | Represent public exposure as typed, revocable objects with explicit expiry/access policy. Good for publishing files/folders, upload drop boxes, link hygiene, and controlled recipients. | public-link backend and typed pCloud public-link protocol; reads or mutates public/upload/tree links and bookmarks on the remote account | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L1067) |
| [`CryptoSetupV2`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1457) | Set up a crypto profile (fresh or post-password-change). Mirrors the C `crypto_setuserkeys` endpoint which is used both for initial setup and for password rotation; the daemon decides which path by inspecting `CryptoShell::is_setup()`. When `backend == Enhanced`, `acknowledge_no… | Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. Good for Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset. | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L669) |
| [`CryptoGetFolderKey`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1497) | Fetch a folder's RSA-OAEP-wrapped `sym_key_ver1` blob from the server, have the daemon unwrap it using the unlocked priv key, and cache the result for subsequent seal/open calls. Mirrors C `crypto_getfolderkey` — see `C_CODE/pclsync/pcryptofolder.c:826`. Hot-path read; rate-limi… | Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. Good for Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset. | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L680) |
| [`CryptoGetFileKey`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1510) | Fetch a file's RSA-OAEP-wrapped `sym_key_ver1` blob from the server, have the daemon unwrap it using the unlocked priv key, and cache the result for subsequent seal/open calls. Mirrors C `crypto_getfilekey` — see `C_CODE/pclsync/pcryptofolder.c:879`. Hot-path read; rate-limit bu… | Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. Good for Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset. | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | CLI | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L681) |
| [`CryptoFolderEnable`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1526) | T2.4.b — opt a folder into the per-folder crypto policy. Pure mutate request: the daemon updates its in-memory `FolderCryptoPolicy` registry and persists the JSON snapshot to the `value_kv` key `crypto.folder_policy.v1`. Does **not** block on auth or crypto unlock — per-folder K… | Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. Good for Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset. | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L272) |
| [`CryptoFolderDisable`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1541) | T2.4.b — opt a folder out of the per-folder crypto policy. Pure mutate request. After removal, `is_encrypted(folder_id)` falls back to inherited state from the parent chain (or plaintext if no ancestor is opted in). Does **not** block on auth or crypto unlock. | Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. Good for Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset. | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L732) |
| [`CryptoFolderList`](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-ipc/src/methods.rs#L1552) | T2.4.b — list the current per-folder crypto policy registry. Pure query request. Response `message` is a JSON-encoded `pcloud_crypto::folder_policy::FolderCryptoPolicy` snapshot (deterministic on-wire form via its `BTreeMap` backing). Does **not** block on auth or crypto unlock. | Require explicit locked/unlocked/key-profile transitions for client-side encrypted data. Good for Crypto setup, unlock, compatibility, rotation, key lookup, and safe lock/reset. | daemon CryptoShell, crypto backend, pcloud-crypto, and optional KMS; changes crypto lifecycle/key state or processes encrypted metadata/content | no direct construction in CLI/SDK crates | [wired runtime arm](https://github.com/ezechiel203/pcloud-rs/blob/main/crates/pcloud-daemon/src/runtime.rs#L274) |
