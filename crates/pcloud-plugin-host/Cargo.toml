[package]
name = "pcloud-plugin-host"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true
description = "Sandboxed plugin host scaffold (T2.5). Capability-bound message-bus model; wasmtime backing tracked for follow-up integration."

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
serde_json = "1"

[lints]
workspace = true
