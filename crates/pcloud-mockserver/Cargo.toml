[package]
name = "pcloud-mockserver"
edition.workspace = true
version.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true
description = "Local in-process HTTP mock of the pCloud REST API for integration tests. No network access required; no production secrets involved."

[lib]
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
