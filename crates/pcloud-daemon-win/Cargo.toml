[package]
name = "pcloud-daemon-win"
description = "Experimental unshipped Windows SCM host for the per-user pcloudd runtime."
version.workspace = true
edition.workspace = true
license.workspace = true
publish = ["kellnr"]
rust-version.workspace = true

[target.'cfg(windows)'.dependencies]
pcloud-daemon = { path = "../pcloud-daemon" }
windows-service = "0.7"

[[bin]]
name = "pcloudd-svc"
path = "src/main.rs"

[lints]
workspace = true
